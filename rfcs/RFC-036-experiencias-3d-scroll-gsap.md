# RFC-036: Experiências 3D (GSAP + Three.js) na trilha Aprender e na Cidade Financeira

- **Status**: em andamento — **Fase A concluída** (v1.60.0): camada 3D
  decorativa na trilha Aprender/Empreender implementada, corrigida (achado
  1 do QA) e reverificada (seção 9), aprovada sem ressalvas bloqueantes.
  **Fase A.3 (riqueza crescente) concluída** (v1.63.0): implementada e
  aprovada com ressalvas não bloqueantes pelo QA Engineer (seção 14) —
  pool de 12 unidades de orçamento (31 objetos-unidade / 39 objetos
  físicos reais confirmados por `scene.traverse()`, dentro do recomendado
  e do teto de 34), validada de forma independente via Chrome headless
  (curva de aparição em resolução mais fina, zona de exclusão de topo por
  bounding box real, FPS combinado com `TrailAvatar3D`, `prefers-reduced-
  motion`, zero acoplamento, paleta, zero erros de console) — 2 achados
  informativos registrados (nenhum bloqueante: divergência de metodologia
  de medição de FPS em ambiente headless sem vsync real, e uma nota de
  harness de teste sobre a classe `.visible`), documentada pelo
  Documentation Specialist (CHANGELOG, README, ROADMAP). Pendência ainda
  aberta (não bloqueante, para uma futura rodada de QA/DevOps): validação
  em hardware móvel físico real e o teste de "pior caso" do bioma
  `correnteza-dourada` com conta/curso reais.
  **Fase B (motor 3D navegável da Cidade Financeira) ainda não iniciada** —
  só as decisões de arquitetura (seção 2) e gamificação (seção 4) estão
  registradas, nenhuma implementação começou.
- **Prioridade**: média (Fase A pode ser tratada como vitória rápida de prioridade alta assim que o Software Architect aprovar o desenho técnico; Fase B é esforço/risco altos — prioridade final a reavaliar depois da análise de arquitetura, não antes)
- **Agentes envolvidos**: Product Owner (esta etapa). Próximas etapas: Software Architect, UX/UI Designer, Gamification Designer (só na Fase B, por mexer no loop central de gameplay da Cidade), Financial Specialist (não aplicável — não há conteúdo financeiro novo, só camada de apresentação), Database Engineer (não aplicável — nenhum schema muda), Backend Engineer, Frontend Engineer, Cyber Security Specialist (obrigatório, por introduzir uma nova dependência via CDN), QA Engineer, Documentation Specialist.

## Descrição

Pedido verbatim do usuário: "PARA MELHORAR O SITE UTILIZE AS SKILS /GSAP, THREE.JS, SCROLL WORLD E 3D GAMES (NA ABA CIDADE E APRENDER)".

Três perguntas de esclarecimento foram feitas e respondidas antes desta RFC:

1. **Aba Aprender**: o usuário escolheu explicitamente **"Camada decorativa 3D atrás da trilha"** — a trilha 2D em zig-zag horizontal (`js/trail.js`, RFC-035 Fase 2, v1.55.0, concluída e validada em 4 fases até v1.58.0) permanece 100% intacta: nós, quiz, XP, energia, revisão periódica a cada 7 lições. Adiciona-se apenas um cenário 3D de fundo (Three.js) com animações amarradas ao scroll (GSAP, provavelmente com o plugin ScrollTrigger).
2. **Aba Cidade**: o usuário escolheu **"Substituir o motor 2D (Phaser) por um jogo 3D completo"** — não é um aprofundamento do overlay 3D já existente (RFC-023/026), é uma reescrita da camada de renderização/interação: câmera livre/navegável em 3D. `js/citylife.js` (estado e regras de negócio: ciclo semanal, aposentadoria, negócios, imóveis) deve ser preservado integralmente.
3. **"Scroll World"**: confirmado como **efeito de parallax/cena 3D reagindo ao scroll da página**, técnica comum em sites premium (estilo Awwwards), via GSAP ScrollTrigger.

## Confirmações de código feitas antes desta RFC

- `README.md` (raiz): stack é HTML/CSS/JS puro sem build, sincronização via Supabase; "Fora do escopo" não menciona 3D/GSAP como limitação técnica — não há impedimento arquitetural conhecido para nenhuma das duas fases, além de performance em mobile.
- `index.html` (linhas 794-849): Phaser (linha 836) e Three.js (linha 839) já são carregados via `<script>` de CDN com **versão travada + Subresource Integrity (SRI)** — padrão estabelecido na auditoria de segurança da RFC-027 ("achado 4"). GSAP, se adotado, deve seguir exatamente o mesmo padrão (não é uma decisão nova, é aplicação de uma regra já em vigor no projeto).
- `js/trail.js` (linhas 1-180 lidas): a trilha unificada (`Trail.levels()`, `Trail.flatLessons()`, `Trail.gridHtml()`, `Trail.nodePosition()`) é 100% independente de qualquer motor gráfico — é HTML/CSS puro com grid. Uma camada decorativa 3D atrás dela (posicionada via CSS, ex. `position: fixed`/`absolute` atrás do container da trilha) não tem nenhum ponto de contato óbvio com a lógica de progresso, desbloqueio ou revisão — reduz o risco da Fase A a "quase puramente aditivo", desde que o Architect confirme isso na prática.
- `js/citygame.js` (linhas 1-150 lidas) e `js/citylife.js` (linhas 1-120 lidas): a separação de responsabilidades **já existe e já é o padrão do projeto** — `CityLife` guarda e evolui todo o estado financeiro simulado (`STORAGE_KEYS.CITY_LIFE`), dispara eventos DOM (`citylife:scenario`, `citylife:aposentadoria`) e expõe métodos `render*Into(container)` que devolvem HTML puro para cada construção (Banco, Universidade, Concessionária, Imobiliária, Escritório). `CityGame` (Phaser) só desenha o mapa, controla proximidade/movimento do jogador e monta o diálogo que hospeda o HTML devolvido por `CityLife`. Essa fronteira já testada em 10 RFCs (017-026) é exatamente o que a Fase B precisa preservar: trocar `CityGame` (e possivelmente `js/citypolvin3d.js`, hoje um overlay 3D pontual só do personagem) por um motor 3D completo, sem tocar em `CityLife` nem nos contratos de evento/método que ele expõe.

## Objetivo

Duas iniciativas independentes, sob o mesmo guarda-chuva de "modernização visual com GSAP/Three.js", com riscos e esforços muito diferentes:

- **Fase A (Aprender)**: adicionar uma camada 3D decorativa de fundo com Three.js, animada por scroll via GSAP/ScrollTrigger, sem alterar nenhuma lógica de progresso/quiz/energia/revisão já em produção.
- **Fase B (Cidade)**: substituir o motor de renderização 2D (Phaser) por um jogo 3D navegável, preservando 100% a lógica de negócio de `js/citylife.js` — troca de "como a Cidade é desenhada e como o jogador se move nela", nunca do que ela significa ou registra.

## Motivação

- Pedido explícito e já esclarecido do usuário (dono do produto), motivado por elevar a percepção de qualidade/modernidade do site.
- Já existe precedente arquitetural direto: Three.js roda em produção desde a RFC-023 (overlay 3D do PolvIn dentro da Cidade) — não é uma dependência nova e desconhecida, é uma dependência já validada, só sendo usada de forma mais ambiciosa.
- Separar em fases por risco é o mesmo padrão que já funcionou nas RFC-017 a RFC-026 (Cidade Financeira, construída em 10 incrementos) e na RFC-035 (trilha, 4 fases) — não há motivo para tratar este pedido como uma entrega monolítica.

## Benefícios

- Ganho de apelo visual em duas das abas centrais do app (Aprender é a aba de maior uso recorrente; Cidade é a aba de maior "momento uau" hoje).
- Fase A é de baixo risco, isolada e reversível — pode virar uma "vitória rápida" (mesmo espírito de RFC-035 Fases 1/2).
- Fase B, mesmo sendo de alto risco/esforço, tem um caminho seguro porque a fronteira `CityLife` (regra) x `CityGame` (apresentação) já existe e já foi exercitada por 10 RFCs anteriores — não é preciso inventar essa separação, só respeitá-la.

## Impacto

- `js/trail.js` — nenhuma mudança de lógica esperada na Fase A; possíveis hooks de leitura de posição de scroll para alimentar a cena 3D (decisão de arquitetura, não de produto).
- `js/business.js` — reaproveita literalmente o mesmo layout/CSS de `trail.js` (zig-zag horizontal, RFC-035 Fase 2). Precisa de decisão explícita (Software Architect + UX/UI Designer) sobre se a trilha Empreender também ganha a camada decorativa 3D nesta fase ou fica de fora — não pode divergir silenciosamente.
- `css/style.css` — novo container para a camada decorativa (Fase A).
- `index.html` — novo(s) `<script>` de CDN para GSAP (core + plugin ScrollTrigger), com versão travada e SRI, no mesmo padrão de Supabase/Phaser/Three.js.
- `js/citygame.js` — candidato a reescrita total ou parcial (Fase B).
- `js/citypolvin3d.js` — pode deixar de fazer sentido como overlay pontual se toda a cena já passar a ser 3D; decisão do Software Architect (absorver, substituir ou manter coexistindo).
- `js/citylife.js` — **não deve ser tocado** nesta iniciativa, nas duas fases.
- Performance/mobile — Three.js já é carregado hoje só na aba Cidade; a Fase A passa a carregá-lo também potencialmente na aba Aprender, uma das mais visitadas do app, incluindo por usuários mobile de aparelhos mais fracos (público-alvo do PolvIn, ver `README.md`).

## Dependências

- Fase A depende da RFC-035 (trilha zig-zag horizontal, concluída v1.55.0-v1.58.0) permanecer estável — não deve ser tocada por esta iniciativa.
- Fase B depende de um mapeamento completo, pelo Software Architect, de tudo que `js/citygame.js` faz hoje antes de qualquer código: mapa/mundo (`WORLD_W/H`), câmera, 5 construções e seus diálogos (`BUILDINGS`), clima reativo ao cenário econômico (RFC-024), ciclo dia/noite decorativo (RFC-024), celebração de aposentadoria (RFC-025).
- Ambas dependem de validação do Cyber Security Specialist para a introdução do GSAP via CDN (SRI hash, versão travada) antes do primeiro deploy — mesmo processo já usado para as libs existentes.

## Critérios de aceite

1. `js/citylife.js` (estado e regras de negócio da Cidade: ciclo semanal, aposentadoria, negócios, imóveis, emprego) **não pode ser reescrito** nesta iniciativa — só a camada de apresentação (`js/citygame.js` e/ou `js/citypolvin3d.js`) pode mudar.
2. O sistema de progresso/quiz/energia/revisões periódicas da trilha Aprender (RFC-035, já em produção, v1.55.0-v1.58.0) **não pode regredir** — nenhuma mudança em `isUnlocked()`, `isDone()`, `progressKey()`, `Trail.levels()`/`flatLessons()`, `COURSE_REVIEWS`/`COURSE_PROGRESS`/`HISTORY_PROGRESS`.
3. Qualquer dependência nova (GSAP, e qualquer plugin seu) deve ser adicionada via CDN com **versão travada + Subresource Integrity (SRI)**, seguindo exatamente o padrão já usado para Supabase/Phaser/Three.js (RFC-027, achado 4) — sem exceção.
4. Performance: as cenas 3D adicionais não podem degradar de forma perceptível o tempo de carregamento/interatividade em dispositivos móveis mais fracos — critério objetivo (ex.: orçamento de FPS/tempo de carregamento mínimo) a ser definido pelo Software Architect/QA Engineer antes da implementação.
5. A camada decorativa da Fase A deve respeitar `prefers-reduced-motion`, como toda a identidade visual atual do PolvIn (RFC-008, RFC-035).
6. A decisão de incluir ou não a trilha Empreender (`js/business.js`) na camada decorativa da Fase A deve ser explícita e registrada — não pode divergir silenciosamente do que acontece em `js/trail.js`.
7. A Fase B deve preservar as 5 interações/construções hoje existentes (`CityGame.BUILDINGS`: Banco, Universidade, Concessionária, Imobiliária, Escritório) e os contratos de evento já usados por integrações existentes (`citylife:scenario`, `citylife:aposentadoria`) sem quebra.
8. A Fase B deve ter um MVP explicitamente definido e menor que "portar 100% das interações de uma vez" (ex.: primeiro só o cenário/chão navegável em 3D com os elementos básicos, preservando toda a lógica de `citylife.js`, antes de portar cada interação Phaser específica) — a ser detalhado pelo Software Architect.
9. Nenhuma das duas fases pode remover, contornar ou enfraquecer a exigência de conta/Supabase como fonte de verdade dos dados (RFC-027) nem qualquer sincronização já existente.
10. Nenhuma parte desta iniciativa depende de infraestrutura de backend adicional — continua 100% estático/client-side, coerente com a arquitetura descrita no `README.md`.
11. **(Fase A.3, novo)** A cena decorativa (`js/trailscene3d.js`) ganha elementos visuais adicionais de "riqueza" cuja densidade/quantidade/proximidade aumenta conforme o valor de `progress` (0→1) avança — mensurável objetivamente (contagem/escala/opacidade de objetos visíveis em `progress=0` deve ser sensivelmente menor que em `progress=1`).
12. **(Fase A.3, novo)** Por padrão (MVP desta fase), `progress` continua sendo exclusivamente a métrica de scroll de página já existente (0=topo, 1=fim da página) — nenhuma leitura de `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS` é introduzida, preservando o critério de aceite 2 já vigente nesta RFC. Qualquer mudança para progresso real do jogador (XP/% lições/nível) exige decisão explícita e justificada do Software Architect + Product Owner, seguindo o mesmo processo de exceção documentada já usado na RFC-037 (avatar navegável) — nunca uma decisão silenciosa do Frontend Engineer.
13. **(Fase A.3, novo)** Os novos elementos de "riqueza" seguem 100% a linguagem visual já estabelecida: cel-shading (`MeshToonMaterial`+`gradientMap`), geometria procedural baixo-poly, paleta já existente em `:root` (nenhuma cor nova), sem textura de imagem externa, sem asset `.glb`/`.gltf`. Nenhuma imagem realista de riqueza (iate, mansão, carro de luxo) — mantém a identidade "Correnteza do Conhecimento" (oceano estilizado do PolvIn).
14. **(Fase A.3, novo)** Nenhum elemento novo pode ultrapassar o teto de orçamento de performance já definido pelo Software Architect (30-40 meshes simultâneos, RFC-036 seção 2) — a contagem total (19 objetos já existentes + novos) deve ser revalidada e aprovada explicitamente pelo Software Architect/QA Engineer antes de produção.
15. **(Fase A.3, novo)** `prefers-reduced-motion` continua respeitado — o "nível de riqueza" exibido no frame estático (hoje travado em `progress=0`) deve ser definido explicitamente pelo UX/UI Designer, não assumido por omissão pelo Frontend Engineer.
16. **(Fase A.3, novo)** Nenhuma recompensa de gamificação (XP, moeda, badge) é concedida por causa da cena ficar "mais rica" — é puramente decorativo, mesmo princípio já usado para as celebrações da RFC-037 (critério 10 daquela RFC). Não pode virar um sistema de recompensa paralelo a `js/energy.js`/`js/achievements.js`/XP-moedas.

## Etapas puladas e por quê

- **Financial Specialist**: não aplicável às duas fases desta RFC — nenhum conteúdo financeiro/educativo novo é criado ou alterado, é puramente camada de apresentação (3D/scroll) sobre conteúdo já existente e já validado por este agente em RFCs anteriores. Justificativa já registrada no cabeçalho desta RFC (linha 5) desde a etapa 1 (Product Owner).
- **Database Engineer**: não aplicável — nenhuma das duas fases introduz ou altera `STORAGE_KEYS`, schema do Supabase ou qualquer estrutura de dado persistido, confirmado explicitamente pelo Software Architect (etapa 2) e reconfirmado pelo Gamification Designer (etapa 4) e pelo Frontend Engineer (etapa 6, "Não tocados"). Justificativa já registrada no cabeçalho desta RFC (linha 5) desde a etapa 1.
- **Backend Engineer**: não pulado — a Fase A implementada até agora (etapas 1-6) é inteiramente client-side/apresentação, sem necessidade de trabalho de "regra de negócio nos bastidores" (papel do Backend Engineer, conforme `.claude/agents/backend-engineer.md`). O Software Architect (etapa 2) atribuiu a este agente apenas um detalhe futuro da Fase B (a flag `CITY_3D_ENABLED`, se lida de `Store`/query string) — isso permanece pendente, a ser retomado quando a Fase B começar, não uma etapa pulada da Fase A.

Nenhuma etapa foi pulada sem justificativa dentro do escopo já executado (Fase A, etapas 1-6). A Fase B (Cidade 3D) ainda não teve nenhuma etapa de implementação iniciada — não há "pulo" a justificar ali, é trabalho futuro conforme o faseamento definido pelo Software Architect (etapa 2, "Faseamento de execução recomendado").

## Registro por etapa

### 1. Product Owner

- **Resumo da etapa**: interpretação e escopo do pedido do usuário ("melhorar o site com GSAP, Three.js, scroll world e jogos 3D na Cidade e no Aprender"), confirmado via 3 perguntas de esclarecimento já respondidas. Proposto faseamento por risco: Fase A (Aprender — camada decorativa 3D + scroll, baixo risco, aditiva) e Fase B (Cidade — substituição do motor de renderização 2D por 3D navegável, alto risco/esforço, exige MVP reduzido antes de portar todas as interações). Confirmado por leitura direta de código que a separação necessária para a Fase B (regra de negócio em `js/citylife.js` x apresentação em `js/citygame.js`/`js/citypolvin3d.js`) já existe hoje e já é o padrão consolidado do projeto desde a RFC-017.
- **Decisões tomadas**:
  - Fase A não deve alterar nenhuma lógica de `js/trail.js`, apenas adicionar uma camada visual aditiva.
  - Fase B deve ser tratada como reescrita de motor de apresentação, nunca de lógica de negócio — `js/citylife.js` é intocável nesta iniciativa.
  - Fase B precisa de um MVP explicitamente menor que o escopo final (primeiro o cenário/chão 3D navegável com elementos básicos, antes de portar cada interação Phaser específica).
  - Qualquer nova dependência de CDN (GSAP) segue o padrão de segurança já estabelecido no projeto (versão travada + SRI) — não é uma decisão nova, é aplicação de uma regra já em vigor.
  - A trilha Empreender (`js/business.js`) não foi automaticamente incluída no escopo da Fase A — decisão explícita fica para a próxima etapa, para não haver divergência silenciosa entre as duas trilhas que hoje compartilham o mesmo layout.
- **Pendências** (decisões técnicas que cabem ao Software Architect, não a este agente):
  - Como estruturar tecnicamente a Fase B: Three.js puro ou uma lib de jogo 3D adicional (ex.: um framework sobre WebGL); como isolar a troca de motor da lógica de negócio existente na prática (ex.: manter os métodos `render*Into(container)` de `CityLife` como estão, ou adaptar a interface).
  - Orçamento de performance objetivo (FPS mínimo, tempo de carregamento máximo) para dispositivos móveis mais fracos, nas duas fases.
  - Decisão sobre o destino de `js/citypolvin3d.js` (overlay 3D pontual do PolvIn) diante de uma Cidade inteiramente 3D — manter, absorver ou substituir.
  - Decisão sobre incluir ou não a trilha Empreender na camada decorativa da Fase A.
  - Detalhamento do MVP exato da Fase B (quais das 5 construções entram no primeiro incremento navegável em 3D).
- **Riscos**:
  - Fase B é a maior mudança de arquitetura de apresentação já proposta neste projeto para a Cidade Financeira — risco de regressão em qualquer uma das features construídas ao longo de 10 RFCs (017-026) se a fronteira `CityLife`/`CityGame` não for respeitada rigorosamente.
  - Performance em mobile: duas cenas 3D simultâneas em abas de alto uso (Aprender e Cidade) podem degradar a experiência em aparelhos mais fracos, especialmente combinadas (GSAP + Three.js + Phaser, se ainda coexistir durante a transição da Fase B).
  - Escopo "3D games" do pedido original é vago — sem uma definição clara de MVP pelo Software Architect, a Fase B corre risco de virar um projeto sem fim, ao contrário do padrão de entregas incrementais já validado neste projeto.
  - Introduzir GSAP como nova dependência de terceiros aumenta a superfície de auditoria de segurança (mais uma lib via CDN) — mitigado pelo padrão de SRI já em vigor, mas precisa ser formalmente revisado pelo Cyber Security Specialist antes do primeiro deploy.
- **Próximo agente responsável**: Software Architect

### 2. Software Architect

- **Resumo da etapa**: leitura completa de `index.html` (linhas 794-849, ordem e padrão de carregamento via CDN), `js/trail.js` (603 linhas), `js/citygame.js` (742 linhas), `js/citylife.js` (852 linhas), `js/citypolvin3d.js` (247 linhas), `js/fx.js` (325 linhas), `js/business.js` (trecho inicial) e `css/style.css` (trilha, tabs, z-index em uso) para embasar as duas decisões de arquitetura pedidas. Confirmações relevantes feitas por leitura direta de código (não por suposição):
  - `js/tabs.js` (`Tabs.go`) não tem lifecycle de "sair da aba" — só faz `classList.toggle("hidden")` nos `.tab-panel` e dispara `tab:changed`. Não existe destroy/cleanup automático de módulo nenhum hoje: `CityGame.init()` roda 1x (guard `if (this.game...) return`) e o `Phaser.Game` continua rodando seu próprio loop mesmo com a aba Cidade escondida — é o precedente real do projeto, não um objetivo a copiar às cegas (ver risco abaixo).
  - A trilha (`#trailContainer`, dentro de `#tab-aprender`) **não tem scroll próprio**: `html`/`body` só têm `overflow-x:hidden` (`css/style.css` linhas 49-64), não há `overflow-y` em nenhum ancestral da trilha, e `Tabs.go()` usa `window.scrollTo()` — confirma a suposição do Product Owner (seção 21 da RFC) e resolve a maior incerteza técnica da Fase A: **GSAP ScrollTrigger pode escutar o scroll nativo da janela (comportamento padrão do plugin, sem precisar de `scroller:` customizado)**, sem nenhum conflito com scroll de container.
  - `js/business.js` reaproveita literalmente o mesmo HTML/CSS de `trail.js` (`.trail-*`) e vive **dentro do mesmo painel** `#tab-aprender` (`academiaTrilhaContent`/`academiaEmpreenderContent`, alternados por clique num chip, não por `Tabs.go`/`tab:changed`) — ver `index.html` linhas 509-520.
  - O projeto já usa uma escala de `z-index` até 9999 (tooltip) e 100-300 para telas de gate/onboarding/modais (`css/style.css`); não há nenhum elemento com `position:fixed` de z-index negativo hoje.
  - Three.js (`build/three.min.js`, UMD) já é carregado incondicionalmente em toda página, independente da aba ativa — **a Fase A não adiciona peso de download novo de Three.js**, só de GSAP. O build UMD usado **não inclui os módulos de exemplo** (`OrbitControls`, `PointerLockControls`, `GLTFLoader` etc. vivem em `three/examples/jsm/*`, ES modules) — o projeto não usa import maps nem build step, então esses módulos de exemplo **não estão disponíveis** sem introduzir um mecanismo de módulos ES novo (mudança de arquitetura maior, fora do escopo desta RFC).

#### Fase A — decisão de arquitetura

- **Novo módulo**: `js/trailscene3d.js` (nome análogo a `citypolvin3d.js` — mesmo padrão "nome da tela + 3d"). Objeto global `TrailScene3D` com `init()`, `setActive(bool)`, `render()`/loop interno, seguindo o padrão de todo módulo do projeto (`const X = { ... }`).
- **Onde entra na lista de `<script>`** (`index.html`): dois blocos novos.
  1. GSAP: adicionar `gsap.min.js` **e** `ScrollTrigger.min.js` como CDN com versão travada + SRI, agrupados junto ao bloco de "motores" já existente (Phaser/Three.js, linhas 834-839) — **antes** de `js/citypolvin3d.js`. `ScrollTrigger.min.js` precisa vir depois de `gsap.min.js` (o plugin estende o core) e antes de qualquer módulo que chame `gsap.registerPlugin(ScrollTrigger)`.
  2. `js/trailscene3d.js`: entra **depois** do bloco Three.js/GSAP e depois de `js/citygame.js` (mesma "zona de motores 3D/jogo" do arquivo, por convenção de agrupamento — não por dependência real). Não depende de `js/trail.js` nem `js/business.js` estarem carregados antes (é desacoplado de propósito, ver abaixo) — a ordem em relação a eles é irrelevante.
  - Dependências reais em tempo de execução (resolvidas dentro de métodos chamados por `App.init()`, não no top-level do arquivo, então a ordem exata do `<script>` só importa para leitura/convenção, não para funcionamento): `THREE` e `gsap`/`ScrollTrigger` globais precisam existir antes de `TrailScene3D.init()` ser chamado — garantido por estarem todos como `<script>` síncronos antes de `js/app.js`.
- **Onde a cena vive no DOM**: **não** dentro de `#trailContainer` nem de `#tab-aprender`. Um elemento novo, criado 1x via JS e anexado como filho direto de `<body>` (irmão do wrapper `.app`), fora de qualquer `.tab-panel` — evita o problema já documentado no próprio `trail.js` (comentário de `observeReveal()`, linhas 282-290) de elementos dentro de um painel `display:none` terem geometria zero. Fixed, cobrindo o viewport inteiro:
  ```css
  #trailScene3d { position: fixed; inset: 0; z-index: -1; pointer-events: none; }
  ```
  `z-index: -1` (não `0`) é deliberado: como o elemento é filho direto de `body` (que não tem `transform`/`filter`/`opacity` aplicados, confirmado em `css/style.css` linhas 54-64), ele fica garantidamente atrás de **todo** o conteúdo estático normal da página sem exigir tocar o CSS de mais nenhum componente existente — a alternativa (`z-index:0` + promover `#tab-aprender` e tudo dentro dele a `position:relative`) exigiria auditar cada componente da trilha por cima e é desnecessariamente arriscada para uma camada puramente decorativa. `pointer-events:none` é redundante com o z-index negativo, mas mantido como segunda camada de proteção (mesmo padrão de `.city-game-polvin3d`, `css/style.css` linha 1467).
- **Escopo dentro da aba Aprender (resolve a pendência 5 do Product Owner)**: **sim, inclui Empreender.** Não é uma decisão de conteúdo — é consequência direta da arquitetura: como a camada é anexada fora de qualquer `.tab-panel` e sua visibilidade é controlada por `tab:changed` no nível da ABA (`tabId === "aprender"`), e tanto a trilha financeira/história quanto `#businessTrailContainer` (Empreender) vivem dentro do MESMO `#tab-aprender` (`index.html` linhas 509-520), a camada decorativa aparece atrás dos dois automaticamente, sem branch extra de código. Divergir exigiria trabalho a mais (checar qual chip do `#academiaSubnav` está ativo), não menos — a opção "não incluir Empreender" é a que exigiria esforço extra e fica descartada por isso, não só por preferência visual. UX/UI Designer pode, se quiser, variar sutilmente a paleta/tema entre os dois sub-modos lendo o mesmo evento de clique do chip (`#academiaSubnav`), mas isso é decisão de composição visual, não de arquitetura.
- **Init/destroy ao trocar de aba (evitar vazamento de memória)**: **pausar, não destruir**, e inicializar **lazy** (só na primeira vez que a aba Aprender é aberta, não no boot do app):
  - `document.addEventListener("tab:changed", (e) => { if (e.detail.tabId === "aprender") TrailScene3D.activate(); else TrailScene3D.deactivate(); })`.
  - `activate()`: se a cena ainda não existe, cria (`init()` 1x: renderer, scene, camera, objetos); sempre que ativa, roda `ScrollTrigger.refresh()` (a altura da página pode ter mudado desde a última vez — a trilha re-renderiza com `Trail.render()` a cada `course:updated`) e retoma o loop de `requestAnimationFrame`.
  - `deactivate()`: `cancelAnimationFrame` do loop próprio (não o de nenhum outro módulo) e `ScrollTrigger`s pausados (`.disable()`, não `.kill()`). **Não** chama `renderer.dispose()`/perde o contexto WebGL a cada troca de aba — criar/destruir um `WebGLRenderingContext` repetidamente é caro e o navegador tem um limite de contextos simultâneos por página (o projeto já usa 2: `CityPolvin3D` e o Phaser do `CityGame`); pausar o loop é suficiente para o objetivo real (não gastar CPU/GPU/bateria com uma aba invisível) sem pagar esse custo a cada clique de aba. `document.visibilitychange` (aba do navegador/app em segundo plano) usa o mesmo `deactivate()`/`activate()`, mesmo motivo.
  - Isso é uma escolha deliberadamente diferente do padrão observado em `CityGame`/Phaser (que nunca pausa) — não replicar esse comportamento aqui é intencional: Aprender é a aba de maior uso recorrente do app (Descrição da RFC, Product Owner), então deixar um loop de render rodando indefinidamente em segundo plano teria custo real de bateria/CPU em mobile, ao contrário da Cidade.
- **`prefers-reduced-motion`** (critério de aceite 5): checado 1x em `activate()` via `window.matchMedia` (mesmo helper que `Fx.prefersReducedMotion()` já usa, reaproveitar a mesma media query, não duplicar lógica) — se ativo, a cena renderiza 1 frame estático (sem loop de `requestAnimationFrame`, sem `ScrollTrigger` de movimento) em vez de desligar inteiramente a camada visual. Frontend Engineer decide o frame estático exato com o UX/UI Designer.
- **Acoplamento com `trail.js`/`business.js`**: **zero**, por design — `TrailScene3D` nunca importa/lê `Trail.*`/`Business.*` nem escuta `course:updated`/`lesson:passed`/`xp:updated`. Só reage a `tab:changed`, `resize` (redimensionar o renderer) e ao scroll da janela via `ScrollTrigger`. Isso é o que torna a Fase A "quase puramente aditiva" na prática, não só na intenção — não há nenhum ponto em que um bug na cena decorativa possa tocar `isUnlocked()`/`isDone()`/`progressKey()` ou qualquer chave de `STORAGE_KEYS` (critério de aceite 2).
- **Novo `STORAGE_KEYS` / novo `CustomEvent`**: **nenhum**. Não há estado para persistir (a cena não guarda preferência própria — lê `prefers-reduced-motion` ao vivo) e nenhum outro módulo precisa reagir à camada decorativa, então não há necessidade de um evento novo no `document`.
- **Orçamento de performance (Fase A)**: ver seção comum abaixo.

#### Fase B — decisão de arquitetura

- **Three.js puro, sem lib adicional.** Motivo, além do já levantado pelo Product Owner (minimizar superfície de auditoria): o próprio `js/citypolvin3d.js` já prova, em produção, que o projeto consegue construir um personagem articulado com sombreamento toon e animação procedural em Three.js puro, sem nenhuma engine de jogo por cima — o "jogo" de que a Fase B precisa (mundo navegável + 5 pontos de interação + diálogo) é estruturalmente mais simples que isso, não mais complexo. Uma lib de jogo 3D adicional (ex. um framework tipo PlayCanvas/Babylon) resolveria problemas que este projeto não tem (física complexa, animação por esqueleto importado de asset externo) e traria os problemas que o projeto está ativamente evitando: mais uma dependência CDN para o Cyber Security Specialist auditar, mais uma versão para travar/gerar SRI, mais peso de download. Rejeitada.
  - Consequência prática já sinalizada acima: como os módulos de exemplo do Three.js (`OrbitControls` etc.) não estão disponíveis nesse build UMD sem ES modules, **câmera e controles de movimento precisam ser código próprio**, não uma lib pronta — mas isso já é exatamente o que `js/citygame.js` faz hoje para o Phaser (`update()`, câmera `startFollow`/`pan` nativas do Phaser, movimento por clique com lerp manual) — o esforço de "escrever a própria câmera/movimento" não é um custo novo introduzido pela Fase B, é o mesmo tipo de trabalho que o projeto já paga hoje, só que com a API do Three.js em vez da do Phaser.
- **Como isolar a troca de motor da lógica de negócio**: **não tocar a interface pública de `CityLife`** — os métodos `render*Into(container)` continuam recebendo um elemento DOM qualquer e devolvendo/injetando HTML puro; são agnósticos de motor gráfico por natureza (já são hoje — `CityLife` não sabe que existe um Phaser). O ponto de acoplamento a preservar é o **shape do array `BUILDINGS`** de `js/citygame.js` (`id, x, y, promptText, borderColor, draw(scene,b), open(container)`) — a Fase B generaliza esse mesmo shape para 3D (`x,y,z` ou `x,z` com `y` fixo no chão + `draw(scene3d, b)` reescrito em API do Three.js), mantendo `promptText`/`open(container)` **idênticos**: `open(container) { CityLife.renderCicloInto(container); }` etc. não muda uma linha. Isso é o que garante o critério de aceite 7 (as 5 construções e os eventos `citylife:scenario`/`citylife:aposentadoria` sobrevivem) sem exigir nenhuma mudança em `citylife.js`.
- **Módulo novo, não reescrita in-place**: `js/citygame3d.js` (motor novo) coexiste com `js/citygame.js` (Phaser, atual) durante toda a transição, atrás de 1 flag de decisão no boot (`js/app.js`): `const CITY_3D_ENABLED = false;` (ou, se for útil para QA comparar os dois lado a lado durante o desenvolvimento, uma flag lida de `Store`/query string — decisão de detalhe para o Backend Engineer, não estrutural). `App.init()` chama exatamente um dos dois: `(CITY_3D_ENABLED ? CityGame3D : CityGame).init()`. Isso permite fazer o cutover em incrementos reais e reverter instantaneamente se algo quebrar, sem branch de git separada. Só quando `js/citygame3d.js` atingir paridade das 5 construções (Fase B final) é que `js/citygame.js` e a dependência do Phaser em `index.html` são removidos — nunca antes.
- **Destino de `js/citypolvin3d.js`**: **absorvido, não mantido como módulo separado.** A técnica de projeção usada por `CityPolvin3D` (`relX/relY` calculados a partir da posição do mundo 2D do Phaser, projetados numa câmera ortográfica separada, compositada por cima do canvas do Phaser — `setScreenPosition()`, linhas 204-210) deixa de fazer sentido quando o próprio mundo já é 3D: o PolvIn passa a ser um `Object3D` de verdade dentro da MESMA cena/mesma câmera dos prédios e do chão, com posição em coordenadas de mundo reais, não mais projetado de um sistema de coordenadas para outro. Recomendação concreta: `js/citygame3d.js` **porta o código de construção do rig** (`buildRig()`, `buildGradientMap()`, `toonMat()`, a animação de tentáculos) de `citypolvin3d.js` quase literalmente — é geometria procedural boa e testada, sem motivo para reescrever do zero — mas descarta a camada de composição por overlay (canvas próprio, câmera ortográfica separada, `render(opts)` recebendo `relX/relY`). `js/citypolvin3d.js` como arquivo/módulo é removido **no mesmo commit** que remove `js/citygame.js` (cutover final da Fase B), não antes — enquanto o Phaser ainda estiver ativo atrás da flag, ele continua precisando do overlay como está hoje.
- **MVP recomendado (resolve a pendência 6 do Product Owner)**: **mundo/chão 3D navegável + só o Banco**, não "mundo vazio sem nenhuma construção" nem "todas as 5 de uma vez". Justificativa: um mundo 3D sem nenhum ponto de interação não valida a parte mais arriscada do contrato (`building.open(container)` hospedando HTML do `CityLife` dentro de uma cena 3D, reação a `citylife:scenario`/`citylife:aposentadoria`, HUD de idade) — só "andar" não prova que a fronteira `CityLife`/apresentação sobrevive à troca de motor. O Banco é a construção certa para validar primeiro porque hospeda o Ciclo Semanal (`renderCicloInto`), o loop central do jogo e o único painel que reage às DUAS mensagens de evento existentes (`citylife:scenario` toda semana, `citylife:aposentadoria` 1x) — se o pipeline inteiro funciona para o Banco, as outras 4 construções são, estruturalmente, o mesmo padrão repetido com HTML diferente (`renderEducacaoInto`, `renderVeiculosInto`, `renderImoveisInto`, `renderTrabalhoInto` já seguem exatamente a mesma forma `open(container)`/`_activeContainer`/`_activeRenderer` em `citylife.js`).
- **Câmera e movimento — sinalização, não decisão unilateral**: a Descrição desta RFC (Product Owner) fala em "câmera livre/navegável"; recomendo, só para o MVP, uma câmera 3ª-pessoa fixa em ângulo (perspectiva elevada, segue o jogador com lerp, sem órbita/zoom livre do usuário) — mesma sensação de "navegável" que o Phaser já dá hoje, mas sem os problemas novos que uma câmera de fato livre traria (colisão de câmera com prédios, desorientação em mobile, muito mais código de controle para validar em pouco tempo). Movimento: clique/toque no chão continua movendo o jogador até o ponto (mesmo padrão de `pointerdown` + lerp de `CityGame.update()`), só trocando `scene.cameras.main.getWorldPoint()` (Phaser) por raycasting do Three.js contra um plano de chão. Isso preserva a UX já validada em 10 RFCs anteriores. Encaminho como recomendação a confirmar com UX/UI Designer e Gamification Designer (próxima etapa) — não é uma decisão que um Software Architect deva travar sozinho, já que toca em como o jogo é *sentido/jogado*, mas sinalizo a recomendação para não deixar em aberto sem nenhum ponto de partida.
- **Novo `STORAGE_KEYS` / novo `CustomEvent`**: nenhuma chave de storage nova (o estado continua 100% em `STORAGE_KEYS.CITY_LIFE`, intocado). Nenhum evento novo no `document` — `citygame3d.js` escuta os DOIS eventos que já existem (`citylife:scenario`, `citylife:aposentadoria`) exatamente como `citygame.js` faz hoje.

#### Orçamento de performance mobile (as duas fases — critério objetivo para QA)

- **FPS**: mínimo sustentado de **30fps** em aparelho Android de entrada/médio (referência: Moto G/Redmi de geração atual, não topo de linha — público-alvo do PolvIn conforme `README.md`); alvo de 60fps em desktop e mobile topo de linha. Medido pelo QA Engineer via DevTools/Performance tab ou `requestAnimationFrame` timestamp logging, não por sensação subjetiva.
- **Pixel ratio**: capado em `Math.min(window.devicePixelRatio || 1, 2)` nas duas fases — mesmo valor já usado por `CityPolvin3D.init()` (linha 42), não um número novo a inventar.
- **Orçamento de objetos**:
  - Fase A: cena decorativa com no máximo ~30-40 meshes/partículas simultâneos (mesma ordem de grandeza do rig completo do PolvIn 3D atual, que já roda bem em produção) — geometria procedural simples (esferas, cilindros, planos), sem texturas de imagem carregadas (só `CanvasTexture` gerada em runtime, mesmo truque do `gradientMap` de `citypolvin3d.js`), sem novo asset binário.
  - Fase B (MVP: chão + Banco + PolvIn): orçamento comparável a "1x o rig do PolvIn (~30-40 meshes) + 1 construção simples (~10-20 meshes) + chão/mar (poucos meshes)" — a cada construção nova incorporada, o orçamento total é revisto, não some automaticamente.
- **Tempo de carregamento**: Fase A não pode adicionar mais que **~50KB comprimido** de JS de terceiros novos (GSAP core + ScrollTrigger, versões atuais somadas ficam bem abaixo disso) — Three.js já é custo afundado, carregado hoje independente da aba. Fase B **não adiciona nenhuma dependência nova** (Three.js já pago), então o único custo de carregamento novo é o próprio `js/citygame3d.js` (código-fonte do projeto, mesma ordem de tamanho de `js/citygame.js`, ~700 linhas) — sem novo `<script>` de CDN.
- **Sem asset pipeline novo**: nenhuma das duas fases pode introduzir arquivos `.glb`/`.gltf`/texturas de imagem externas nem um loader para eles — mantém a regra já em vigor (`citypolvin3d.js`) de geometria 100% procedural. Introduzir um pipeline de assets 3D é decisão de infraestrutura maior (hospedagem de binários, ferramenta de otimização de asset) fora do escopo desta RFC; se algum dia for necessário, precisa ser escalado ao Product Owner como proposta própria, não decidido de passagem aqui.

#### Faseamento de execução recomendado (RFCs de implementação separadas, mesmo espírito de RFC-025/026/035)

- **Fase A.1** — infraestrutura + cena mínima sem scroll: GSAP+ScrollTrigger via CDN (SRI, revisado pelo Cyber Security Specialist antes de qualquer merge), `js/trailscene3d.js` com o ciclo `activate()/deactivate()` completo e uma cena estática de validação (sem composição visual final). Critério de saída: abrir/fechar a aba Aprender repetidamente sem crescimento de memória (QA via DevTools), zero regressão visual/funcional na trilha (critérios de aceite 1-2), `prefers-reduced-motion` respeitado.
- **Fase A.2** — composição visual + scroll de verdade: UX/UI Designer define a estética (tema, paleta, formas) sobre a fundação da A.1; `ScrollTrigger` liga a cena ao scroll real da página; validação de orçamento de performance mobile pelo QA Engineer (critério de aceite 4).
- **Fase B.1** — fundação isolada: `js/citygame3d.js` com chão navegável, câmera de acompanhamento, movimento por clique — **sem nenhuma construção ainda**, atrás da flag `CITY_3D_ENABLED` (default desligada). Objetivo único: validar performance/sensação de controle antes de integrar qualquer HTML do `CityLife`.
- **Fase B.2** — Banco (MVP funcional completo): proximidade + diálogo hospedando `CityLife.renderCicloInto`, reação a `citylife:scenario` (mar/clima) e `citylife:aposentadoria` (HUD de idade, celebração), PolvIn 3D reparentado para o mundo real (absorve `citypolvin3d.js`). Ainda atrás da flag.
- **Fase B.3** — Escritório (emprego/negócio, segundo loop mais central).
- **Fase B.4** — Universidade + Concessionária + Imobiliária (mesmo padrão de "grid de cards", podem ser agrupadas numa única RFC de implementação se o esforço se confirmar baixo depois da B.2/B.3).
- **Fase B.5** — Cutover final: flag vira padrão ligada, remoção de `js/citygame.js`, `js/citypolvin3d.js` e do `<script>` do Phaser em `index.html` (reduz superfície de auditoria — benefício colateral de segurança).

- **Riscos estruturais identificados**:
  - O maior risco da Fase A não é técnico, é de **disciplina de acoplamento**: nada impede, na prática, que um Frontend Engineer apressado faça `TrailScene3D` ler `Learn.getXp()` ou reagir a `course:updated` "só para dar um efeito legal" — isso violaria o critério de aceite 2 por trás das costas do processo. Sinalizo explicitamente para o Backend/Frontend Engineer e para o QA Engineer: qualquer `import`/leitura de `Trail.*`/`Business.*`/`Learn.*` dentro de `js/trailscene3d.js` é um desvio da arquitetura definida aqui, não uma otimização.
  - A recomendação de câmera fixa (não livre) para o MVP da Fase B é uma redução de escopo em relação à Descrição original da RFC ("câmera livre/navegável") — preciso que o Gamification Designer e o UX/UI Designer confirmem que essa redução ainda entrega "sensação de jogo 3D navegável" antes de travar como decisão final; se discordarem, câmera livre volta à mesa como trabalho adicional da Fase B, não um bloqueio da B.1.
  - Rodar Phaser (`CityGame`) e o novo `CityGame3D` (Three.js) potencialmente carregados ao mesmo tempo durante a transição (ambos os `<script>` continuam no `index.html` até o cutover final) mantém 2 motores de jogo em memória mesmo que só 1 esteja ativo por vez — aceitável como custo temporário da estratégia de flag/rollback, mas deve ser reavaliado pelo QA Engineer a cada incremento da Fase B (garantir que o motor INATIVO não está de fato instanciando nada, só definindo o objeto global).
  - Nenhum risco identificado envolvendo `js/citylife.js`, `STORAGE_KEYS` existentes, autenticação/Supabase ou qualquer coisa fora da camada de apresentação — confirma o critério de aceite 9/10 desta RFC como já satisfeito pela decisão de arquitetura, sem trabalho adicional do Database Engineer nesta iniciativa (ele só entra se, no futuro, alguma das duas fases passar a precisar de dado novo persistido — não é o caso hoje).

- **Próximo agente responsável**: UX/UI Designer (Fase A — definir a estética/composição da cena decorativa sobre a fundação técnica acima) e, em paralelo, Gamification Designer (Fase B — confirmar se a câmera fixa recomendada para o MVP e o corte "Banco primeiro" preservam a sensação de progressão/jogo já validada na Cidade 2D antes de qualquer código da Fase B começar).

### 3. UX/UI Designer (Fase A — Composição visual da cena decorativa)

- **Resumo da etapa**: leitura completa de `js/trail.js` (603 linhas — grid zig-zag, estados de nó, `Fx.js` já usado em cada celebração) e das linhas relevantes de `css/style.css` (paleta em `:root`, `.trail-level`/`.trail-level-banner`/`.trail-node*`, `body`/`.app-header`/`.main-content`) para confirmar, antes de propor qualquer composição, **onde exatamente a cena decorativa vai aparecer de verdade**. Três confirmações de código guiam toda a especificação abaixo:
  - `body { background: var(--bg); }` (linha 56) e `html` sem `background` próprio — por regra do CSS, o fundo de `body` vira o "fundo do canvas" do documento inteiro, pintado por baixo de qualquer conteúdo posicionado, inclusive elementos com `z-index` negativo. `#trailScene3d` (fixed, filho direto de `body`, `z-index:-1`, decisão do Software Architect) fica portanto **acima** desse fundo lavanda base e **abaixo** de tudo o resto — exatamente como o Architect previu, confirmado agora também do lado de pintura de fundo, não só de empilhamento.
  - Dentro de `#tab-aprender`, a maior parte da área NÃO tem fundo opaco próprio: `.main-content` (linha 290), `.trail` (linha 821) e `.trail-level` (linha 856) não declaram `background`. Só `.trail-level-banner` (`var(--surface)`, linha 871) e `.trail-node-ring` (`var(--surface)`, linha 961) são opacos. Na prática: a cena decorativa **fica visível no espaço negativo entre os nós e entre os níveis** (a maior parte da altura da tela) e **fica automaticamente coberta** onde importa para leitura (dentro do card do nível, dentro do círculo do nó, atrás do texto do label) — ou seja, o critério de legibilidade do passo 4 já é estruturalmente favorável antes mesmo de eu escolher qualquer opacidade: texto e ícone nunca competem com a cena, porque nunca ficam sobre ela diretamente.
  - `.app-header` (linha 100, `var(--primary-dark)` opaco) cobre o topo — a cena não aparece atrás do header, só a partir do começo de `.main-content`.
  - Referência de técnica já validada em produção: `js/citypolvin3d.js` usa `MeshToonMaterial` com um `gradientMap` gerado em `CanvasTexture` (2-3 tons, `toonMat()`, linhas 76-83) para dar ao PolvIn 3D o visual "cel-shaded" que já é, hoje, a única linguagem 3D que o app tem. Reaproveito essa técnica (mesmo princípio, não o mesmo código) para a cena decorativa em vez de inventar um segundo estilo de shading — é a aplicação prática da régua da filosofia de design ("se eu tirar a logo e as cores, ainda reconheço que é do PolvIn?"): um cel-shading genérico não seria reconhecível, mas o MESMO cel-shading de 2-3 tons que já aparece no mascote da Cidade, agora também no fundo da trilha, é.

#### Conceito visual: "Correnteza do Conhecimento" — o fundo do mar do PolvIn, atrás da trilha

Referência central: **PolvIn é um polvo** — a trilha já é chamada de "caminho", mas nunca ganhou um cenário que lembrasse o habitat do próprio mascote. A cena decorativa mostra literalmente **o oceano onde o PolvIn vive**, visto de lado, atrás da trilha: um gradiente vertical de luz solar (topo, mais claro) até água profunda (embaixo, mais escura/roxa), com **"gemas do conhecimento"** (poliedros facetados nas cores da marca — roxo, dourado, verde) flutuando em correnteza lenta, **bolhas** subindo continuamente, e uma névoa de **plâncton bioluminescente** (partículas) no plano mais próximo da câmera. Não é decoração genérica de "app com fundo azul" — é o bioma do PolvIn, com a mesma técnica de shading do PolvIn, com a paleta do PolvIn.

**Wow Moment desta tela**: hoje, rolar a trilha é só rolar uma lista. Com a cena, rolar a trilha passa a ser **"mergulhar"**: a cada trecho percorrido, o gradiente de fundo desloca para tons mais escuros e as gemas ficam ligeiramente mais numerosas/próximas da câmera — o usuário sente fisicamente que está indo mais fundo no oceano à medida que avança mais fundo no conhecimento. É a mesma ideia por trás do exemplo canônico do prompt original ("o POLVIn mergulha trazendo uma moeda e diz algo dinâmico") aplicada ao scroll em vez de a um texto: a métrica de progresso (posição na trilha) veste uma metáfora visual (profundidade do oceano), não um número estático.

#### Composição por camada (orçamento: 19 objetos 3D, dentro do teto de 30-40 do Software Architect — sobra folga para o Frontend Engineer ajustar sem estourar)

| Camada | Elemento | Geometria Three.js | Contagem | Material/técnica |
|---|---|---|---|---|
| Fundo (mais distante) | Plano de fundo oceânico | `PlaneGeometry` cobrindo o viewport (billboard, sempre de frente pra câmera) | 1 | `MeshBasicMaterial` com `map` = `CanvasTexture` gerada em runtime (gradiente vertical tileável, ver paleta abaixo) — **não** toon-shaded (é só o pano de fundo, sem volume, não precisa de luz) |
| Meio | "Gemas do conhecimento" | `IcosahedronGeometry(raio 0.4–0.9, detail 0)` — poliedro facetado de baixo poly, silhueta bem diferente das esferas lisas do PolvIn (evita "parecer o mesmo asset reaproveitado") | 8 | `MeshToonMaterial` + `gradientMap` (mesma técnica de `citypolvin3d.js`, portada — 3 tons), cores cicladas entre `--primary` (#6c4fcf), `--gold` (#e8a33d), `--green` (#4fae4a) — 1 em cada 3 gemas de cada cor |
| Meio | "Anéis" (moedas/argolas, reforço temático de "moeda" sem repetir o coin-burst 2D) | `TorusGeometry` fino | 3 | `MeshToonMaterial` + mesmo `gradientMap`, cor `--gold`/`--gold-dark` |
| Próximo | Bolhas ascendentes | `SphereGeometry(raio 0.15–0.4, poucos segmentos — 8×6)` | 6 | `MeshBasicMaterial` `wireframe:true`, cor `--primary-light` (#8a70e0), opacidade 0.25 — contorno de bolha, não esfera sólida (mais leve, mais "água" que "bola") |
| Mais próximo | Plâncton bioluminescente | `THREE.Points` (1 único draw call) | ~350 pontos | `PointsMaterial`, cor `--gold` a 0.35 de opacidade, `size` pequeno (~0.03 em unidades de cena), `sizeAttenuation:true` |
| Ambiente | Névoa | `THREE.FogExp2` | — | cor `--primary-dark` (#2d1b4e), densidade baixa (~0.015) — só suaviza os elementos mais distantes, não deve esconder as gemas do primeiro plano |
| Luz | 2 luzes (reaproveita o mesmo setup mínimo de `citypolvin3d.js` para o `MeshToonMaterial` funcionar) | `AmbientLight` + `DirectionalLight` | 2 | intensidade baixa, só o suficiente para o degradê de 3 tons do toon material aparecer — não é uma cena "iluminada", é uma cena flat com leve volume |

Total: 1 + 8 + 3 + 6 + 1(Points) = 19 objetos de malha/partícula + 2 luzes + 1 fog. O Frontend Engineer tem margem para, por exemplo, dobrar as bolhas (6→12) se a cena parecer "vazia" em telas grandes, sem se aproximar do teto de 30-40.

**Por que não texturas/imagens**: nenhum elemento usa imagem externa — o degradê do plano de fundo e o `gradientMap` do toon material são os dois únicos "mapas", ambos `CanvasTexture` gerados em runtime (mesmo truque já usado por `citypolvin3d.js`), coerente com a restrição "sem asset pipeline/glTF novo" do Software Architect.

#### Paleta e opacidade exatas

- **Plano de fundo** (`CanvasTexture`, gradiente vertical, tileável — ver "Reação ao scroll" abaixo para como ele se move): 4 paradas de cor, todas variáveis já existentes em `:root` (nenhuma cor nova inventada para a cena):
  1. `#8a70e0` (`--primary-light`) — "superfície iluminada"
  2. `#6c4fcf` (`--primary`) — água intermediária
  3. `#3b6e8f` (`--blue`) — transição fria
  4. `#2d1b4e` (`--primary-dark`) — água profunda
  - Renderer com `alpha:true` (canvas transparente) e o **próprio plano** com opacidade de material **0.6** — o lavanda de `--bg` (#f6f4fb) continua "respirando" por trás em vez de a cena impor um azul/roxo sólido chapado sobre o app inteiro. Isso é o principal controle de sutileza: mesmo no ponto mais saturado do gradiente, o resultado percebido é um tingimento, não uma parede de cor.
- **Gemas**: opacidade de material **1.0** (são o elemento "sólido" da cena, o toon shading de 3 tons já dá a elas presença sem precisar de transparência) — mas raio pequeno (0.4-0.9 unidades) e distribuídas esparsas, nunca em bloco.
- **Anéis dourados**: opacidade **0.85**.
- **Bolhas** (wireframe): opacidade **0.25** — são o elemento mais "fantasma" da cena, só reforçam a leitura de "água em movimento".
- **Plâncton (Points)**: opacidade **0.35**.
- **Regra geral de sutileza (critério de aceite/acessibilidade)**: nenhum elemento da cena pode ultrapassar opacidade 1.0 nem usar branco/preto puro que reduza contraste do texto — como já demonstrado na leitura de código acima, os elementos de leitura (label do nó, título do nível, XP) sempre se sentam sobre `var(--surface)` opaco, então a cena nunca é vista "atrás do texto" — ainda assim, mantenho todo elemento não-opaco (bolhas, plâncton, plano de fundo) abaixo de 0.6 de opacidade de material como margem de segurança para telas onde o usuário tenha zoom/fonte grande e o card encolha, deixando mais espaço negativo visível.

#### Reação ao scroll (GSAP ScrollTrigger)

Um único `ScrollTrigger` "mestre", `trigger: document.body` (ou `html`), `start: "top top"`, `end: "bottom bottom"`, `scrub: true` (sem `pin`, a trilha 2D continua rolando normalmente por cima) — o Software Architect já confirmou que a trilha usa o scroll nativo da janela, sem container próprio, então isso é direto. Um único valor `progress` (0→1, "o quão fundo o usuário já rolou nesta sessão da aba") dirige três coisas diferentes, com "velocidades" (parallax) diferentes para dar sensação de profundidade real:

1. **Cor de fundo ("mergulho")** — em vez de eu exigir que a cena leia `--level-color` de qualquer `.trail-level` (o Software Architect foi explícito: `TrailScene3D` não pode importar/ler `Trail.*`/`Business.*`, e ler atributos de elementos específicos da trilha para fins de composição criaria uma dependência de DOM que quebra se o layout de `trail.js` mudar amanhã), a "profundidade" é calculada **só a partir do `progress` do scroll da página inteira**, sem tocar em nenhum elemento da trilha: o offset vertical (`texture.offset.y`) do `CanvasTexture` do plano de fundo se desloca conforme `progress`, ciclando pelas 4 paradas de cor acima (textura construída já repetida 2-3× verticalmente, `wrapS/wrapT = THREE.RepeatWrapping`, para o ciclo ser contínuo em trilhas de qualquer tamanho, das 2 lições às 35). Isso satisfaz o pedido "mudar de cor conforme progride pelos níveis" com uma leitura 100% agnóstica de conteúdo (só `window.scrollY`/altura do documento, nada de `Trail.levels()` nem `--level-color`) — mais seguro architeturalmente e mais barato do que orquestrar leitura de DOM a cada frame.
2. **Parallax entre camadas** — cada camada se desloca em Y a uma fração diferente do scroll real, criando profundidade:
   - Plano de fundo: não se move em posição (só a textura desliza, ver item 1) — fica "grudado" na câmera, como um céu/oceano no horizonte.
   - Gemas + anéis (camada meio): deslocamento vertical = `progress × factor 0.3` (movem devagar, ficam "para trás")
   - Bolhas + plâncton (camada próxima): deslocamento vertical = `progress × factor 0.12` (quase paradas em relação ao scroll — reforça que estão "mais perto")
3. **Leve balanço de câmera** — não amarrado ao scroll, é movimento ambiente contínuo e independente (`camera.position.x = Math.sin(t × 0.05) × 0.3`), para a cena nunca parecer uma pintura estática quando o usuário não está rolando — junto com a rotação própria lenta de cada gema (`rotation.y += dt × valor pequeno, diferente por gema para não sincronizarem`) e a subida contínua das bolhas (posição Y sobe e a bolha reaparece embaixo ao sair do topo do viewport — "loop", igual chuva/neve de fundo em jogos).

**Nota para o Frontend Engineer**: como `js/business.js` alterna Aprender↔Empreender **dentro do mesmo painel** por clique num chip (`#academiaSubnav`), não por `Tabs.go`/`tab:changed` (achado do Software Architect, seção 2), a altura do documento pode mudar nessa troca sem disparar `tab:changed`. `ScrollTrigger.refresh()` (já previsto pelo Architect em `activate()`) também precisa ser chamado no listener de clique do `#academiaSubnav`, senão o cálculo de `progress` fica desatualizado depois da primeira troca de chip.

#### Empreender vs. Aprender: composição idêntica, sem variação

Confirmo a decisão do Software Architect (arquiteturalmente já resolvida, seção "Escopo dentro da aba Aprender") **sem nenhuma variação de paleta/tema entre as duas sub-trilhas**. Motivo de design, não só de arquitetura: as duas trilhas já compartilham 100% do layout/CSS desde a RFC-035 Fase 2 ("mesma identidade visual, decisão explícita de não divergir") — introduzir uma cena de fundo diferente para Empreender quebraria essa consistência já estabelecida sem nenhum ganho perceptível (o usuário não troca de "mundo" ao trocar de chip, troca de assunto dentro do mesmo mundo). A metáfora "correnteza do conhecimento" já é genérica o suficiente para cobrir educação financeira E empreendedorismo sem forçar um segundo cenário (ex.: não preciso inventar um "recife dos negócios" à parte). Fica como variação válida **só se o Software Architect ou o usuário pedir explicitamente no futuro** — não é uma lacuna desta especificação, é uma decisão deliberada de não divergir.

#### `prefers-reduced-motion`: cena aparece, mas congela num quadro estático — não desliga

Confirmo a recomendação do Software Architect (seção "Fase A — decisão de arquitetura", item `prefers-reduced-motion`) e detalho o quadro exato: com o modo ativo, `TrailScene3D` renderiza **1 frame só**, sem `requestAnimationFrame` e sem `ScrollTrigger` de movimento, congelado no estado `progress = 0`:
- Plano de fundo com `texture.offset.y = 0` (parada de cor `--primary-light`→`--primary`, a mais clara/próxima da "superfície" — não a mais escura, para não parecer "cena carregando incompleta").
- Gemas e anéis na posição de repouso (`progress=0`, sem deslocamento de parallax), mas **cada uma mantém sua orientação/rotação individual "congelada" num ângulo não-alinhado** (não todas viradas igual) — para não parecer um erro de renderização, e sim uma cena real só "pausada".
- Bolhas nas posições iniciais espalhadas (não todas no mesmo Y, como se a sequência de "nascimento" delas já tivesse acontecido uma vez).
- Câmera sem o balanço senoidal (posição fixa em `x=0`).
- Nenhum listener de scroll é registrado nesse modo (evita trabalho de CPU à toa, não só a sensação de movimento).

Isso segue a régua do prompt original ("nunca remover a identidade, só a intensidade do movimento"): quem ativa reduced-motion ainda vê o oceano, as gemas, o PolvIn-bioma — só não vê nada se mexendo.

#### Resumo para o Frontend Engineer (implementação)

| Item | Decisão |
|---|---|
| Técnica de shading | `MeshToonMaterial` + `gradientMap` (`CanvasTexture`, 3 tons) — portar o padrão `toonMat()`/`buildGradientMap()` de `js/citypolvin3d.js`, não reinventar |
| Objetos 3D | 1 plano de fundo, 8 gemas (`Icosahedron`), 3 anéis (`Torus`), 6 bolhas (`Sphere` wireframe), 1 `Points` (~350) = 19 objetos + 2 luzes + 1 fog |
| Paleta | Só variáveis já existentes: `--primary-light` #8a70e0, `--primary` #6c4fcf, `--blue` #3b6e8f, `--primary-dark` #2d1b4e (fundo); `--primary`/`--gold`/`--green` (gemas); `--gold`/`--gold-dark` (anéis) |
| Opacidade | Fundo 0.6 · gemas 1.0 · anéis 0.85 · bolhas 0.25 (wireframe) · plâncton 0.35 · renderer `alpha:true` em todos os casos |
| Scroll → cor | `progress` global (0-1, scroll da página inteira) desloca `texture.offset.y` do plano de fundo por uma textura tileável de 4 paradas — **não lê `--level-color` nem `Trail.*`/`Business.*`**, evita qualquer acoplamento |
| Scroll → parallax | Camada meio (gemas/anéis) × 0.3 · camada próxima (bolhas/plâncton) × 0.12 · fundo não se move em posição |
| Movimento ambiente (sempre ativo, independente de scroll) | Balanço senoidal leve de câmera, rotação própria das gemas, bolhas subindo em loop |
| Empreender vs. Aprender | Idêntico — mesma composição, mesma paleta, sem branch visual. `ScrollTrigger.refresh()` também no clique do `#academiaSubnav`, não só em `tab:changed` |
| `prefers-reduced-motion` | 1 frame estático em `progress=0` (tom mais claro), sem rAF, sem listener de scroll — identidade mantida, movimento removido |
| Fx.js | **Nenhuma função nova aqui** — a cena é um módulo contínuo (`js/trailscene3d.js`, decisão do Architect), não um efeito disparado por evento como `Fx.confetti`/`Fx.coinBurst`; não crio ganchos entre a cena e eventos de lição/XP nesta fase, para não violar o "zero acoplamento" que o Architect definiu como a principal proteção do critério de aceite 2 |

- **Pendências**: nenhuma bloqueando a Fase A.1 (infraestrutura) do faseamento do Architect. Para a Fase A.2 (composição real sobre a fundação), esta especificação já é suficiente para implementação direta sem decisão visual adicional.
- **Riscos**: o principal risco de design é o mesmo que o Architect já sinalizou para acoplamento de código — aqui, o equivalente visual é a tentação de "ligar" a cena a eventos de gamificação (ex.: fazer uma gema brilhar ao completar uma lição) antes que isso seja formalmente decidido/priorizado; não é parte desta especificação e não deve ser implementado de passagem. Sinalizo como ideia futura legítima (ex.: uma RFC própria de "Fase A.3 — reações pontuais"), não uma pendência desta.
- **Próximo agente responsável**: Cyber Security Specialist (validar GSAP via CDN) e depois Frontend Engineer (implementação da Fase A).

### 4. Gamification Designer (Fase B — Câmera, controles e impacto no gameplay)

- **Resumo da etapa**: leitura completa de `js/citylife.js` (852 linhas) e `js/citygame.js` (742 linhas) para mapear o loop de jogo atual antes de decidir. Confirmações relevantes:
  - Movimento hoje é **clique/toque no chão → alvo de destino → lerp linear em `update()`** (`this.targetPos`, `MOVE_SPEED = 260px/s`, `Math.hypot`/`ratio` em `js/citygame.js` linhas 357-371). Não existe WASD nem D-pad — a documentação do briefing citava "WASD/toque" incorretamente; o código real é só apontar-e-andar, sem tecla nenhuma.
  - Câmera hoje é **`scene.cameras.main.startFollow(player, true, 0.09, 0.09)`** (linha 201) — segue com lerp suave, sem controle do jogador sobre ângulo/zoom, mais um `pan()`/`zoomTo()` script-driven só em 2 momentos: entrar numa construção (linha 579) e a celebração de aposentadoria (linha 692-693). Ou seja, o Phaser atual **já é câmera fixa/assistida**, nunca livre — não existe uma "sensação 3D navegável de câmera livre" a preservar hoje, porque ela nunca existiu; o que existe e precisa ser preservado é a sensação de "andar até onde eu toquei e a câmera me acompanha sem esforço".
  - `CityLife.avancarSemana()` **não consome nem verifica Energia** (`js/energy.js`) — confirmado por grep que `Energy.*` só é referenciado em `js/trail.js`, `js/business.js` e `js/app.js`. A Cidade nunca foi gated por sessão/energia; é uma tela de "brincar sem limite de tentativas", em contraste deliberado com a trilha. Isso não muda com o motor 3D.
  - `CityLife` nunca chama `Learn.addXp`/`Learn.addCoins` (confirmado por grep, 0 ocorrências) — o cabeçalho do próprio arquivo já declara essa fronteira ("Patrimônio/atributos são uma métrica PARALELA — nunca convertem em COINS/XP reais"). A única ponte real com XP fica em `Achievements.checkAll()`, chamada em vários pontos de `citylife.js` (compra de curso, compra de bem, abrir negócio, resolver decisão, avançar semana) — isso é lógica de negócio, **não é tocado pela Fase B** (Architect já travou isso).

#### 1. Câmera: fixa (3ª pessoa, seguindo o jogador) — confirmo a recomendação do Architect, sem ressalvas, para todo o MVP B.1-B.4

- **Comportamento reforçado**: sessões curtas e recorrentes de "abrir a Cidade, avançar 1-2 semanas, visitar 1 construção, sair" — o mesmo padrão de uso leve que a Cidade já tem hoje (não é uma tela de exploração longa como um mundo aberto; é uma tela de decisão financeira com cenário visual de apoio). Câmera livre serve a jogos cujo valor está em explorar o espaço; aqui o valor está em decidir o que fazer com a "sobra da semana" — a câmera é pano de fundo, não o produto.
- **Custo de não fazer (câmera livre) vs. risco de fazer**: o pedido original do usuário ("jogo 3D completo") é uma ambição legítima de percepção de qualidade, mas câmera livre introduz 3 riscos concretos de retenção que superam o ganho de "sensação de liberdade": (1) desorientação em mobile — perder de vista o próprio personagem ou uma construção é a forma mais rápida de um usuário casual simplesmente fechar a aba; (2) colisão de câmera com prédios/objetos, que em uma implementação nova sem engine de jogo (Three.js puro, conforme decisão do Architect) tende a gerar bugs visuais folclóricos nas primeiras semanas pós-launch — pior para retenção que uma câmera "simples mas sólida"; (3) mais uma superfície de controle (arrastar/pinçar/orbitar) competindo por atenção do polegar do usuário numa tela que já usa toque para clicar-para-andar e clicar em construções — ambiguidade de gesto é atrito, e atrito é abandono.
- **Decisão**: câmera 3ª-pessoa fixa em ângulo elevado, seguindo o jogador com lerp (equivalente ao `startFollow` de hoje, portado para Three.js com posição de câmera = posição do jogador + offset fixo, atualizada a cada frame com interpolação), **sem órbita/zoom livres do usuário**. É uma redução de escopo real frente à Descrição original da RFC, mas entrega o "uau" de forma mais segura: perspectiva 3D verdadeira (profundidade, luz, sombra, paralaxe entre prédios) sem o risco de uma câmera mal controlada quebrar a sensação de qualidade que o pedido queria elevar em primeiro lugar. Um jogo 3D com câmera travada e polida bate um jogo 3D com câmera livre e capenga, para o objetivo de retenção — a segunda opção arrisca transformar o "uau" em frustração já na primeira sessão.
- **Não fechado para sempre**: se o MVP (B.1-B.2) validar bem em produção (sem queixa de desorientação, QA aprova performance), câmera livre/zoom controlado pelo usuário pode voltar à mesa como incremento de polish pós-B.5 (cutover final), nunca como bloqueio do MVP. Registro para o `ROADMAP.md` a cargo do Documentation Specialist, não uma promessa desta RFC.

#### 2. Controles: mantém clique/toque-para-andar, mesmo padrão de hoje — sem WASD novo

- Confirmo a recomendação do Architect: raycasting do Three.js contra um plano de chão substitui `scene.cameras.main.getWorldPoint()` do Phaser, mas o **contrato de interação do jogador não muda uma linha**: toca/clica num ponto do chão → personagem anda até lá com movimento suavizado → prompt de construção aparece por proximidade de coordenadas (raio, não por câmera) → toca no prompt para abrir o diálogo. Essa curva de aprendizado já é conhecida por quem já jogou a Cidade (RFC-017-026) e não exige nenhuma tecla nova nem gesto novo a aprender.
- **Por que não introduzir WASD/joystick virtual mesmo a nova dimensão "convidando" a isso**: WASD favorece navegação de exploração livre — que é exatamente o padrão de câmera que acabei de rejeitar no item 1. Adicionar um esquema de controle novo sem uma câmera livre para acompanhá-lo de verdade seria pior nos dois eixos (mais uma coisa para aprender, sem ganho real de liberdade). Se no futuro a câmera livre for revisitada como polish, aí sim WASD/joystick volta à discussão como par indissociável dela — não antes.
- Nenhum ponto de fricção nova para o público mobile do PolvIn (a maioria da base, conforme `README.md`): toque continua sendo toque.

#### 3. Recompensa/celebração visual nova por causa do 3D: nenhuma agora — cedo demais, registrado como oportunidade futura, não como pendência do MVP

- A celebração de aposentadoria (RFC-025: brilho dourado + "punch" de câmera + confete/troféu no painel do Banco, `celebrateRetirement()`/`Fx.retirementCelebration()`) já é uma mecânica validada e correta na sua forma atual — **não deve ser redesenhada como parte da troca de motor**. Redesenhar uma celebração já funcional no mesmo commit que troca o motor gráfico misturaria dois riscos que deveriam ficar isolados (risco de regressão de motor + risco de regressão de UX de recompensa), contrariando o próprio espírito desta RFC de fases pequenas e reversíveis.
- Sinalizo, para depois que o MVP (B.2, Banco) estiver estável em produção, uma oportunidade real e barata de melhoria: hoje o PolvIn 3D é um overlay projetado por cima de uma cena 2D (`CityPolvin3D.setScreenPosition`); a partir da Fase B ele passa a ser um `Object3D` de verdade na mesma cena/câmera dos prédios (decisão do Architect, item "Destino de `js/citypolvin3d.js`"). Isso abre a porta para a celebração de aposentadoria ganhar profundidade real (partículas voando em coordenadas 3D ao redor do personagem, não num plano projetado; um dolly-zoom de câmera em vez do zoom 2D atual) — mas isso é trabalho de polish pós-MVP, a ser proposto como incremento próprio (ex. dentro da Fase B.5 ou uma RFC de acabamento), não uma dependência do B.1/B.2 funcionarem.
- Nenhuma nova recompensa (badge, XP bônus, moeda) associada à simples troca de motor — trocar de 2D para 3D é puramente estético; não é um marco de progresso do jogador e não deve gerar recompensa de gamificação. Se algum dia o PolvIn quiser celebrar "primeira vez que o jogador entra na Cidade 3D" como um evento de produto, isso é decisão de Product Owner/Marketing, não uma mecânica de jogo — e mesmo assim eu recomendaria não fazer, para não banalizar o sistema de conquistas com marcos técnicos que o jogador não "conquistou" de fato.

#### 4. Confirmação explícita: nenhuma regra de XP/moedas/energia/progressão muda por causa da troca de motor

- **Energia**: a Cidade não é e não deve passar a ser gated por `js/energy.js` só porque ganhou um motor mais "caro" visualmente. Se o Software Architect/QA identificar que rodar 2 contextos WebGL (Three.js da Cidade + Three.js/Phaser residual durante a transição) é pesado demais para aparelhos fracos, a resposta correta é otimização técnica (orçamento de meshes, pixel ratio, o que o Architect já definiu na seção de performance) — **nunca** introduzir um novo limitador de sessão para a Cidade. Isso duplicaria o papel que a Energia já cumpre para a trilha Aprender e criaria dois sistemas de "acesso limitado" divergentes no mesmo app, contrariando a lógica de retenção que a Energia foi desenhada para servir sozinha.
- **XP/Moedas**: `CityLife` continua sem tocar `Learn.addXp`/`Learn.addCoins` diretamente — confirmado por leitura de código (0 ocorrências). A ponte com XP real continua sendo exclusivamente via `ACHIEVEMENTS` (`Achievements.checkAll()`), que não muda com a troca de apresentação.
- **Progressão/streak**: `CityLife.avancarSemana()`, `resolverDecisao()`, aposentadoria e Nova Temporada continuam com as mesmas regras e o mesmo `STORAGE_KEYS.CITY_LIFE` — nenhuma chave nova de gamificação é necessária para a Fase B (concordo com o Architect: zero `STORAGE_KEYS` novo, zero `CustomEvent` novo).
- **Vetor de abuso avaliado e descartado**: a troca de câmera/raycasting não abre nenhum caminho novo para "ganhar patrimônio simulado infinito" ou pular a decisão semanal (`decisaoPendente` continua bloqueando `avancarSemana()` até ser resolvida, intocado pela Fase B) — a superfície de abuso relevante desta mecânica é 100% de `citylife.js`, que está fora de escopo desta troca de motor.

#### 5. Resumo das decisões (para Frontend/Backend Engineer)

| Item | Decisão |
|---|---|
| Câmera (B.1-B.4) | Fixa, 3ª pessoa, segue o jogador com lerp (equivalente a `startFollow`). Sem órbita/zoom livre do usuário. |
| Movimento | Clique/toque no chão → raycasting Three.js → personagem anda até o ponto com movimento suavizado (mesmo padrão de `targetPos`/lerp do Phaser atual). Nenhuma tecla nova. |
| Proximidade/prompt | Por distância de coordenadas de mundo (como hoje), independente da câmera. |
| Celebração de aposentadoria | Portar como está (RFC-025) no B.2 — nenhum redesenho nesta iniciativa. Melhoria "3D nativa" fica para depois do MVP, RFC própria. |
| XP/Moedas/Energia/Streak | Nenhuma mudança de regra. Cidade continua fora do sistema de Energia. |
| Novo `STORAGE_KEYS`/evento de gamificação | Nenhum. |
| Câmera livre/WASD | Não descartada para sempre — fica registrada como possível polish pós-B.5, condicionada ao MVP validar bem em produção; não é compromisso desta RFC. |

- **Pendências**: nenhuma pendência de gamificação bloqueando o início do B.1 — todas as perguntas endereçadas ao Gamification Designer pelo Architect foram respondidas nesta seção.
- **Riscos**: o único risco de engajamento que sinalizo para o QA Engineer acompanhar no B.1/B.2 é a sensação de "andar" em si (responsividade do lerp, se o raycasting do Three.js introduzir qualquer atraso perceptível vs. o `getWorldPoint()` do Phaser) — não é um risco de regra, é um risco de "feel" que só se confirma jogando a build real; se o QA ou early feedback perceber o movimento "pesado" comparado ao Phaser atual, isso deve voltar para este agente antes de prosseguir para B.3/B.4, não ser silenciosamente aceito como suficiente.
- **Próximo agente responsável**: Cyber Security Specialist (validar GSAP via CDN) e depois Frontend/Backend Engineer (implementação, começando pela Fase A e o MVP B.1 da Fase B).

### 5. Cyber Security Specialist (validação do GSAP via CDN)

- **Resumo da etapa**: leitura de `index.html` (linhas 794-849) para confirmar o padrão real de carregamento de dependências de CDN hoje em produção; pesquisa externa sobre licenciamento, CDN oficial e versão estável do GSAP/ScrollTrigger; verificação por leitura de código (não por confiança na RFC) de duas afirmações estruturais feitas pelo Software Architect na seção 2 que sustentam a redução de risco da Fase A.

#### Padrão de carregamento confirmado (linha de base para o GSAP)

`index.html` linhas 794-839 confirmam exatamente o que a RFC descreve: Supabase (`@supabase/supabase-js@2.112.2`), Phaser (`phaser@3.80.1`) e Three.js (`three@0.150.0`) são todos carregados via `<script>` do CDN `cdn.jsdelivr.net/npm/...`, com **versão numérica travada** (nenhum `@2`/`@latest` flutuante), atributos `integrity="sha384-..."` e `crossorigin="anonymous"`, cada um com um comentário HTML citando a RFC-027 (achado 4) como origem da política. Esse é o padrão de fato em vigor — não uma aspiração descrita apenas em documentação — e é o padrão que o GSAP deve seguir sem exceção, exatamente como a RFC já assume.

#### GSAP + ScrollTrigger: licenciamento e CDN oficial

Confirmado por pesquisa: em abril de 2025 a Webflow (que adquiriu a GreenSock em outubro de 2024) tornou todo o GSAP gratuito, incluindo os plugins antes exclusivos do Club GreenSock (ScrollTrigger, SplitText, MorphSVG, DrawSVG etc.), para qualquer uso, inclusive comercial. Isso remove a única barreira não técnica que existiria para adotar ScrollTrigger (antes era um plugin pago) — não há custo de licença nem termos de "watermark"/trial a verificar no bundle publicado hoje.

CDN recomendado: jsDelivr, `https://cdn.jsdelivr.net/npm/gsap@VERSAO/dist/gsap.min.js` e `https://cdn.jsdelivr.net/npm/gsap@VERSAO/dist/ScrollTrigger.min.js` — mesmo provedor já usado para Supabase/Phaser/Three.js neste projeto (consistência operacional: 1 CDN só para auditar/monitorar, em vez de múltiplos provedores com políticas de cache/disponibilidade diferentes). cdnjs também publica o pacote (`cdnjs.cloudflare.com/ajax/libs/gsap/...`) e é uma alternativa válida, mas recomendo manter jsDelivr por já ser o padrão estabelecido no projeto.

Versão estável mais recente identificada na pesquisa: **gsap 3.15.0** (npm/jsDelivr, checado em 2026-08-09). Condição explícita: como o GSAP lança novas versões com frequência, o Frontend Engineer deve reconferir no npmjs.com/package/gsap ou na página do pacote em jsdelivr.com/package/npm/gsap a versão estável mais recente no momento exato da implementação (não usar cegamente "3.15.0" se meses tiverem se passado) — mas, seja qual for a versão escolhida, ela deve ser um número fixo (nunca `@latest`/tag flutuante), com hash SRI gerado para exatamente esse arquivo.

#### Hashes SRI — processo obrigatório, não geração automática de terceiros

Esta função não tem acesso a `curl`/`openssl` para baixar e verificar o binário exato do bundle GSAP, então nenhum hash SRI é fornecido pronto abaixo — isso é deliberado, não uma omissão. O Frontend Engineer deve gerar o hash com o mesmo processo já documentado no comentário da RFC-027 (linhas 799-800 de `index.html`), para os dois arquivos (`gsap.min.js` e `ScrollTrigger.min.js`):

    curl -sL https://cdn.jsdelivr.net/npm/gsap@VERSAO/dist/gsap.min.js -o gsap.min.js
    openssl dgst -sha384 -binary gsap.min.js | openssl base64 -A

    curl -sL https://cdn.jsdelivr.net/npm/gsap@VERSAO/dist/ScrollTrigger.min.js -o ScrollTrigger.min.js
    openssl dgst -sha384 -binary ScrollTrigger.min.js | openssl base64 -A

Regras não-negociáveis (mesmas já aplicadas a Supabase/Phaser/Three.js, reforçadas aqui):
1. Nunca colar um hash gerado por um "SRI hash generator" de terceiro sem antes baixar o arquivo exato servido pela URL final e rodar o comando acima manualmente — um gerador de terceiro comprometido, ou que aponte para um arquivo diferente do que o navegador de fato vai carregar, anula toda a proteção do SRI.
2. O hash deve ser gerado a partir do arquivo baixado da mesma URL exata que vai para o `src` do `<script>` (mesma versão, mesmo caminho `/dist/`) — qualquer diferença de build (ex.: baixar de `unpkg` mas apontar o `<script>` para `jsdelivr`) invalida a garantia, mesmo que o conteúdo pareça idêntico.
3. Repetir a verificação sempre que a versão for atualizada no futuro (mesma disciplina já em vigor no projeto).

#### Riscos de segurança específicos de adicionar GSAP

- **Superfície de dependência de terceiro**: adicionar GSAP eleva de 3 para 4 (e, com o plugin, 5) o número de bundles JS de terceiro carregados via CDN. Mitigado pelo mesmo controle já usado para as outras 4: versão travada + SRI + `crossorigin="anonymous"` — com SRI corretamente verificado, um comprometimento do CDN (supply-chain attack, ex. o incidente real que motivou a política SRI original do projeto na RFC-027) resulta em falha de carregamento do script (erro no console, cena decorativa simplesmente não aparece), não em execução de código malicioso — o navegador recusa executar um arquivo cujo hash não bate.
- **CVEs conhecidos**: encontrada 1 vulnerabilidade histórica, **CVE-2020-28478** — prototype pollution / DoS em `gsap` antes da versão 3.6.0 (2020). Não é aplicável à versão recomendada aqui (3.15.0, ou qualquer 3.x atual — a distância de 9+ versões menores é grande) — nenhuma vulnerabilidade conhecida em aberto foi encontrada para as versões 3.x recentes do GSAP/ScrollTrigger na pesquisa realizada. Recomendo, ainda assim, que o Frontend Engineer confirme a ausência de qualquer advisory novo em github.com/greensock/GSAP/security/advisories no momento exato da implementação, como checagem de rotina antes de travar a versão — não porque haja indício de problema, mas porque é o mesmo cuidado de baixo custo já aplicável a qualquer dependência nova.
- **Exposição do uso pretendido**: o uso descrito (animação de propriedades CSS/transform e scroll-linked animation via ScrollTrigger, numa cena puramente decorativa) não lê, envia ou manipula dados sensíveis do usuário, não faz requisições de rede próprias (GSAP não tem I/O de rede embutido — não é como o cliente Supabase, que fala com uma API e guarda um JWT), e não escreve em `localStorage`/cookies. A superfície de ataque prática de "o que um atacante ganharia comprometendo o GSAP" neste projeto se resume a: poder rodar JS arbitrário no contexto da página (mesmo risco genérico de qualquer script de terceiro sem SRI) — que é exatamente o risco que o SRI já neutraliza. Não há um vetor adicional específico de ScrollTrigger além do genérico de "script de terceiro".
- **`gsap.registerPlugin(ScrollTrigger)` e ordem de carregamento**: risco puramente funcional, não de segurança — se `ScrollTrigger.min.js` carregar antes de `gsap.min.js`, ou se `js/trailscene3d.js` rodar antes dos dois, a chamada falha silenciosamente/lança erro. Já coberto pela ordem definida pelo Software Architect (seção 2); sinalizo aqui só para reforçar que a ordem dos `<script>` no `index.html` deve ser respeitada exatamente como especificado.

#### Verificação da arquitetura da Fase A (por leitura de código, não por aceitar a RFC)

Duas afirmações estruturais da RFC foram checadas diretamente:

1. **"Elementos dentro de um painel `display:none` têm geometria zero"** (Software Architect, base para colocar `#trailScene3d` fora de `.tab-panel`): confirmado em `js/trail.js` linha 291-297 (`observeReveal()`), que já contém a guarda `if (!container || container.getClientRects().length === 0) return;` — prova, no próprio código de produção, que este é um problema real e já tratado em outro lugar do projeto, não uma suposição nova.
2. **"Não existe hoje nenhum elemento com `z-index` negativo"** (base para a escolha de `z-index:-1` no novo `#trailScene3d` não colidir com nada existente): confirmado por busca em `css/style.css` — todos os `z-index` declarados no arquivo são positivos (valores encontrados: 1, 2, 4, 5, 20, 90, 95, 100, 150, 200, 300, 9999); nenhum negativo. A escolha de `z-index:-1` para a nova camada é, portanto, segura no sentido de "não compete com nenhuma regra de empilhamento já existente".

Essas duas confirmações validam, na prática, a alegação central de segurança da Fase A: como `js/trailscene3d.js` **ainda não existe** (verificado — arquivo não encontrado no repositório neste momento), a garantia de "zero acoplamento com `Trail.*`/`Business.*`/`Learn.*`" descrita pelo Software Architect é hoje uma decisão de arquitetura, não um fato de código ainda auditável linha a linha. Recomendo ao QA Engineer e, na revisão de código do Frontend Engineer, checar explicitamente na implementação real que `js/trailscene3d.js`:
- Não importa/lê `Trail.*`, `Business.*`, `Learn.*` nem qualquer `STORAGE_KEYS.*`.
- Não escuta `course:updated`, `lesson:passed`, `xp:updated` ou qualquer evento de progresso.
- Só escuta `tab:changed`, `resize`, o clique de `#academiaSubnav` (para `ScrollTrigger.refresh()`) e o scroll nativo da janela (via ScrollTrigger).

Se essas três condições se confirmarem no código final (não apenas na RFC), a superfície de risco real da Fase A fica, de fato, limitada a "mais um script de CDN a auditar" — não há caminho de dados do usuário passando por ele. Concordo com a avaliação do Software Architect/Product Owner nesse ponto, com a ressalva de que é uma verificação a repetir no code review da implementação, não algo que esta validação de RFC (código ainda inexistente) possa encerrar definitivamente.

#### Veredito

**Aprovado com condições.** Não há objeção de segurança à adoção do GSAP (core) + ScrollTrigger via CDN para a Fase A. Condições para o merge:

1. Versão travada (número fixo, nunca tag flutuante) — reconferir a mais recente estável no momento da implementação, não assumir "3.15.0" indefinidamente.
2. Hash SRI gerado pelo processo manual acima (baixar o arquivo exato + `openssl dgst -sha384 -binary | openssl base64 -A`) para os dois arquivos, nunca copiado de gerador de terceiro.
3. Ordem dos `<script>` respeitada: `gsap.min.js` → `ScrollTrigger.min.js` → (demais scripts existentes) → `js/trailscene3d.js`.
4. Na implementação real de `js/trailscene3d.js`, o code review (Frontend Engineer + QA Engineer) deve confirmar as três condições de zero-acoplamento listadas acima antes de considerar a Fase A.1 concluída — esta validação de segurança foi feita sobre a arquitetura proposta, não sobre código já existente, porque o arquivo ainda não foi criado.

Tag `<script>` pronta para uso, com placeholder de hash a ser preenchido após a verificação manual (item 2):

```html
<!-- GSAP (core) + ScrollTrigger (RFC-036, Fase A — camada decorativa 3D na trilha Aprender).
     Gratuito e open-source para todos os usos, incluindo plugins antes pagos como ScrollTrigger
     (Webflow/GreenSock, abril de 2025). CDN jsdelivr, versão travada + Subresource Integrity,
     mesmo padrão de Supabase/Phaser/Three.js (RFC-027, achado 4). Ao atualizar a versão, regenere
     o hash (baixar o arquivo exato e rodar openssl dgst -sha384 -binary arquivo.js | openssl
     base64 -A — NUNCA copiar hash de um gerador de SRI de terceiro sem essa verificação manual).
     ScrollTrigger.min.js precisa vir DEPOIS de gsap.min.js (estende o core) e ANTES de
     js/trailscene3d.js (que chama gsap.registerPlugin(ScrollTrigger)). -->
<script src="https://cdn.jsdelivr.net/npm/gsap@3.15.0/dist/gsap.min.js" integrity="sha384-SUBSTITUIR_PELO_HASH_VERIFICADO_MANUALMENTE" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/gsap@3.15.0/dist/ScrollTrigger.min.js" integrity="sha384-SUBSTITUIR_PELO_HASH_VERIFICADO_MANUALMENTE" crossorigin="anonymous"></script>
```

(Substituir `3.15.0` pela versão estável reconfirmada no momento da implementação, se diferente.)

- **Pendências**: nenhuma bloqueando o início da Fase A.1 além das 4 condições acima, todas de execução (não de desenho). Nenhuma pendência sobre a Fase B nesta etapa — a introdução de dependência de CDN da Fase B é zero (Three.js já pago, confirmado pelo Software Architect), então não há nova validação de segurança de CDN a fazer para a Fase B nesta RFC; ela será revisitada se `js/citygame3d.js` introduzir alguma dependência nova no futuro.
- **Riscos**: nenhum risco de segurança novo identificado que já não estivesse coberto pelo padrão SRI existente. O único risco residual é operacional/de disciplina (item 4 das condições) — não é um risco desta biblioteca especificamente, é o mesmo risco de acoplamento silencioso já sinalizado pelo Software Architect na seção 2, e reforço aqui a mesma preocupação sob a lente de segurança: qualquer leitura futura de dado de progresso do usuário dentro de `js/trailscene3d.js` deveria, no mínimo, disparar uma nova revisão deste agente antes de ir para produção.
- **Próximo agente responsável**: Frontend Engineer (implementação da Fase A)

### 6. Frontend Engineer (Fase A — Implementação da cena decorativa 3D)

- **Resumo da etapa**: implementação completa da Fase A.1 + A.2 (fundação técnica e composição visual final já num só incremento, já que a especificação do UX/UI Designer estava suficientemente detalhada para implementação direta). Confirmado `git status`/`git diff --stat` no início: nenhum arquivo da RFC-035 em andamento foi tocado; único arquivo pendente era esta própria RFC (untracked).

#### O que foi construído

1. **`index.html`** — GSAP 3.15.0 (core) + ScrollTrigger 3.15.0 adicionados via CDN jsDelivr, na "zona de motores" (depois de Three.js, antes de `js/citypolvin3d.js`), com versão travada e **hash SRI real** (não placeholder): baixei os dois arquivos exatos das URLs de produção (`curl -sL https://cdn.jsdelivr.net/npm/gsap@3.15.0/dist/{gsap,ScrollTrigger}.min.js`) e rodei `openssl dgst -sha384 -binary arquivo.js | openssl base64 -A` manualmente sobre eles — mesmo processo da RFC-027, nenhum gerador de terceiro usado. Confirmei a versão 3.15.0 via `https://data.jsdelivr.com/v1/packages/npm/gsap/resolved` (retornou `3.15.0`, batendo com o que o Cyber Security Specialist já tinha identificado em 2026-08-09) e inspecionei o cabeçalho dos arquivos baixados (`GSAP 3.15.0`/`ScrollTrigger 3.15.0`, licença GreenSock 2026) para confirmar que não baixei um arquivo trocado. Hashes gerados:
   - `gsap.min.js`: `sha384-XmJ9SoHtVOHoQUcKvFAzVXwdkKo1Ie3bhmSoIAkcdsHGaIrVJIkmozyq0FJeb/Ly`
   - `ScrollTrigger.min.js`: `sha384-wl5TeDVvOWt30Pbf8aSo2ZrzsOjddu3avOBvHe+p+OhJt9gP6w9YXmDkN5DK2/dF`
   - `js/trailscene3d.js` registrado depois de `js/citygame.js`, mantendo a ordem prescrita (`gsap` → `ScrollTrigger` → ... → `trailscene3d.js` → ... → `app.js`).
2. **`css/style.css`** — regra `#trailScene3d` (`position:fixed; inset:0; z-index:-1; pointer-events:none`) inserida logo antes de `.trail`, com comentário explicando a decisão de z-index negativo (reaproveitando a mesma verificação já feita pelo Security Specialist de que nenhum z-index negativo existia antes).
3. **`js/trailscene3d.js`** (novo módulo, ~526 linhas) — objeto global `TrailScene3D` com `init()`, `activate()`, `deactivate()`, `setActive(bool)` (alias). Implementa exatamente a composição da seção 3 do UX/UI Designer: 1 plano de fundo oceânico (`CanvasTexture` com gradiente de 4 paradas, `RepeatWrapping` vertical ×3), 8 gemas (`IcosahedronGeometry`, cicladas entre `--primary`/`--gold`/`--green`, `MeshToonMaterial`+`gradientMap` portado literalmente da técnica de `js/citypolvin3d.js`), 3 anéis dourados (`TorusGeometry`), 6 bolhas wireframe ascendentes em loop, 1 `THREE.Points` de ~350 partículas — 19 objetos + `AmbientLight`+`DirectionalLight`+`FogExp2`, confirmado por leitura em runtime (`scene.children.length === 5` no nível raiz: bgMesh, midGroup, nearGroup, 2 luzes; gems/rings dentro de midGroup, bubbles/plankton dentro de nearGroup).
   - Scroll: 1 `ScrollTrigger` mestre (`trigger: document.body`, `start:"top top"`, `end:"bottom bottom"`, `scrub:true`, sem `pin`) alimenta `TrailScene3D.progress` (0-1); offset da textura de fundo e parallax de `midGroup`/`nearGroup` (fatores 0.3/0.12, exatamente a razão 2.5 especificada) derivam só desse `progress` — nunca leem `Trail.*`/`Business.*`/`--level-color`.
   - Ciclo de vida "pausar, não destruir": `activate()`/`deactivate()` ligados a `tab:changed` (`e.detail.tabId === "aprender"`) e a `visibilitychange`, combinados via `syncActiveState()`. `deactivate()` cancela o `requestAnimationFrame` e desabilita (`.disable()`, não `.kill()`) o `ScrollTrigger`, mas mantém o `WebGLRenderingContext` vivo — nunca chama `renderer.dispose()`. Decisão adicional não detalhada explicitamente pela RFC, mas necessária na prática: como o canvas é filho direto de `<body>` (fora de `.tab-panel`), ele ficaria visível "vazando" por trás de QUALQUER aba enquanto pausado (não só Aprender) — resolvido escondendo o canvas via `style.visibility = "hidden"/"visible"` em `deactivate()`/`activate()`, sem tocar no contexto WebGL nem no z-index.
   - `#academiaSubnav`: listener de clique próprio (registrado 1x, no load do módulo) que chama `ScrollTrigger.refresh()` num `requestAnimationFrame`, conforme a nota do UX/UI Designer sobre a troca Trilha↔Empreender não disparar `tab:changed`.
   - `prefers-reduced-motion`: reaproveita `Fx.prefersReducedMotion()` (mesma media query, sem duplicar lógica); quando ativo, `setupScrollTrigger()` nunca cria o `ScrollTrigger` e `activate()` desvia para `renderStaticFrame()` — 1 frame em `progress=0`, sem `requestAnimationFrame`, sem listener de scroll.

#### Bug encontrado e corrigido durante o teste manual (fora do escopo literal da especificação, mas necessário)

Ao testar em viewport móvel estreito (390×844, `Emulation.setDeviceMetricsOverride`), as gemas/anéis apareciam **desproporcionalmente enormes**, cobrindo boa parte do cabeçalho "Academia PolvIn" — regressão real de legibilidade que meus próprios testes pegaram antes do QA. Causa raiz: os objetos usavam raio absoluto fixo em unidades de mundo (`0.4-0.9`), mas a área visível calculada a partir do FOV/aspect da câmera perspectiva (`visibleWidthAtZ`) encolhe proporcionalmente ao aspect ratio — numa tela retrato estreita, a largura visível em unidades de mundo cai muito, então um raio fixo passa a ocupar uma fração enorme da tela. Corrigido dimensionando todo objeto (gemas, anéis, bolhas, tamanho do ponto do plâncton) como fração da **menor dimensão visível** (`visibleMinDimAtZ = Math.min(visibleWidthAtZ, visibleHeightAtZ)`) no seu próprio Z, em vez de um raio absoluto — mantém a mesma sensação de escala em qualquer proporção de tela (retrato estreito ou ultrawide). Recontagem de objetos após o fix confirmou que nenhuma contagem mudou (`[8, 3, 6, 350, 5]`), só o tamanho relativo.

#### Testes executados (Chrome headless real via CDP, não simulado)

Servidor local via `python -m http.server 8791`; Chrome 151 headless lançado com `--remote-debugging-port` + `--remote-allow-origins=*`; driver próprio em Python (`websocket-client`) para `Runtime.evaluate`, `Page.captureScreenshot`, `Emulation.setEmulatedMedia`/`setDeviceMetricsOverride`/`setCPUThrottlingRate`, captura de `Runtime.consoleAPICalled`/`exceptionThrown`. Resultados:

1. **Cena atrás da trilha, sem quebrar legibilidade**: confirmado por leitura de estilo computado (`position: fixed`, `z-index: -1`, canvas **fora** de qualquer `.tab-panel` — `closest('.tab-panel')` retorna `null`) e por screenshot real (a onboarding gate real do app — `#authGateScreen`, RFC-027 — foi escondida só via `style.display` para a foto, sem tocar `Auth.*`/`Store`; markup estático com as classes reais `.trail-level`/`.trail-node`/`.trail-node-ring` foi injetado só para a foto, sem chamar `Trail.*`). O oceano, as gemas e os anéis aparecem claramente atrás do cabeçalho/cards. Não criei conta real no Supabase de produção (`js/supabase-config.js` aponta para um projeto real) só para este teste visual — decisão deliberada para não escrever dados de teste no banco de produção.
2. **Scroll anima a cena**: verificado numericamente, não só visualmente — `progress` foi de `0` (topo) a `1` (fundo) rolando a janela real; `bgTexture.offset.y` foi de `0` a `-2`; `midGroup.position.y`/`nearGroup.position.y` foram a `-3`/`-1.2` (razão `2.5`, batendo exatamente com os fatores `0.3`/`0.12` da especificação).
3. **Troca de aba não duplica contexto WebGL nem vaza memória**: Aprender→Cidade→Aprender testado; `document.querySelectorAll('#trailScene3d').length === 1` sempre; `TrailScene3D.active`/`rafId` corretamente `false`/`null` na Cidade e `true`/preenchido de volta em Aprender; canvas escondido (`visibility:hidden`) na Cidade, visível de volta em Aprender; nenhuma mensagem de "too many WebGL contexts" ou erro de contexto no console em nenhuma rodada.
4. **`prefers-reduced-motion`**: emulado via `Emulation.setEmulatedMedia` (precisa de reload da página, já que o módulo só checa 1x em `init()` — comportamento correto, documentado). Confirmado: `TrailScene3D.reducedMotion === true`, `active === false`, `rafId === null`, `scrollTriggerInstance === null` (nunca criado), canvas ainda visível (cena aparece, só não anima) — bateu exatamente a especificação do UX/UI Designer. Screenshot salvo confirmando o frame estático.
   - Achado lateral: a própria máquina/perfil de teste tinha `prefers-reduced-motion: reduce` como padrão do sistema — o que na prática validou o caminho reduced-motion "de graça" antes mesmo de eu forçar a emulação, e me obrigou a forçar explicitamente `no-preference` para também exercitar e confirmar o caminho animado normal (item 2 acima).
5. **Nenhuma regressão em XP/energia/quiz/revisões (RFC-035)**: não foi possível clicar num nó real da trilha e completar uma lição de ponta a ponta neste ambiente de teste porque o app exige conta (`#authGateScreen`, RFC-027) antes de renderizar `#trailContainer` com conteúdo real — isso é comportamento correto e preexistente do app, não algo que a Fase A tocou. Verificação alternativa feita: `Trail.flatLessons().length` retornou `132` (dado carregado normalmente, `js/data.js`/`js/trail.js` intactos), zero exceções JS em qualquer interação (troca de aba, clique em `#academiaSubnav`, scroll), e grep confirma que `js/trailscene3d.js` não contém nenhuma referência funcional a `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS`/`course:updated`/`lesson:passed`/`xp:updated` (só nos comentários que documentam a própria restrição). Registro explícito para o QA Engineer: **validar com uma conta real** que completar uma lição continua funcionando — o Frontend Engineer confirmou por leitura de código e por design que não há caminho de acoplamento, mas o teste de ponta a ponta com progresso real fica pendente de um ambiente com conta.
6. **Performance**: 8 gemas + 3 anéis + 6 bolhas + 1 `Points` (350) + 1 plano = 19 objetos, dentro do orçamento de 30-40 do Software Architect. FPS medido via `requestAnimationFrame` timestamping por 2-3s: **~60fps em condição normal** e **~60fps ainda com `Emulation.setCPUThrottlingRate: 4x`** (simulação de CPU 4× mais lenta, aproximando um Android de entrada) — bem acima do orçamento mínimo de 30fps definido pelo Software Architect. Nenhum warning de performance no console.
7. **Zero erros de console**: confirmado em todas as rodadas (`Runtime.exceptionThrown` e `console.error` capturados via CDP) — único warning presente é o já preexistente de depreciação do build UMD do Three.js (`three@0.150.0`, não introduzido por esta mudança).

#### Observação para o QA Engineer (não bloqueante, registrada por transparência)

Em viewport móvel estreito, 1-2 gemas pequenas ainda passam visualmente perto do título "Academia PolvIn" e do parágrafo de introdução (que não têm fundo opaco próprio, mesma característica de `.main-content`/`.trail` já identificada pelo UX/UI Designer para o resto da tela) — não é uma regressão de contraste (texto `--ink` sobre fundo claro continua legível nos screenshots capturados) nem diverge da composição especificada, mas é um ponto fino de acabamento que vale um olhar de QA/UX em dispositivos reais antes de considerar a Fase A.2 100% polida.

#### Verificação dos critérios de aceite desta RFC relevantes à Fase A

1. `js/citylife.js` — não tocado (Fase A não mexe na Cidade). ✅
2. Progresso/quiz/energia/revisões da trilha — nenhuma função (`isUnlocked`/`isDone`/`progressKey`/`Trail.levels`/`flatLessons`/`COURSE_REVIEWS`/`COURSE_PROGRESS`/`HISTORY_PROGRESS`) foi tocada; `js/trail.js` e `js/business.js` não foram editados nesta implementação. ✅
3. GSAP com versão travada + SRI real, mesmo padrão de Supabase/Phaser/Three.js. ✅
4. Performance: 19 objetos (dentro do teto 30-40), ~60fps mesmo com CPU 4× throttled. ✅
5. `prefers-reduced-motion` respeitado (frame estático, sem rAF, sem listener de scroll). ✅
6. Empreender incluído na mesma composição (arquitetura já cobre isso automaticamente — confirmado, `#trailScene3d` fica atrás de `#tab-aprender` inteiro, sem branch por chip). ✅
7-10. Fase B (não aplicável a este incremento). —

#### Arquivos alterados/criados

- **Criado**: `js/trailscene3d.js`
- **Editado**: `index.html` (tags `<script>` GSAP/ScrollTrigger + `js/trailscene3d.js`), `css/style.css` (`#trailScene3d`)
- **Não tocados** (confirmado antes e depois): `js/trail.js`, `js/business.js`, `js/learn.js`, `js/energy.js`, `js/citylife.js`, qualquer arquivo da RFC-035 em andamento

- **Pendências**: nenhuma bloqueando a conclusão da Fase A do ponto de vista de implementação. Para o QA Engineer: (a) validar o fluxo de lição completo com uma conta real (item 5 acima); (b) revisar em dispositivo móvel real (não só emulado) a observação de acabamento sobre gemas próximas ao título; (c) checar se o achado lateral de `prefers-reduced-motion` já vir `true` por padrão no ambiente de CI/teste é esperado (indica que o SO/perfil de teste tem a preferência de acessibilidade ligada — não é um bug do app, mas vale registrar).
- **Riscos**: nenhum risco novo de acoplamento identificado (grep confirma). Risco residual de acabamento visual em mobile real, sinalizado acima, não bloqueante.
- **Próximo agente responsável**: QA Engineer

### 7. QA Engineer (Fase A — Validação da implementação)

- **Resumo da etapa**: testes executados via Chrome 151 headless real (CDP), servidor HTTP local — mesma técnica do Frontend Engineer, mas de forma independente (sem reaproveitar os mesmos scripts). Cobertura: grep de acoplamento, recálculo independente dos hashes SRI, troca de aba repetida (6+ ciclos), scroll real de ponta a ponta, `prefers-reduced-motion` nos dois sentidos, viewport mobile 390×844, FPS instrumentado.
- **Confirmações independentes**: zero referência funcional a `Trail.*`/`Business.*`/`STORAGE_KEYS`/eventos de progresso em `js/trailscene3d.js` (só em comentários); hashes SRI de `gsap.min.js`/`ScrollTrigger.min.js` recalculados via `openssl dgst -sha384` batem exatamente com os declarados no `index.html`; `js/supabase-config.js` só contém chave pública (`sb_publishable_...`), sem segredo exposto; nenhuma duplicação de `#trailScene3d` nem vazamento de contexto WebGL em 6+ trocas de aba (mesma instância de `renderer` reaproveitada); scroll real confirma `progress` 0→1, `bgTexture.offset.y` 0→-2, parallax na razão 2.5× (0.3/0.12) especificada; `prefers-reduced-motion` respeitado nos dois sentidos (forçado ligado e desligado); ~59.7fps normal e ~60fps com CPU 4× throttled — **nota**: ambiente de teste usa renderização por software (`ANGLE ... D3D11`, sem GPU real), teste mais pessimista que celular real, mas não substitui medição em hardware móvel real conforme pedido pelo Software Architect (não verificável neste ambiente).
- **Achado 1 (baixa-média, não bloqueante)**: gemas/anéis nascem em posição aleatória (`Math.random()`, `buildGems()`/`buildRings()`) numa área centrada na câmera sem zona de exclusão para a faixa onde `.section-title`/`#academiaSubnav` (cabeçalho "Academia PolvIn" + chips Trilha Principal/Empreender) são renderizados — que não têm fundo opaco (UX/UI Designer, seção 3). Em viewport mobile estreito (390×844), isso pode colocar um objeto **diretamente sobre** o texto/chips em algumas cargas da página (probabilístico, não em toda carga) — mais forte do que a observação original do Frontend Engineer ("gemas próximas ao título"). Não é regressão de contraste de texto, é colisão visual com área interativa (chips clicáveis). QA recomenda adicionar uma zona de exclusão de spawn nos ~15-20% superiores da tela.
- **Achado 2 (informativo)**: confirmado que o ambiente de teste (headless Chrome neste container Windows) tem `prefers-reduced-motion: reduce` como padrão do sistema — precisa forçar `no-preference` explicitamente para exercitar o caminho animado em QA/CI futuro. Não é bug do app.
- **Pendência (a) ainda não verificada**: fluxo de lição completo com conta real permanece não testado por dois QAs consecutivos (Frontend Engineer e QA Engineer), por falta de ambiente de staging Supabase — ambos optaram por não escrever dados de teste no banco de produção. Mitigado por evidência indireta forte (zero acoplamento de código, nenhum arquivo de lógica de trilha/quiz/energia tocado), mas o teste de ponta a ponta real continua pendente. Escalar ao Product Owner: avaliar projeto Supabase de staging dedicado a QA.
- **Recomendação**: **Aprovar com ressalvas.** Nenhum achado crítico ou de alta gravidade. Achado 1 deve ser corrigido antes de considerar a Fase A.2 "100% polida" (já esperado pelo próprio Frontend Engineer), mas não bloqueia o uso em produção.
- **Próximo agente responsável**: Frontend Engineer (corrigir Achado 1 — zona de exclusão de spawn no topo da tela) e, em paralelo, Product Owner (avaliar ambiente de staging para a pendência (a), fora do escopo de correção imediata desta Fase).

### 8. Frontend Engineer (Fase A — Correção do Achado 1 do QA)

- **Resumo da etapa**: correção cirúrgica do achado 1 (gemas/anéis nascendo sem zona de exclusão na faixa superior da tela, onde ficam o título "Academia PolvIn" e os chips `#academiaSubnav`). Antes de editar, reli `js/trailscene3d.js` inteiro e confirmei por leitura de `init()` (`camera.position.set(0,0,8)`, `camera.lookAt(0,0,0)`, `up` padrão do THREE `(0,1,0)`, sem nenhuma rotação de câmera) que **Y positivo é o topo da tela** — a convenção que a correção abaixo assume.
- **O que foi mudado** (`js/trailscene3d.js`):
  - Nova constante `TOP_EXCLUSION_FRACTION: 0.2` (linha ~221) — 20% da altura total do frustum, medida a partir do topo, reservada ao cabeçalho/subnav (usei 20%, o teto da faixa 15-20% sugerida pelo QA, para dar mais folga).
  - Novo método `randomSpawnY(depthZ, halfHFraction, objectRadius = 0)` (linha ~257) — sorteia Y uniformemente no mesmo range vertical que cada `build*()` já usava (`halfHFraction`, preservando o range inferior intacto), mas com o teto puxado para `(0.5 - TOP_EXCLUSION_FRACTION) * alturaTotal - objectRadius`. O parâmetro `objectRadius` garante que a **borda** do objeto (não só o centro) fique fora da faixa reservada — sem isso, um objeto com centro bem no limite ainda entraria visualmente na área excluída pela metade do seu próprio raio.
  - `buildGems()` (linha ~326): `mesh.position.set(..., this.randomSpawnY(depthZ, 0.42, radius), depthZ)` — passa o raio já calculado da gema.
  - `buildRings()` (linha ~345 aprox.): `outerRadius = ringRadius * 1.11` (raio do anel + raio do tubo do torus, borda externa real) e `mesh.position.set(..., this.randomSpawnY(depthZ, 0.38, outerRadius), depthZ)`.
  - **Bolhas e plâncton não foram alterados** — avaliei aplicar a mesma zona por consistência (pedido explícito do Orchestrator) e decidi não aplicar: (1) o QA não os citou (wireframe opacidade 0.25 e `Points` opacidade 0.35, "elemento mais fantasma da cena", não o "sólido" citado no achado); (2) bolhas sobem continuamente por design (`updateAmbient`) e reaparecem embaixo ao sair do topo — mesmo com zona de exclusão no spawn inicial, elas atravessariam a faixa reservada de qualquer forma como parte do próprio ciclo de vida da animação, então uma exclusão de spawn não resolveria nada ali, só mudaria o ponto de partida; corrigir isso de verdade exigiria mudar o `topBound` do loop de subida (fora do "achado 1", que é sobre posição de nascimento, não sobre o comportamento de bolhas em loop) — não fiz essa mudança maior sem novo pedido explícito, para manter a correção cirúrgica.
  - Nenhuma mudança em contagem de objetos (continuam 8 gemas, 3 anéis), paleta, opacidade, parallax ou ciclo de vida de aba — só a função de posicionamento em Y.
- **Como foi validado**:
  1. `node --check js/trailscene3d.js` — sintaticamente válido.
  2. Validação matemática independente (script Python, não Chrome headless — indisponível neste ambiente) recalculando `visibleHeightAtZ`/`visibleWidthAtZ`/`visibleMinDimAtZ` com a mesma fórmula do código (`2*tan(fov/2)*distância`, `aspect = 390/844`) para simular exatamente o viewport móvel estreito citado pelo QA. Testado em 3 profundidades Z por elemento, cobrindo os extremos e o meio de cada range (`gems: depthZ ∈ {-6, -4, -2}`, `rings: depthZ ∈ {-5, -3.5, -2}`), sempre no **pior caso** (maior raio possível sorteado, `Math.random()` no teto). Resultado: em todas as 6 combinações, o ponto mais alto do objeto (`centro máximo permitido + raio`) bate exatamente no limite da faixa de exclusão (`(0.5 - 0.2) * alturaTotal`) e nunca o ultrapassa — confirma matematicamente que, mesmo no cenário mais desfavorável (maior objeto possível, centro no teto do range permitido), nenhuma gema ou anel invade os 20% superiores da tela em 390×844.
  3. Não tive acesso, nesta sessão, à técnica de Chrome headless via CDP usada nas etapas anteriores da RFC (ferramenta indisponível no meu ambiente) — a validação ficou só na verificação geométrica/matemática acima, que é determinística e cobre o pior caso, não uma amostra aleatória. Fica registrado como diferença de método em relação às validações anteriores desta RFC, para o QA Engineer reproduzir visualmente se quiser confirmar por screenshot.
- **Arquivos alterados**: `js/trailscene3d.js` (`TOP_EXCLUSION_FRACTION`, `randomSpawnY()`, `buildGems()`, `buildRings()`). Nenhum outro arquivo tocado.
- **Pendências**: reverificação visual pontual pelo QA Engineer (screenshot real em 390×844, algumas recargas para amostrar posições aleatórias diferentes) para confirmar que a validação matemática corresponde ao resultado visual — a lógica está provada por cálculo, mas o QA havia usado captura de tela real na rodada anterior e pode preferir repetir isso especificamente para o achado 1.
- **Riscos**: nenhum novo. Mudança isolada à função de posicionamento; zero leitura/import de `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS`/eventos de progresso (não tocado nesta correção, confirmado por não ter sido necessário mexer em nada além do já listado acima).
- **Próximo agente responsável**: QA Engineer (reverificação pontual do achado 1, não uma nova rodada completa).

### 9. QA Engineer (Fase A — Reverificação visual pontual do Achado 1)

- **Resumo da etapa**: reverificação visual real (Chrome headless via CDP, viewport 390×844 literal, confirmado por leitura de `window.innerWidth/innerHeight` no próprio navegador) da correção aplicada na etapa 8, complementando a validação matemática do Frontend Engineer com evidência empírica. Testado via harness isolado (`TrailScene3D` carregado sem modificação, com os mesmos `<script>` de produção e SRI), já que o app real exige autenticação Supabase antes de renderizar a trilha — mesma limitação já registrada nas rodadas anteriores.
- **Método**: 10 reloads completos (cada um dispara `init()` do zero, gerando novas posições `Math.random()`), com dois métodos de verificação independentes por objeto: (1) reaplicação da fórmula do próprio código; (2) projeção via `camera.project()` (API padrão do Three.js, independente da lógica auditada) para saber a fração de tela real onde cada objeto aparece.
- **Resultado**: **110 objetos amostrados (8 gemas + 3 anéis × 10 reloads), 0 violações** nas duas verificações. Caso mais próximo do limite: fração 0.2054 do topo (limite 0.2, ~0.4% de margem) — consistente com a previsão de "pior caso bate exatamente no limite" do Frontend Engineer. Contagem de objetos estável em todas as rodadas (8 gemas, 3 anéis, 6 bolhas); nenhum erro/exceção de console além do warning pré-existente de depreciação do build UMD do Three.js (não é regressão desta correção). `node --check js/trailscene3d.js` OK; `git diff --stat` confirma que só `js/trailscene3d.js` foi tocado por esta correção, nenhum vazamento para outros arquivos.
- **Confirmado visualmente por screenshot**: elementos sólidos (gemas/anéis) nunca sobrepõem a área simulada de cabeçalho/subnav nas 10 capturas. O único elemento ocasionalmente próximo ao topo é uma bolha (wireframe, 0.25 de opacidade) — comportamento explicitamente fora do escopo desta correção (já documentado na etapa 8: bolhas atravessam o topo por design, como parte do próprio loop de subida, não um problema de spawn).
- **Veredito**: **Achado 1 resolvido.** Nenhum problema novo introduzido pela correção.
- **Pendências herdadas (não desta correção, sem mudança de status)**: teste em dispositivo móvel real e fluxo de lição completo com conta real continuam pendentes por falta de ambiente de staging Supabase — mesma lacuna de processo já escalada ao Product Owner na etapa 7.
- **Próximo agente responsável**: Documentation Specialist (fechar o registro da Fase A no `CHANGELOG.md`/`ROADMAP.md`, mesmo padrão já usado para a RFC-035).

### 10. Product Owner (Fase A.3 — Riqueza crescente na cena decorativa conforme o avanço)

- **Resumo da etapa**: releitura completa de `js/trailscene3d.js` (560 linhas, estado atual pós-RFC-037) e da RFC-037 (`rfcs/RFC-037-trilha-turbinada-avatar-biomas-celebracoes.md`, concluída v1.62.0) antes de escopar o pedido do usuário — verbatim: "imaginei na aba aprender, um cenário de fundo mais pobre no começo da trilha e conforme vai avançando o cenário fica mais rico e com mais fortunas e itens que remetem a bilionários". Confirmações relevantes por leitura direta de código:
  - `TrailScene3D` já calcula e usa um `progress` (0-1) — hoje **exclusivamente derivado do scroll da página** (`ScrollTrigger`, `trigger: document.body`), não do progresso real do jogador — para deslocar a textura de fundo (efeito "mergulho") e o parallax das camadas. Não existe hoje nenhuma leitura de `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS` neste módulo (confirmado por grep, também documentado no cabeçalho do arquivo) — isso é a "principal proteção" da RFC-036 (critério de aceite 2), reafirmada nas etapas de Software Architect, UX/UI Designer e Cyber Security Specialist.
  - A RFC-037 (avatar navegável) **já enfrentou uma tensão equivalente** e resolveu sem chamar `Trail.*`/`Business.*` diretamente: `js/trailavatar3d.js` lê **atributos de DOM** já renderizados por `trail.js` (`data-level`/`data-lesson` do nó "atual") e escuta `course:updated`, em vez de chamar a API/estado de progresso diretamente. Isso é um precedente de arquitetura já formalmente revisado e aprovado (RFC-037, critério de aceite 6, decisão do Software Architect) — relevante para a decisão abaixo, mas não torna a leitura de progresso real "grátis": ainda é uma mudança de comportamento perceptível (cena passa a variar entre sessões, não só dentro de uma rolagem), então continua exigindo decisão e justificativa explícitas, não deve ser assumida por analogia.

- **Interpretação do pedido — duas leituras possíveis, com implicações bem diferentes**:
  1. **Riqueza = posição de scroll na página** (mesma métrica que já existe hoje). "Começo da trilha" = topo da página; "avançar" = rolar para baixo. Zero mudança de arquitetura — é uma composição visual nova sobre o `progress` já calculado, extensão direta do efeito "mergulho" já existente e já validado em produção.
  2. **Riqueza = progresso real do jogador** (XP total, % de lições concluídas, nível). Um jogador com 80% de progresso veria a cena "rica" ao simplesmente abrir a aba, mesmo sem rolar — comportamento persistente entre sessões, não só dentro de uma rolagem. Exige relaxar deliberadamente o "zero acoplamento" da RFC-036 (mesmo tipo de exceção já concedida ao avatar na RFC-037, mas para um elemento diferente, com sua própria revisão).

- **Recomendação**: adotar a **Leitura 1 (scroll) como padrão desta Fase A.3**, pelos seguintes motivos:
  - É uma extensão direta e de baixíssimo risco do que já está em produção (`progress` já existe, já move a textura de fundo e o parallax) — não abre uma nova frente de arquitetura, mantém a Fase A.3 "quase puramente aditiva" no mesmo espírito da Fase A original.
  - Como a trilha renderiza TODOS os níveis (bloqueados e desbloqueados) de uma vez e o usuário já pode rolar até o fim independente do progresso real (confirmado no comportamento atual de `Trail.render()`), a Leitura 1 é **consistente** com o efeito "mergulho" já existente — não introduz um sinal visual novo e diferente do que a cena já comunica hoje, só reforça a mesma metáfora com mais elementos.
  - Entrega literalmente o que foi pedido — "mais pobre no começo, mais rico ao avançar" — dentro de uma única sessão de rolagem, sem exigir uma nova rodada de revisão de Cyber Security Specialist (como a Leitura 2 exigiria, pelo mesmo motivo que a RFC-037 precisou justificar a exceção do avatar).
  - **Risco explícito desta recomendação, que devolvo ao usuário para confirmação antes do Software Architect fechar o desenho técnico**: se a intenção real do usuário for "quero que a riqueza visual seja uma recompensa pelo que eu de fato conquistei na trilha" (persistente entre sessões, não um efeito de rolagem), a Leitura 1 **não entrega essa sensação** — um jogador iniciante que rola até o fim no primeiro dia veria a cena "rica" mesmo com 0% de progresso real. Se essa for a intenção, a Leitura 2 é a correta, e isso deveria ser confirmado ANTES do Software Architect avançar, porque muda o esforço/risco da fase de forma relevante (nova revisão de segurança, novo critério de aceite formal, precedente a seguir o desenhado na RFC-037 em vez do da RFC-036). **Pergunta direta para o usuário**: "a cena deve ficar mais rica conforme você rola a página (efeito de cada visita) ou conforme você realmente progride na trilha ao longo dos dias (efeito persistente, ligado ao seu progresso real)?"
  - Caso o usuário confirme a Leitura 2 no futuro, registro que a Leitura 1 desta fase não precisa ser descartada — pode coexistir como Fase A.3 (scroll, entregue agora) seguida de uma Fase A.4 (progresso real, se confirmado), sem retrabalho: os novos elementos de riqueza continuam existindo, só a fonte do "quanto mostrar" mudaria.

- **Objetivo**: reforçar a metáfora "quanto mais eu avanço, mais rico o cenário fica" já iniciada pelo efeito de profundidade ("mergulho") da Fase A, adicionando elementos visuais que remetam a prosperidade/riqueza (não só profundidade oceânica), crescendo em quantidade/presença conforme o progresso (scroll, nesta fase).

- **Motivação**:
  - Pedido explícito do usuário (dono do produto), consistente com o tema central do PolvIn (educação financeira leva à prosperidade) — a cena de fundo pode reforçar essa mensagem sem tocar em conteúdo educativo.
  - Reaproveita 100% da infraestrutura já construída e validada (RFC-036 Fase A/A.2, GSAP+ScrollTrigger, técnica de cel-shading) — nenhuma dependência nova, nenhum novo módulo obrigatório (extensão de `js/trailscene3d.js`).
  - Complementa, sem duplicar, o sistema de "temas por bioma" já entregue na RFC-037 (`js/trailthemes.js`, que tematiza os NÓS/NÍVEIS 2D da trilha) — a riqueza crescente vive na camada 3D de fundo, uma camada visual diferente, mas o UX/UI Designer deve revisar as duas juntas para garantir que não competem/parecem desconexas (risco listado abaixo).

- **Benefícios**:
  - Reforço de identidade visual de baixo custo/risco sobre uma feature já validada em produção (v1.60.0-1.62.0).
  - Momento "uau" adicional na aba de maior uso recorrente do app (Aprender, conforme `README.md`).
  - Não compete por orçamento de gamificação real (XP/moedas) — é estritamente cosmético, preservando a economia já calibrada.

- **User stories**:
  - Como jogador no início de uma sessão de rolagem, quero ver um cenário de fundo mais simples/vazio no topo da página, para sentir que a jornada tem um ponto de partida "humilde", coerente com a ideia de construir riqueza aos poucos.
  - Como jogador que rola bastante pela trilha, quero ver o cenário de fundo cada vez mais rico — mais gemas, tesouros, símbolos de prosperidade — para sentir uma recompensa visual tangível de "estar indo mais fundo/mais longe".
  - Como Product Owner, quero que essa riqueza crescente reforce a mensagem central do PolvIn sem introduzir uma segunda economia de recompensa paralela ao XP/moedas já existentes.

- **Documentação funcional** (o que a feature faz, para quem, por quê — sem entrar em como):
  - Para todo usuário na aba Aprender (trilha financeira/história unificada e Empreender, mesma composição, sem branch visual — mesma decisão de não divergir já tomada na Fase A original), a cena de fundo 3D existente ganha elementos adicionais que remetem a riqueza/prosperidade (mais gemas, mais anéis dourados, e ao menos um elemento novo do tipo "tesouro" — ver ideias abaixo), cuja presença/densidade cresce conforme o usuário rola a página para baixo. No topo da página, a cena está visualmente mais "pobre" (poucos elementos, mais espaçados); no fim, mais "rica" (mais elementos, mais densos/próximos). É puramente decorativo — não altera lições, XP, energia ou qualquer regra de progresso.

- **Ideias concretas de "itens que remetem a bilionários"** (direção de produto — composição final e viabilidade técnica exata cabem ao UX/UI Designer e Software Architect):
  - **Mais gemas e anéis dourados** conforme o progresso avança — extensão direta do que já existe (aumentar contagem/escala, não só posição), a opção de menor risco/esforço.
  - **Baús do tesouro estilizados** — geometria procedural simples (2 caixas formando uma tampa curva), cel-shaded em dourado/marrom, sem textura de imagem, no mesmo espírito de "gema"/"anel" já existentes.
  - **Coroas geométricas** — arranjo de poliedros pequenos (cones/prismas) formando a silhueta de uma coroa, douradas, flutuando como as gemas.
  - **"Ilhas"/pilhas de moedas** — aglomerados de cilindros achatados dourados (pilha de moedas estilizada), mais numerosos/próximos perto do fim da rolagem.
  - **Destroços de navio afundado com tesouro** — silhueta baixo-poly de casco + moedas espalhadas ao redor; reforça "riqueza acumulada no fundo do mar" sem recorrer a imagem realista de iate/mansão.
  - **Pérolas** — esferas brancas/douradas em cachos, reforço de luxo compatível com o tema oceânico, sem imagem realista.
  - **Feixes de luz mais intensos e frequentes** perto do fim da rolagem, "iluminando" o tesouro — reforça a sensação de destaque/holofote sobre a riqueza.
  - Em todos os casos: **sem imagens realistas de riqueza** (iate, mansão, carro de luxo, notas de dinheiro fotográficas) — quebrariam a identidade visual cel-shaded/baixo-poly já validada (mesma régua já usada pelo UX/UI Designer na Fase A: "se eu tirar a logo e as cores, ainda reconheço que é do PolvIn?").

- **Critérios de aceite**: ver itens 11-16 adicionados à seção "Critérios de aceite" desta RFC.

- **Pendências**:
  - **Para o usuário (antes do Software Architect fechar o desenho técnico)**: confirmar se a leitura correta é "riqueza por rolagem" (recomendação desta etapa, Leitura 1) ou "riqueza por progresso real" (Leitura 2) — ver justificativa acima. Software Architect pode iniciar já assumindo a Leitura 1 como padrão reversível, mas não deve tratar essa confirmação como dispensável.
  - **Para o Software Architect**: definir o mecanismo técnico de "densidade crescente" sem violar o orçamento de performance já estabelecido (30-40 meshes, hoje já em 19) — recomenda-se avaliar reaproveitar objetos pré-criados (variando escala/opacidade/visibilidade conforme `progress`) em vez de criar/destruir meshes a cada frame de scroll, mas a decisão final é dele. Se a Leitura 2 for confirmada pelo usuário, definir o mecanismo de leitura de progresso real seguindo o precedente da RFC-037 (leitura de estado via DOM/eventos já emitidos, não chamada direta a `Trail.*`/`Business.*`), e encaminhar nova revisão ao Cyber Security Specialist antes de produção.
  - **Para o UX/UI Designer**: composição final dos novos elementos de riqueza (formas exatas, quantidade, curva de crescimento ao longo do `progress`), decisão sobre variar (ou não) a ênfase entre Aprender e Empreender, e definição do "nível de riqueza" a mostrar no frame estático de `prefers-reduced-motion`. Recomenda-se também uma checagem cruzada com a composição de "temas por bioma" da RFC-037 (`js/trailthemes.js`) para garantir que as duas camadas (bioma nos nós 2D, riqueza no fundo 3D) reforcem-se em vez de competir visualmente.

- **Riscos**:
  - Ambiguidade de leitura do pedido (Leitura 1 vs. 2) pode gerar expectativa não atendida se o usuário realmente queria "recompensa por progresso real" e a fase entregar apenas um efeito de rolagem — mitigado pela pergunta direta registrada acima, a ser feita antes do fechamento do desenho técnico.
  - Orçamento de performance: a cena atual já usa 19 de 30-40 objetos (teto do Software Architect, RFC-036 seção 2) — adicionar elementos de riqueza crescente pode aproximar do teto, exigindo reuso/otimização em vez de simplesmente somar mais meshes.
  - Risco de sobreposição/confusão com o sistema de "temas por bioma" já entregue (RFC-037) — são camadas diferentes por design, mas a composição final precisa ser revisada em conjunto para não parecerem dois sistemas desconexos competindo por atenção visual na mesma tela.
  - Se a Leitura 2 for confirmada no futuro, reabre a mesma tensão de arquitetura já navegada uma vez na RFC-037 (avatar) — não é proibitivo (há precedente), mas exige nova revisão formal de Cyber Security Specialist e novo critério de aceite, não deve ser tratado como decisão trivial.

- **Próximo agente responsável**: Software Architect (definir viabilidade técnica da densidade crescente dentro do orçamento de performance existente, e confirmar a leitura de `progress` a adotar — assumindo Leitura 1 como padrão reversível até confirmação do usuário).

### 11. Software Architect (Fase A.3 — Arquitetura da riqueza crescente)

- **Resumo da etapa**: releitura completa de `js/trailscene3d.js` (560 linhas, estado atual pós-correção do achado 1 do QA) e leitura dirigida de `js/trailavatar3d.js` (cabeçalho, linhas ~42-43 e ~101-105) e da seção "Orçamento de performance mobile" da RFC-037, para embasar a decisão de orçamento sem repetir o erro de tratar `TrailScene3D` como se fosse o único consumidor de GPU/CPU 3D na aba Aprender. Confirmações relevantes por leitura direta de código (não por suposição):
  - Contagem atual confirmada: 1 `bgMesh` + 8 `gems` + 3 `rings` + 6 `bubbles` + 1 `Points` (350 partículas, 1 draw call) = **19 objetos**, exatamente como documentado nas etapas 6 e 9 — nenhuma mudança desde a Fase A.2/correção do achado 1.
  - `this.progress` é escrito 1x por atualização do `ScrollTrigger` (`onUpdate: (self) => { this.progress = self.progress; }`, linha 433, atribuição de número — sem alocação) e **lido** 1x por frame de `requestAnimationFrame` dentro de `renderFrame()` (linha 465-466), não 1x por evento de scroll. Esse desacoplamento entre "frequência de scroll" (pode disparar dezenas de vezes por segundo num gesto rápido) e "frequência de render" (capada em 60fps pelo rAF) já é o padrão estabelecido por `applyParallax`/`applyBackgroundOffset` — a riqueza crescente deve seguir exatamente esse mesmo padrão, não inventar um caminho de atualização novo.
  - `THREE.InstancedMesh` é API de **núcleo** do Three.js (`src/objects/InstancedMesh.js`, parte do bundle principal desde a revisão r105, 2020) — diferente de `OrbitControls`/`GLTFLoader`/`BufferGeometryUtils`, que vivem em `examples/jsm/*` e **não** estão disponíveis no build UMD (`three@0.150.0`) já carregado por este projeto sem ES modules (restrição já documentada pelo Software Architect na etapa 2, seção Fase B). `InstancedMesh` não tem essa restrição — pode ser usado hoje, sem nenhuma dependência nova.
  - `js/trailavatar3d.js` roda seu **próprio** `WebGLRenderer`/`THREE.Scene` (linhas 101-105) — um segundo contexto WebGL, independente do de `TrailScene3D` — com orçamento próprio de ~23 meshes (rig do PolvIn), já formalmente combinado no precedente "**~42 meshes combinados em 2 contextos WebGL simultâneos** na aba Aprender" (comentário no próprio `trailavatar3d.js`, linhas 42-43, e RFC-037 seção "Orçamento de performance mobile"). **Isso importa diretamente para a decisão de teto abaixo**: qualquer crescimento do orçamento de `TrailScene3D` se soma a esse total combinado na prática de FPS real do dispositivo — os dois módulos têm tetos "próprios" documentados separadamente, mas rodam ao mesmo tempo, na mesma aba, competindo pelo mesmo GPU.

#### 1. Mecanismo técnico: pool fixo pré-criado, nunca criar/destruir por frame — confirmo a sugestão do Product Owner, sem ressalvas

- **Decisão**: todos os objetos novos de "riqueza" são criados **1x**, dentro de `init()` (mesmo momento lazy de hoje — 1ª vez que a aba Aprender abre), e **nunca** removidos da cena nem tem sua geometria/material recriados depois disso. O que varia com `progress` é só `object.visible` (boolean), `object.scale` (número) e `object.material.opacity` (número) — três leituras/escritas de propriedade primitiva por objeto por frame, exatamente o mesmo tipo de operação barata que `applyParallax()` já faz hoje em `midGroup.position.y`/`nearGroup.position.y`.
- **Por que não criar/destruir por frame de scroll (rejeitado)**: `scrub:true` faz o `ScrollTrigger` recalcular `progress` em alta frequência durante um gesto de rolagem rápido (potencialmente dezenas de vezes por segundo) — se a aparição de um item de riqueza fosse implementada como "instanciar `new THREE.Mesh(new THREE.IcosahedronGeometry(...), ...)` quando `progress` cruza um limiar", isso rodaria alocação de heap (geometria = `Float32Array`s novos para posição/normal/UV) dentro do que é, na prática, um hot path de scroll — o antipadrão clássico de WebGL "nunca alocar dentro do loop de render/scroll". Pior ainda no sentido inverso: destruir um objeto corretamente em Three.js exige chamar `geometry.dispose()`/`material.dispose()` para não vazar buffers de GPU — sem isso, cada aparição/desaparição ao longo de uma sessão de rolagem (usuário sobe e desce a página várias vezes) vazaria memória de vídeo progressivamente. Um pool fixo elimina as duas categorias de problema de uma vez: não há alocação nova depois do `init()`, e não há necessidade de `dispose()` nenhuma vez, porque nada é destruído — mesmo princípio "pausar, não destruir" que o Software Architect já usou para o ciclo de vida `activate()/deactivate()` da cena inteira (etapa 2), agora aplicado no nível de objeto individual dentro da cena.
- **Consequência prática**: os itens do pool nascem **invisíveis** (`visible:false`, `scale.setScalar(0)`) na construção, e a função de atualização por frame (`applyRichness`, abaixo) é o único código que os torna visíveis — nunca `scene.add()`/`scene.remove()` depois do `init()`.

#### 2. Novos objetos: 12 unidades de orçamento (dentro da folga de 15), 3 tipos técnicos, todos reaproveitando técnica já validada

Orçamento restante calculado abaixo (seção 5) permite até 15 unidades novas mantendo margem de segurança. Proponho **12**, deixando 3 de folga para ajuste do UX/UI Designer sem nova rodada de aprovação de orçamento:

| Tipo | Geometria/técnica | Unidades de orçamento | Contagem visual real | Por que essa técnica |
|---|---|---|---|---|
| **Gemas douradas extras** | `IcosahedronGeometry` — literalmente `buildGems()` reaproveitado, só com paleta enviesada para `--gold`/`--gold-dark` (reforça "riqueza" sem geometria nova) | 4 (1 mesh cada) | 4 | Zero código novo de geometria — mesma função, novo array de cor e novo `userData.appearAt` (ver seção 3) |
| **Baús do tesouro** | `THREE.Group` de 2 `BoxGeometry` (corpo + tampa, tampa levemente rotacionada para sugerir "entreaberto") — ideia do Product Owner, geometria mais simples possível para a silhueta ler como "baú" | 6 (3 baús × 2 meshes) | 3 baús | Compostos (2 formas ≠), não cabem num único `InstancedMesh` (que exige geometria idêntica repetida) — mas 3 unidades é pouco o bastante para não pesar no orçamento como meshes individuais |
| **Pilha de moedas** | `THREE.InstancedMesh(CylinderGeometry achatado, material, instanceCount)` | 1 (1 objeto de cena, N instâncias internas) | ~18 "moedas" | Mesmo princípio já usado pelo `Points` do plâncton (350 partículas = 1 draw call): muitas peças idênticas, 1 unidade de orçamento. `InstancedMesh` é a forma certa quando a geometria se repete (moedas) — ao contrário do baú, que não se repete |
| **Pérolas** | `THREE.InstancedMesh(SphereGeometry baixo-poly, material, instanceCount)` | 1 (1 objeto de cena, N instâncias internas) | ~14 "pérolas" | Mesma justificativa da pilha de moedas — reforça luxo sem custo de orçamento proporcional à contagem visual |
| **Total novo** | — | **12** | ~39 elementos visuais percebidos | — |

- **Coroas (ideia do Product Owner) — viável, mas fica de fora da proposta inicial por orçamento, não por dificuldade técnica**: tecnicamente cabe no mesmo padrão de `InstancedMesh` (um `ConeGeometry` pequeno repetido 5-6× por coroa, todas as coroas da cena num único `InstancedMesh` — cada instância recebe sua matriz de transformação própria, então múltiplas coroas em posições diferentes continuam custando **1** unidade de orçamento total, não 1 por coroa). Deixo como a primeira extensão natural se o UX/UI Designer quiser usar parte da folga de 3 unidades restantes, ou se o Product Owner priorizar coroas sobre pérolas/moedas na composição final — troca 1-por-1 dentro do mesmo teto, não uma adição.
- **Destroços de navio (ideia do Product Owner) — não incluído na proposta inicial**: tecnicamente viável (casco = `THREE.Group` de poucos `BoxGeometry`/`CylinderGeometry` deformados, mesmo padrão do baú), mas é o item de maior custo de unidades (silhueta de casco precisa de mais peças que um baú para ler como "navio" e não como "caixa comprida") pela menor "riqueza visual por unidade de orçamento" da lista do Product Owner — recomendo tratá-lo como candidato de uma iteração futura (Fase A.4/refinamento), não obrigatório para o primeiro incremento da Fase A.3.
- **Sem asset externo em nenhum caso** (critério de aceite 13): todas as geometrias acima são procedurais (`BoxGeometry`/`CylinderGeometry`/`SphereGeometry`/`ConeGeometry`, todas primitivas núcleo do Three.js), todos os materiais são `MeshToonMaterial` + o mesmo `gradientMap` já construído 1x em `buildGradientMap()` — nenhuma textura de imagem, nenhum `.glb`/`.gltf`, nenhuma cor fora de `:root`.

#### 3. Curva de crescimento: aparição escalonada por item + rampa suave (smoothstep), não um interruptor por faixa

- **Cada item do pool recebe, na construção, um `userData.appearAt` (0-1)** — o valor de `progress` a partir do qual o item começa a aparecer — distribuído de forma escalonada ao longo da faixa `[0.15, 0.95]` (não `[0,1]` inteiro): o início em `0.15`, não `0`, garante que a cena realmente comece "pobre" nos primeiros instantes de rolagem (critério de aceite 11 — "sensivelmente menor em `progress=0`"); o fim em `0.95`, não `1.0`, garante que o último item já esteja com fade concluído antes do fim absoluto da página, evitando um "pop" de última hora. Distribuição sugerida (Frontend Engineer/UX ajustam os valores exatos, a ordem de aparição é a decisão estrutural aqui): gemas douradas primeiro (`0.15-0.4`, o reforço mais "sutil"), pilha de moedas em seguida (`~0.35`), baús espalhados no meio-fim (`0.5/0.7/0.9`), pérolas por último (`~0.6`) — a lógica de produto (qual elemento é mais "impressionante" deve vir mais tarde) cabe ao UX/UI Designer confirmar, a mecânica de escalonamento é a parte que fixo aqui.
- **Rampa de entrada suave, não abrupta**: cada item usa uma janela de transição fixa, `RICHNESS_FADE_WINDOW = 0.18` (18% do percurso total de `progress`), com easing `smoothstep` (`t*t*(3-2*t)`, a mesma curva de easing já usada implicitamente em várias animações do projeto — suave nas duas pontas, sem `easeIn`/`easeOut` abruptos):
  ```js
  applyRichness(progress) {
    this.richnessPool.forEach((obj) => {
      const t = THREE.MathUtils.clamp((progress - obj.userData.appearAt) / this.RICHNESS_FADE_WINDOW, 0, 1);
      const factor = t * t * (3 - 2 * t); // smoothstep — sem popping
      obj.visible = factor > 0.01; // evita custo de render de objetos com fator ~0
      obj.scale.setScalar(obj.userData.baseScale * factor);
      if (obj.material && obj.userData.baseOpacity != null) {
        obj.material.opacity = obj.userData.baseOpacity * factor;
      }
    });
  }
  ```
  Isso satisfaz o critério de aceite 11 de forma mensurável: em `progress=0`, todo `factor` é `0` (nenhum item visível, `visible=false` — custo de render zero, não só invisual); em `progress=1`, todo item com `appearAt + RICHNESS_FADE_WINDOW ≤ 1` está em `factor=1` (escala/opacidade plenas). Entre os dois extremos, a densidade cresce em **degraus sobrepostos e suavizados** (não uma faixa só, não um salto único) — cada item começa a nascer, cresce suavemente, e o próximo já começa a nascer antes do anterior terminar de crescer (janelas de `0.18` com `appearAt` espaçados por `~0.1-0.2` se sobrepõem), dando a sensação de "cada vez mais coisas aparecendo" em vez de "3-4 blocos discretos ligando/desligando".
- **`InstancedMesh` (moedas/pérolas) usa o mesmo mecanismo, mas em granularidade de objeto inteiro, não por instância individual**: o cluster inteiro (todas as ~18 moedas ou ~14 pérolas) tem 1 `appearAt`/1 `factor`, aplicado a `mesh.visible`/`mesh.scale` do `InstancedMesh` como um todo — decisão deliberada de **não** escalonar instância-por-instância (o que exigiria recompor a matriz de cada instância a cada frame via `setMatrixAt`/`instanceMatrix.needsUpdate=true`, um custo de CPU proporcional à contagem de instâncias, rodando a cada rAF). Escalonar por objeto inteiro mantém `applyRichness()` com custo O(tamanho do pool, ~16 objetos), não O(total de instâncias, ~39) — mais barato e, com 4 tipos diferentes de `appearAt`, ainda produz sensação de crescimento gradual, só que em granularidade de "tipo de tesouro" em vez de "peça individual".
- **Luz "spotlight" sobre o tesouro — reforço gratuito, sem custo de orçamento**: a ideia do Product Owner de "feixes de luz mais intensos perto do fim" não precisa de nenhum mesh novo — basta modular `dirLight.intensity` (já existente, linha 90-92) em função de `progress`: `dirLight.intensity = 0.5 + progress * 0.35` (base 0.5, sobe até 0.85 no fim), dentro de `applyRichness()`. Recomendo incluir por ser custo zero de orçamento de meshes e reforçar a metáfora "holofote sobre a riqueza" sem exigir aprovação de orçamento adicional.

#### 4. Onde no código (`js/trailscene3d.js`, sem novo módulo)

- **Novo array de estado**: `richnessPool: []` (mesmo nível de `gems`/`rings`/`bubbles`, linhas 39-42).
- **Novas constantes**: `RICHNESS_FADE_WINDOW: 0.18`, `RICHNESS_APPEAR_START: 0.15`, `RICHNESS_APPEAR_END: 0.95`, `REDUCED_MOTION_RICHNESS_PROGRESS: 0.5` (ver seção 6) — mesmo padrão de constante nomeada em maiúsculas já usado por `TOP_EXCLUSION_FRACTION` (linha 221).
- **Novos métodos de construção**, chamados em `init()` logo depois de `this.buildPlankton()` (linha 104) e antes de `this.reducedMotion = this.prefersReducedMotion()` (linha 106) — mesma posição relativa dos `build*()` existentes, mesma convenção de nome:
  - `buildRichnessGems()` — 4 gemas douradas extras, reaproveita a lógica de `buildGems()` quase literalmente (mesmo `IcosahedronGeometry`, mesmo `randomSpawnY`/`visibleMinDimAtZ`), mas empurra cada mesh para `richnessPool` (via helper `scheduleAppearance`, abaixo) em vez de deixá-las sempre visíveis como as 8 gemas originais.
  - `buildChests()` — 3 baús (`THREE.Group` de 2 `BoxGeometry` cada), posicionados com o mesmo `randomSpawnY`/exclusão de topo já usado por gemas/anéis (são elementos "sólidos", mesma regra do achado 1 do QA já corrigido).
  - `buildCoinCluster()` — 1 `InstancedMesh` de moedas.
  - `buildPearlCluster()` — 1 `InstancedMesh` de pérolas.
  - Helper comum `scheduleAppearance(obj, appearAt, baseScale, baseOpacity)` — centraliza a inicialização (`visible=false`, `scale.setScalar(0)`, grava `userData`, `this.richnessPool.push(obj)`, adiciona como filho de `this.midGroup`) para as 4 funções de build acima não repetirem essa lógica 4 vezes.
- **Todos os itens novos entram em `this.midGroup`** (não um grupo novo): reaproveita, sem código adicional, o mesmo `applyParallax()` que já move `midGroup.position.y` (fator `0.3`) a cada frame — riqueza se move junto com as gemas/anéis existentes, na mesma profundidade narrativa ("tesouro no mesmo estrato do meio da cena"), sem precisar de um terceiro grupo/fator de parallax dedicado. Menos estado novo = menos risco.
- **Novo método de atualização por frame**: `applyRichness(progress)` (corpo no item 3 acima), chamado em `renderFrame()` logo depois de `this.applyParallax(this.progress)` (linha 465) — mesma posição relativa, mesmo padrão de chamada de método único por responsabilidade que o arquivo já usa (`updateAmbient`/`applyParallax`/`applyBackgroundOffset`, todas chamadas em sequência dentro de `renderFrame()`).
- **`renderStaticFrame()` (linha 518-526)**: adicionar `this.applyRichness(this.REDUCED_MOTION_RICHNESS_PROGRESS)` — ver seção 6 para o porquê do valor não ser `0`. As chamadas existentes (`applyParallax(0)`/`applyBackgroundOffset(0)`) **não mudam** — só a riqueza usa um valor de `progress` diferente no frame estático, isolando a mudança nova do comportamento já testado/aprovado pelo QA (etapas 7/9) para o "mergulho".
- **Nenhum novo `STORAGE_KEYS`, nenhum novo `CustomEvent`, nenhuma nova leitura de `Trail.*`/`Business.*`/`Learn.*`** — confirma o critério de aceite 12: `progress` continua vindo exclusivamente do mesmo `ScrollTrigger` já existente (linha 427-436), sem nenhuma linha nova de leitura de estado de jogo.

#### 5. Orçamento de performance final

- **Teto de `TrailScene3D`: 31 objetos-unidade no cenário recomendado** (19 existentes + 12 novos, seção 2), com **34 como teto rígido desta fase** (deixando 3 unidades de folga só para ajuste do UX/UI Designer, ex. trocar pérolas por coroas ou adicionar 1 unidade extra — qualquer coisa além de 34 exige nova aprovação explícita de orçamento, não é uma decisão que o Frontend Engineer deva tomar sozinho). Isso fica dentro do teto de 30-40 já definido na etapa 2 desta RFC, mas **deliberadamente na metade inferior da faixa, não perto de 40** — motivo abaixo.
- **Por que não usar toda a folga até 40**: `js/trailavatar3d.js` roda um segundo `WebGLRenderer` simultâneo na mesma aba, com orçamento próprio de ~23 meshes já formalmente combinado no precedente "~42 meshes combinados em 2 contextos WebGL" da RFC-037. Se `TrailScene3D` sozinho subisse a 40, o total combinado passaria a ~63 — quase 50% acima do que já foi validado em produção (~42). Manter `TrailScene3D` em 34 mantém o total combinado em ~57, ainda acima do precedente de 42, mas um crescimento mais defensável (~36%) do que levar `TrailScene3D` ao teto absoluto. **Sinalizo explicitamente para o QA Engineer**: o teste de FPS desta fase não pode repetir o método da Fase A original (medir só `TrailScene3D` isoladamente) — precisa medir com os **dois** contextos WebGL ativos ao mesmo tempo (`TrailScene3D` com riqueza em `progress` próximo de 1 **e** `TrailAvatar3D` visível/navegável simultaneamente), porque esse é o cenário real de pior caso da aba Aprender, não uma composição hipotética.
- **Critério objetivo de validação (QA Engineer)**: mesmo processo rigoroso já usado nas etapas 6/7/9 desta RFC — Chrome headless real via CDP, FPS por `requestAnimationFrame` timestamping, viewport mobile 390×844, `Emulation.setCPUThrottlingRate: 4x` — mas desta vez com 3 cenários obrigatórios, não 1:
  1. `TrailScene3D` sozinho (avatar fora de viewport/não intersectando, replicando o teste original da Fase A) — mínimo 30fps mobile/4x throttle, alvo 60fps desktop, **igual ao já validado**.
  2. `TrailScene3D` com `progress≈1` (densidade máxima de riqueza) **+** `TrailAvatar3D` ativo simultaneamente — mesmo mínimo de 30fps; este é o cenário novo que a Fase A.3 introduz e que precisa de validação própria, não pode herdar "passou antes" da Fase A original.
  3. Transição de `progress` de `0` a `1` em scroll contínuo (não só os extremos) — checar ausência de "engasgo"/frame drop perceptível durante a janela de `RICHNESS_FADE_WINDOW` de cada item, quando `applyRichness()` está de fato alterando `scale`/`opacity` de múltiplos objetos por frame (o cenário de maior custo de CPU por frame da função nova).
  - Se o cenário 2 não atingir 30fps em dispositivo real (não só emulado), a alavanca de correção é **reduzir o teto de `TrailScene3D`** (não o de `TrailAvatar3D`, que já está validado e é o rig central do avatar) — primeira coisa a cortar: pérolas/moedas extras (menor "riqueza visual por unidade", conforme análise da seção 2), não os baús/gemas (maior valor de composição por unidade).
- **Sem asset pipeline novo, sem mudança de pixel ratio**: mantém tudo já definido na etapa 2 (`Math.min(devicePixelRatio,2)`, geometria 100% procedural, `CanvasTexture` em runtime apenas).

#### 6. `prefers-reduced-motion`: frame estático usa um valor intermediário fixo, não `progress=0` — decisão nova desta fase

- **Decisão**: `progress=0` **não é aceitável** para o frame estático da riqueza (embora continue sendo o valor correto para `applyParallax`/`applyBackgroundOffset`, que não mudam nesta fase). Se a riqueza usasse literalmente `progress=0`, todo `factor` calculado em `applyRichness(0)` seria `0` (a distribuição de `appearAt` começa em `0.15`, seção 3) — ou seja, usuários com `prefers-reduced-motion` **nunca veriam nenhum elemento novo desta fase**, em nenhuma circunstância, porque o único momento em que a cena é desenhada para eles é esse frame único. Isso não seria "manter a identidade, remover só o movimento" (o princípio já usado com sucesso na Fase A original, etapa 3) — seria **esconder a feature inteira** por trás de uma preferência de acessibilidade, o que é uma regressão de experiência mais séria do que simplesmente "não animar".
- **Escolha do valor**: introduzo a constante `REDUCED_MOTION_RICHNESS_PROGRESS = 0.5`, usada **só** dentro de `applyRichness()` quando chamada por `renderStaticFrame()` — um ponto médio da sessão de rolagem, que na prática (com `appearAt` distribuído em `0.15-0.95`, seção 3) mostra aproximadamente metade dos itens do pool já visíveis em escala/opacidade parcial ou plena, e a outra metade ainda ausente. Isso comunica visualmente "a cena tem uma progressão de riqueza" mesmo em uma única imagem estática — mais representativo do que mostrar 0% (parece que a feature não existe) ou 100% (parece que a cena é estaticamente rica, sem comunicar que existe uma progressão ligada ao scroll, que é justamente a mensagem pedida pelo Product Owner).
- **`0.5` é um valor de partida técnico, não uma decisão visual final**: como o próprio Product Owner delegou explicitamente essa escolha ("decida... e justifique"), fixo a decisão **estrutural** (usar um valor fixo intermediário nomeado, não `0` nem um valor mágico solto no meio do código) e deixo o **valor exato** (`0.5` vs. `0.4` vs. `0.6`) como parâmetro que o UX/UI Designer pode ajustar na próxima etapa olhando o resultado visual real — é 1 constante, não uma mudança estrutural, então o custo de revisão é desprezível.
- **Todo o resto do comportamento de `prefers-reduced-motion` já definido na etapa 3 permanece intocado**: 1 frame só, sem `requestAnimationFrame`, sem `ScrollTrigger` de movimento, sem listener de scroll registrado — a riqueza no frame estático é desenhada 1x e nunca recalculada, exatamente como gemas/anéis/bolhas já são hoje.

#### Resumo para o UX/UI Designer (próxima etapa)

| Item | Decisão do Software Architect | O que falta decidir |
|---|---|---|
| Mecanismo | Pool fixo de 12 objetos-unidade (4 gemas douradas, 3 baús=6 meshes, 1 cluster de moedas, 1 cluster de pérolas), nunca criado/destruído após `init()` | — (fechado) |
| Orçamento | 19 existentes + até 12 novos = 31 recomendado, 34 teto rígido | Composição exata dentro desse teto (trocar pérolas por coroas, redistribuir contagem) |
| Curva de aparição | `appearAt` por item, distribuído em `[0.15, 0.95]`, rampa `smoothstep` de `0.18` de largura | Ordem de aparição exata por tipo (qual "tesouro" é mais impressionante e deve vir por último) |
| Cores | `--gold`/`--gold-dark` (moedas, baús, gemas extras), branco/dourado claro (pérolas) — nenhuma cor nova | Tonalidade exata dentro da paleta já aprovada |
| Luz | `dirLight.intensity` sobe de 0.5 a 0.85 com `progress` — custo zero de orçamento | Confirmar se o efeito "holofote" é perceptível o bastante ou precisa de ajuste de faixa |
| `prefers-reduced-motion` | Frame estático usa `progress=0.5` (constante nomeada, ajustável) para a riqueza — `0` continua valendo para mergulho/parallax | Validar visualmente se `0.5` é o ponto certo ou se outro valor comunica melhor "há uma progressão" |
| Checagem cruzada com RFC-037 | Riqueza entra em `midGroup` (mesma profundidade das gemas/anéis) — recomendo ao UX/UI Designer revisar lado a lado com os temas de bioma de `js/trailthemes.js` para confirmar que dourado/tesouro não colide com a paleta de nenhum bioma específico | Revisão visual conjunta (pendência já sinalizada pelo Product Owner na etapa 10) |

- **Pendências**:
  - Confirmação do usuário sobre Leitura 1 vs. Leitura 2 (scroll vs. progresso real) continua em aberto — esta seção assume Leitura 1 (já era o padrão recomendado pelo Product Owner na etapa 10); se o usuário confirmar Leitura 2 no futuro, o mecanismo de pool/curva de aparição desta seção **não muda** (continua válido), só a fonte do valor injetado em `applyRichness()` mudaria de `this.progress` (scroll) para um valor derivado de progresso real lido via DOM/evento (mesmo precedente da RFC-037 para o avatar) — sinalizo que essa troca de fonte é estrutural e exigiria nova revisão do Cyber Security Specialist antes de produção, como já apontado pelo Product Owner.
  - Valor exato de `REDUCED_MOTION_RICHNESS_PROGRESS` (`0.5` proposto) fica para confirmação visual do UX/UI Designer.
  - Se o UX/UI Designer preferir coroas a pérolas ou moedas, a troca é 1-por-1 dentro do teto de 34 — não precisa voltar a este agente, desde que a contagem total não ultrapasse o teto.
- **Riscos**:
  - **Orçamento combinado com `TrailAvatar3D`** (seção 5) é o risco estrutural mais concreto desta fase — o teto de 34 é uma escolha deliberadamente conservadora por causa disso, mas só o QA Engineer com os 3 cenários de teste definidos acima pode confirmar se isso é suficiente na prática, em hardware real (não emulado).
  - Mesmo risco de disciplina de acoplamento já sinalizado na etapa 2 para a Fase A original: nada impede um Frontend Engineer apressado de, "só para dar mais impacto", ligar `appearAt`/`RICHNESS_FADE_WINDOW` a `Trail.getProgresso()` ou qualquer chamada real de progresso em vez do `this.progress` de scroll — isso violaria o critério de aceite 12 (Leitura 1 como padrão até confirmação do usuário) pelas costas do processo. Sinalizo explicitamente, mesma vigilância pedida ao QA Engineer/Cyber Security Specialist na etapa 5.
  - Se o UX/UI Designer optar por escalonar `InstancedMesh` por instância individual (em vez de por cluster inteiro, decisão da seção 3) para um efeito visual mais gradual, isso muda o custo de `applyRichness()` de O(tamanho do pool) para O(total de instâncias) — não é proibitivo (~39 instâncias é pouco), mas se o UX/UI Designer pedir isso, deve ser sinalizado de volta a este agente para reavaliar o orçamento de CPU por frame, não implementado como um ajuste "só visual" pelo Frontend Engineer.

- **Próximo agente responsável**: UX/UI Designer (composição visual final dos novos objetos de riqueza, curva de progressão sobre o mecanismo técnico definido aqui).

### 12. UX/UI Designer (Fase A.3 — Composição visual da riqueza crescente)

- **Resumo da etapa**: releitura de `js/trailscene3d.js` (estado atual, `buildGems()`/`buildRings()`/`toonMat()`/`randomSpawnY()`/`visibleMinDimAtZ()` já existentes), da seção 11 (Software Architect — mecanismo de pool fixo, orçamento de 12 unidades em 4 tipos, curva `smoothstep`/`RICHNESS_FADE_WINDOW`, já fechados e não reabertos aqui) e de `js/trailthemes.js` + a seção CSS `BIOMAS` (`css/style.css` linhas 1128-1218) para a checagem cruzada pedida pelo Product Owner (etapa 10). Três confirmações de código guiam as decisões abaixo:
  - A correção do achado 1 do QA (etapa 7, `TOP_EXCLUSION_FRACTION`/`randomSpawnY(depthZ, halfHFraction, objectRadius)`) já é tratada pelo Architect como aplicável aos baús ("são elementos sólidos, mesma regra do achado 1"). **Estendo explicitamente a mesma regra aos clusters de moedas e pérolas** (`InstancedMesh`): mesmo eles tendo opacidade abaixo de 1.0 (0.9/0.8, ver abaixo), ainda são visualmente dos elementos mais "carregados" da cena (muitas instâncias pequenas e brilhantes concentradas numa área) — se o footprint de um cluster nascer parcialmente na faixa reservada ao cabeçalho/subnav, o efeito de "várias moedas atrás do texto" é pior que 1 gema isolada ali (a falha original do achado 1). O Software Architect não cobriu isso explicitamente na seção 11 (só falou de baús); fecho a lacuna aqui: **todo objeto/cluster novo usa `randomSpawnY`/exclusão de topo**, sem exceção, incluindo os `InstancedMesh` (o "objeto" para fins de exclusão é o centro/âncora do cluster, com `objectRadius` = raio aproximado do footprint do amontoado, não de uma instância individual).
  - `TRAIL_THEMES`/`data-bioma` (RFC-037) tematiza os **nós/níveis 2D** (`.trail-level[data-bioma]::before`, um wash de cor absoluto sobre cada card de nível) — camada visualmente distinta da cena 3D de fundo (`z-index:-1` vs. o wash dentro de `.trail-level`, `position:relative;z-index:1`), mas **compartilham o mesmo canal de cor** em 2 dos 5 biomas: `correnteza-dourada` usa `rgba(232,163,61,*)` — exatamente `--gold`, o mesmo hex que domina toda a composição de riqueza desta fase — e `abismo-profundo` usa roxo quase preto (`rgba(45,27,78,*)`/`rgba(10,6,20,*)`), próximo da parada final do gradiente de fundo já existente (`--primary-dark`). Análise e recomendação na subseção "Checagem cruzada" abaixo.
  - `buildGems()`/`buildRings()` já ciclam cor "1 em cada 3" (padrão de alternância já estabelecido) — reaproveito literalmente essa convenção para as gemas douradas extras e para a alternância branco/dourado das pérolas, em vez de inventar uma nova regra de distribuição de cor.

#### Baús do tesouro: geometria exata

**Corpo + tampa em meio-cilindro "entreaberta"**, não tampa plana — silhueta mais reconhecível como "baú" e reforça a narrativa "derramando riqueza" (ver ordem de aparição abaixo):

| Peça | Geometria Three.js | Proporção (relativa a `visibleMinDimAtZ(depthZ)`) | Rotação/posição |
|---|---|---|---|
| Corpo | `BoxGeometry(w, h, d)` | `w ≈ 0.16×dim`, `h ≈ 0.10×dim`, `d ≈ 0.11×dim` (proporção ~1 : 0.6 : 0.7, igual a um baú real, não um cubo) | Base assentada em `y = spawnY`, sem rotação própria (só a leve rotação individual herdada do grupo, mesma lógica de `mesh.rotation` randomizada por objeto que gemas/anéis já usam) |
| Tampa | Metade de `CylinderGeometry(raio ≈ h/2, raio, altura = d, radialSegments 10, openEnded:true, thetaStart:0, thetaLength:Math.PI)` — um "telhado em arco", não uma tampa reta | Rotacionada -90° em X (eixo do cilindro deitado, alinhado com a profundidade `d` do corpo) + inclinação extra de **~18° em torno do eixo X**, simulando a tampa entreaberta; posicionada no topo do corpo (`y = h/2` relativo ao grupo) | 

Ambas as peças vivem dentro de 1 `THREE.Group` por baú (3 grupos = as "3 unidades de agendamento", mesmo que a contagem de orçamento do Architect seja 6 — 1 grupo = 1 `userData.appearAt`/1 `scheduleAppearance()`, os 2 meshes filhos herdam visibilidade/escala do grupo, não são agendados separadamente).

**Por que meio-cilindro e não uma segunda `BoxGeometry` (opção que o Architect também citou como válida)**: a silhueta de um baú com tampa reta, na distância/escala pequena em que esses objetos aparecem no fundo da cena, tende a ler como "caixa" genérica — o mesmo problema de "componente genérico" que a filosofia de design do PolvIn pede para evitar. O arco da tampa é a diferença mínima de geometria (1 primitiva a mais de configuração, não mais peças) que torna a silhueta inconfundivelmente "baú do tesouro" mesmo em baixa resolução visual, coerente com o mesmo padrão de "escolher a geometria com maior leitura por unidade de complexidade" que já rege gemas (icosaedro facetado, não esfera) e bolhas (wireframe, não esfera sólida).

#### Distribuição espacial dos clusters (`InstancedMesh`) — amontoado intencional, não ruído

**Pilha de moedas (18 instâncias)** — 5 "torres" de moedas empilhadas (não 18 posições soltas), contagem por torre `[2, 3, 4, 4, 5]` (cresce em direção ao centro do amontoado, criando um perfil de "montinho", não um platô uniforme):
- Centros das 5 torres dispostos num pentágono irregular pequeno (raio ≈ `0.12×visibleMinDimAtZ(depthZ)` a partir do centro do cluster), a torre de 5 moedas no centro geométrico, as demais ao redor — o olho é puxado para o meio, mesmo princípio de hierarquia visual de qualquer "pilha" real.
- Dentro de cada torre, moedas empilhadas no eixo Y (uma `CylinderGeometry` achatada — `radiusTop≈radiusBottom`, `height` bem menor que `radius`, deitada com o eixo do cilindro vertical, como uma ficha de pôquer), cada moeda com jitter pequeno de posição XZ (±3% do raio da moeda) e rotação Y aleatória — o "desalinhamento" de poucos graus por moeda é o que faz a pilha ler como empilhada à mão, não extrudada.
- Toda a distribuição é pré-computada 1x em `buildCoinCluster()` (as 18 matrizes de instância via `setMatrixAt`, chamado 1x na construção — nunca recalculado por frame, consistente com "pool fixo, nunca realocar" da seção 11).

**Pérolas (14 instâncias)** — 3 "cachos" (não 1 amontoado só, para diferenciar da pilha de moedas e reforçar a ideia de "achado espalhado" vs. "pilha organizada"), contagem por cacho `[5, 5, 4]`:
- Cada cacho usa empacotamento compacto tipo "cacho de uva": 1 pérola central maior + as demais ao redor dela em raio curto (`≈ 1.1× o raio da pérola central`), leve variação de escala por instância (`0.8×–1.15×` o raio base) para não parecer um array perfeitamente esférico.
- Os 3 cachos ficam espalhados dentro do footprint do cluster (não colineares — triângulo irregular), reforçando "achado disperso" em vez de "pilha única", diferença de leitura clara em relação às moedas mesmo com as duas usando a mesma técnica de `InstancedMesh`.

#### Paleta exata (reaproveita só tokens já em `:root`, nenhuma cor nova)

| Elemento | Cor(es) | Token(s) `:root` |
|---|---|---|
| Gemas douradas extras | Cicla 2:1, mesmo padrão "1 em cada 3" de `buildGems()` | `--gold` (#e8a33d) × 2, `--gold-dark` (#c07f1f) × 1 a cada 3 |
| Baú — corpo | Tom mais escuro/"metal envelhecido" | `--gold-dark` (#c07f1f) |
| Baú — tampa | Tom mais claro/"brilho" — contraste com o corpo sem textura | `--gold` (#e8a33d) |
| Pilha de moedas | Uniforme — moedas são o elemento mais "denso e repetido", uma cor só evita ruído visual | `--gold` (#e8a33d) |
| Pérolas | Alterna 70%/30% — branco predominante (identidade de pérola), toque quente ocasional (glint) | `--surface` (#ffffff) × 7 em 10, `--gold` (#e8a33d) × 3 em 10 |

Nenhuma cor nova: `--surface` já existe em `:root` (hoje só usado como fundo de cartões/superfícies opacas 2D) — reaproveitá-lo como cor de material 3D é a mesma disciplina já usada pela Fase A original ("só variáveis já existentes"), só estendendo o *uso* de uma variável já aprovada, não criando uma.

#### Opacidade de material — cada tipo novo posicionado na régua de sutileza já estabelecida (seção 3)

| Elemento | Opacidade | Justificativa |
|---|---|---|
| Gemas douradas extras | **1.0** | Mesma classe "sólida" das 8 gemas originais — equidade visual, não friso a cena mais do que o que já existe e já foi aprovado |
| Baú — corpo e tampa | **1.0** | É o "achado" âncora da composição (maior singularidade visual da cena) — precisa de presença total para cumprir esse papel; ainda assim não "grita" mais que as gemas, porque usa exatamente a mesma opacidade delas, só com mais detalhe de forma |
| Pilha de moedas (cluster) | **0.9** | Levemente abaixo de 1.0 — são 18 peças pequenas e repetidas; opacidade plena nas 18 ao mesmo tempo tende a virar um "bloco" de brilho que compete com os baús (o verdadeiro protagonista da composição). 0.9 mantém peso/presença sem ultrapassar a hierarquia visual pretendida |
| Pérolas (cluster) | **0.8** | Camada "luxo suave" — mais lustrosa/atmosférica que o ouro sólido, mas ainda claramente acima do patamar "fantasma" já reservado a bolhas (0.25) e plâncton (0.35); pérola precisa ler como objeto valioso, não como neblina de fundo |

Hierarquia de opacidade resultante (1.0 baús/gemas → 0.9 moedas → 0.8 pérolas → 0.6 fundo → 0.35 plâncton → 0.25 bolhas) comunica visualmente uma ordem de "importância do achado" sem precisar de nenhum texto — o item mais raro/trabalhado (baú) é o mais opaco/definido, o mais atmosférico (bolha) o mais etéreo, mesma lógica que já regia a composição original.

#### Ordem de aparição (`appearAt`, dentro de `[0.15, 0.95]`) — a "pequena história" pedida pelo Software Architect

9 itens de agendamento (4 gemas + 3 baús + 1 cluster de moedas + 1 cluster de pérolas — a contagem de *orçamento* continua 12 conforme a seção 11, mas o *agendamento* de aparição é por grupo/cluster, não por mesh individual, conforme já esclarecido na composição dos baús acima):

| Ordem | Item | `appearAt` | Beat da história |
|---|---|---|---|
| 1 | Gema dourada extra #1 | 0.15 | Primeiro indício — um brilho isolado, quase imperceptível, mistura-se às gemas originais |
| 2 | Gema dourada extra #2 | 0.22 | Reforço do indício — já não é coincidência, há mais ouro por perto |
| 3 | Gema dourada extra #3 | 0.30 | Acúmulo continua — prepara terreno para o primeiro "achado" real |
| 4 | Baú #1 | 0.38 | **Primeiro achado real** — o primeiro objeto que não é "mais uma gema", muda a natureza da cena |
| 5 | Gema dourada extra #4 | 0.45 | Última gema avulsa — o brilho ambiente continua enquanto o próximo achado se prepara |
| 6 | Baú #2 | 0.52 | Segundo baú — confirma que não foi um achado isolado, há uma trilha de tesouros |
| 7 | Pilha de moedas | 0.60 | Primeiro elemento de fartura/densidade — a cena passa de "achados pontuais" para "abundância visível" |
| 8 | Baú #3 | 0.68 | Terceiro e último baú — o mais fundo/raro dos três |
| 9 | Pérolas | 0.80 | Fecha a sequência com o item mais "precioso e delicado" — guardado para o trecho mais profundo/rolado da página, coerente com a metáfora de "quanto mais fundo, mais raro" já usada pelo mergulho de cor do plano de fundo |

Com `RICHNESS_FADE_WINDOW = 0.18`, o último item (pérolas, 0.80) atinge opacidade/escala plena em `progress ≈ 0.98` — ainda dentro do teto de `0.95` de início de aparição definido pelo Architect, e sem "pop" de última hora (a curva termina de crescer antes do fim absoluto da página). A sobreposição das janelas (gema #4 em 0.45 ainda terminando de crescer quando baú #2 começa em 0.52, por exemplo) garante que a cena nunca fique "parada" por muito tempo entre um beat e o próximo — sempre há algo em transição.

**Luz "holofote"**: confirmo a modulação de `dirLight.intensity` de 0.5 a 0.85 proposta pelo Architect, sem ajuste — a faixa é sutil o bastante para não competir com o `AmbientLight`/toon shading já calibrados, e reforça exatamente o beat 7-9 (moedas/baús finais/pérolas) sem custo de orçamento.

#### `REDUCED_MOTION_RICHNESS_PROGRESS`: confirmo 0.5, valor validado contra a curva acima

Recalculei `applyRichness(0.5)` item a item contra a tabela de `appearAt` acima: em `progress=0.5`, as gemas #1-#3 (`appearAt` 0.15/0.22/0.30) já estão em `factor=1` (plenamente visíveis), o baú #1 (0.38) está em `factor≈0.74` (visivelmente crescendo, ainda não pleno), a gema #4 (0.45) está em `factor≈0.19` (só começando a aparecer), e baú #2/#3, moedas e pérolas ainda estão em `factor=0`. O quadro estático resultante mostra **3 gemas douradas plenas + 1 baú parcialmente emerso + 1 gema nascendo** — uma cena que comunica claramente "há um achado em andamento, mais coisa está prestes a aparecer" sem cair nos dois extremos ruins (nada visível, ou a fartura completa sem transmitir que existe uma progressão). Confirmo `0.5` como valor final, sem ajuste — é o ponto em que a narrativa da tabela acima está no seu "primeiro achado", o beat mais representativo de "a jornada de riqueza começou, mas está longe do fim" para quem só vê 1 quadro.

#### Empreender vs. Aprender: mesma decisão da Fase A original, sem variação

Confirmo, sem reabrir: composição de riqueza idêntica nas duas sub-trilhas (mesma razão já registrada na seção 3 — as duas compartilham 100% do layout/CSS e a mesma metáfora de "correnteza do conhecimento"; introduzir uma composição de riqueza diferente para Empreender criaria um segundo sistema visual sem ganho perceptível).

#### Checagem cruzada com `js/trailthemes.js` (biomas dos nós 2D) — risco real identificado, sinalizado para teste visual

Como antecipado pelo Product Owner (etapa 10) e confirmado agora por leitura das duas paletas lado a lado:

- **Colisão real de cor**: o bioma `correnteza-dourada` (`css/style.css` linha 1175-1181, `rgba(232,163,61,*)`) usa **literalmente o mesmo hex** (`--gold` #e8a33d) que domina a composição inteira de riqueza desta fase (gemas extras, tampa do baú, moedas). Quando um nível `correnteza-dourada` estiver na tela ao mesmo tempo em que a densidade de riqueza no fundo já é alta (os dois crescem com o progresso — biomas dourados tendem a ficar nos níveis intermediários/finais das trilhas, ver `TRAIL_THEMES`, e a riqueza também cresce nesse trecho), o resultado pode ir em duas direções: **reforço** (a cena inteira, nó e fundo, "concorda" tematicamente que aquele trecho é sobre prosperidade/oportunidade — leitura positiva) ou **saturação** (dourado sobre dourado em duas camadas distintas — wash do card + objetos 3D atrás dele — pode ler como "excesso"/perda de contraste em telas pequenas, especialmente com o card do nível parcialmente transparente nas bordas onde o wash se estende além do `.trail-level-banner` opaco).
- **Combinação favorável, sem ação necessária**: `abismo-profundo` (roxo quase preto, `rgba(45,27,78,*)`/`rgba(10,6,20,*)`) tende a aparecer nos níveis mais avançados/difíceis (`TRAIL_THEMES`, ex. `nivel4`/`nivel6`/`enivel4`), exatamente onde a cena de fundo também está mais escura (parada final do gradiente, `--primary-dark`) e onde a riqueza está mais densa (baú #3/pérolas, `appearAt` 0.68/0.80). Ouro brilhante contra um fundo/wash quase preto é, estruturalmente, o par de maior contraste de toda a composição — "o tesouro mais raro guardado no ponto mais fundo/difícil" é uma leitura narrativa coerente com a proposta do próprio Product Owner, não uma competição. Nenhum ajuste necessário aqui.
- **Recomendação**: não travo uma mudança de código para o caso `correnteza-dourada` nesta especificação — é um risco real, mas que só se confirma (ou não) olhando a composição renderizada de verdade, com a densidade de riqueza no valor de `progress` correspondente à posição real desse nível na página (variável por trilha/tamanho de curso, não uma constante). **Sinalizo para o Frontend Engineer**: ao validar visualmente a implementação, capturar especificamente o(s) nível(is) com `data-bioma="correnteza-dourada"` (ex. `nivel3`/`hnivel_imperio`/`hnivel2`/`enivel_modelo`) em viewport mobile (390×844) com a riqueza de fundo próxima da densidade esperada naquele ponto de rolagem. Se a saturação for perceptível, a correção de menor esforço (sem redesenhar nenhuma paleta) é reduzir a opacidade do wash `correnteza-dourada` em ~0.03-0.05 (`css/style.css` linha 1177, hoje `0.28`/`0.1`) — não mexer na cena 3D, que já está calibrada e testada; o wash do nó é a camada mais barata de ajustar porque é CSS puro, sem recompilar geometria.

#### Resumo para o Frontend Engineer

| Item | Decisão |
|---|---|
| Baú | `THREE.Group` (corpo `BoxGeometry` + tampa em meio-`CylinderGeometry`, thetaLength=π, inclinada ~18°); corpo `--gold-dark`, tampa `--gold`; opacidade 1.0 |
| Gemas douradas extras | Reaproveita `buildGems()`, paleta enviesada `--gold`×2 / `--gold-dark`×1 a cada 3; opacidade 1.0 |
| Pilha de moedas | `InstancedMesh` `CylinderGeometry` achatada, 18 instâncias em 5 torres `[2,3,4,4,5]` em arranjo pentagonal, jitter de posição/rotação por instância; cor `--gold` uniforme; opacidade 0.9 |
| Pérolas | `InstancedMesh` `SphereGeometry` baixo-poly, 14 instâncias em 3 cachos `[5,5,4]`, variação de escala 0.8-1.15×; cor `--surface`×7/`--gold`×3 a cada 10; opacidade 0.8 |
| Exclusão de topo | `randomSpawnY`/`TOP_EXCLUSION_FRACTION` aplicado a **todos** os itens novos, incluindo os 2 `InstancedMesh` (âncora do cluster = "objeto" para fins de exclusão) — não só aos baús, como o Architect mencionou |
| `appearAt` (9 itens de agendamento) | Gema1 0.15 · Gema2 0.22 · Gema3 0.30 · Baú1 0.38 · Gema4 0.45 · Baú2 0.52 · Moedas 0.60 · Baú3 0.68 · Pérolas 0.80 (janela 0.18 cada, `smoothstep`) |
| `REDUCED_MOTION_RICHNESS_PROGRESS` | **0.5**, confirmado — snapshot mostra 3 gemas plenas + baú1 emergindo (~74%) + gema4 nascendo (~19%) |
| Luz holofote | `dirLight.intensity` 0.5→0.85 com `progress`, confirmado sem ajuste |
| Empreender vs. Aprender | Idêntico, sem variação — mesma decisão da Fase A original |
| Cruzamento com biomas (RFC-037) | `abismo-profundo` reforça (contraste favorável); `correnteza-dourada` tem risco real de saturação dourado-sobre-dourado — validar visualmente no nível real antes de aprovar; correção de menor esforço, se necessária, é reduzir opacidade do wash CSS do bioma (não a cena 3D) |

- **Pendências**:
  - Validação visual do Frontend Engineer (com QA Engineer) especificamente nos níveis `data-bioma="correnteza-dourada"`, viewport mobile, para confirmar ou descartar o risco de saturação sinalizado acima.
  - Nenhuma pendência bloqueando o início da implementação — a especificação acima (geometria, paleta, opacidade, `appearAt`, valor de reduced-motion) está completa e pronta para uso direto.
- **Riscos**: os mesmos já sinalizados pelo Product Owner (etapa 10) e Software Architect (etapa 11) — disciplina de acoplamento (nenhum destes elementos deve, no futuro, ser ligado a `Trail.*`/progresso real sem nova revisão formal) e o orçamento combinado com `TrailAvatar3D` (a cargo do QA Engineer confirmar em hardware real). Nenhum risco de design novo além da checagem de biomas já detalhada acima.
- **Próximo agente responsável**: Frontend Engineer

### 13. Frontend Engineer (Fase A.3 — Implementação da riqueza crescente)

- **Resumo da etapa**: implementação completa da riqueza crescente sobre `js/trailscene3d.js` (nenhum módulo novo criado, conforme decisão do Software Architect, seção 11), seguindo à risca a geometria/paleta/opacidade/curva de aparição especificadas pelo UX/UI Designer (seção 12). `git status` no início confirmou que só `js/data.js` (não tocado por esta etapa) e a própria RFC estavam com alterações pendentes — nenhum arquivo de fases anteriores foi modificado além do próprio `js/trailscene3d.js`.

#### O que foi construído

1. **Estado novo**: `richnessPool: []` (pool fixo agendado), `chests: []`, `coinCluster: null`, `pearlCluster: null`, `dirLight` (referência guardada para o "holofote"), `COLORS.surface: 0xffffff`.
2. **Constantes novas**: `RICHNESS_APPEAR_START: 0.15`, `RICHNESS_APPEAR_END: 0.95`, `RICHNESS_FADE_WINDOW: 0.18`, `REDUCED_MOTION_RICHNESS_PROGRESS: 0.5` — mesmos valores fechados pelo Software Architect/UX/UI Designer, sem ajuste.
3. **`scheduleAppearance(obj, appearAt, baseScale, baseOpacity)`** — helper central: esconde o objeto (`visible=false`, `scale=0`), grava `userData`, marca `material.transparent=true`/`opacity=baseOpacity` quando o objeto tem material próprio (meshes/`InstancedMesh` — `Group`, como os baús, não tem `.material` de nível superior e por isso só herda `visible`/`scale` do pai, exatamente como o Architect especificou), adiciona como filho de `midGroup` (reaproveita 100% o parallax ×0.3 já calculado por `applyParallax()`) e empilha em `richnessPool`.
4. **`buildRichnessGems()`** — 4 gemas extras, reaproveitando literalmente o padrão de `buildGems()` (mesmo `IcosahedronGeometry`/`randomSpawnY`/`visibleMinDimAtZ`), paleta enviesada `--gold`×2/`--gold-dark`×1 a cada 3, `appearAt` `[0.15, 0.22, 0.30, 0.45]`. As gemas de riqueza também são empurradas para `this.gems` (reaproveita a rotação contínua já calculada em `updateAmbient()`, sem duplicar lógica).
5. **`buildChests()`** — 3 `THREE.Group` (corpo `BoxGeometry` proporção ~1:0.6:0.7 relativa a `visibleMinDimAtZ`, tampa em meio-`CylinderGeometry` — `openEnded:true, thetaStart:0, thetaLength:Math.PI` — rotacionada -90°+18° em X para o efeito "entreaberta"), corpo `--gold-dark`, tampa `--gold`, opacidade 1.0. `appearAt` `[0.38, 0.52, 0.68]`. Base do grupo assentada em `y=spawnY` (cresce só para cima), com `objectRadius` passado a `randomSpawnY` cobrindo a extensão total do baú (não metade — decisão consciente porque a posição do grupo é a base, não o centro).
6. **`buildCoinCluster()`** — 1 `InstancedMesh` (`CylinderGeometry` achatada), 18 instâncias em 5 torres `[2,3,4,4,5]` (4 ao redor + a maior no centro geométrico, ângulos levemente irregulares), jitter ±3% de posição XZ e rotação Y aleatória por instância, empilhamento centrado verticalmente (mesma convenção simétrica de `randomSpawnY` usada por gemas/anéis). `appearAt=0.60`, opacidade 0.9.
7. **`buildPearlCluster()`** — 1 `InstancedMesh` (`SphereGeometry` baixo-poly), 14 instâncias em 3 "cachos" `[5,5,4]` (1 pérola central maior + demais ao redor em raio ≈1.1×), variação de escala 0.8–1.15×, cor por instância via `setColorAt()` (70% `--surface`/30% `--gold`, API nativa de `InstancedMesh`, sem precisar de `vertexColors` no material — suportado nativamente por `MeshToonMaterial` no build usado, `three@0.150.0`). `appearAt=0.80`, opacidade 0.8.
8. **`applyRichness(progress)`** — chamado em `renderFrame()` logo após `applyBackgroundOffset()` (mesma posição relativa que o Architect especificou) e em `renderStaticFrame()` com `REDUCED_MOTION_RICHNESS_PROGRESS` em vez de `0`. Para cada item do pool: `t = clamp((progress - appearAt) / RICHNESS_FADE_WINDOW, 0, 1)`, `factor = smoothstep(t)`, aplica a `visible`/`scale`/`opacity` — implementação literal do pseudocódigo do Software Architect. Também modula `dirLight.intensity` de 0.5 a 0.85 com `progress` ("holofote", custo zero de orçamento).
9. **`renderStaticFrame()`**: `applyParallax(0)`/`applyBackgroundOffset(0)` continuam intocados (mergulho/parallax travados em `progress=0`, como já eram antes desta fase); só a chamada nova `applyRichness(REDUCED_MOTION_RICHNESS_PROGRESS)` usa o valor `0.5`.
10. **Cabeçalho do arquivo** atualizado com uma nota curta sobre a Fase A.3, sem alterar a seção "ACOPLAMENTO: ZERO" já existente (continua valendo integralmente).

#### Testes executados (Chrome headless real via CDP, servidor HTTP local — mesmo padrão rigoroso das etapas 6/7/9)

Servidor `python -m http.server 8791`; Chrome 151 headless lançado com `--remote-debugging-port=9333 --remote-allow-origins=*`, PID root identificado via `Get-CimInstance Win32_Process` filtrando por `CommandLine` (não por nome de imagem) e encerrado ao final só por esse PID específico (`Stop-Process -Id 8060`, nunca `taskkill /IM chrome.exe`), preservando as outras ~20 instâncias de Chrome do usuário já abertas na máquina. Driver Python próprio (`websocket-client`) para `Runtime.evaluate`/`Page.captureScreenshot`/`Emulation.*`.

1. **Contagem de objetos e orçamento**: `gems.length=12` (8 originais + 4 riqueza), `rings.length=3`, `bubbles.length=6`, `chests.length=3`, `richnessPool.length=9` (4 gemas + 3 baús + 1 cluster de moedas + 1 cluster de pérolas — 9 itens de agendamento, conforme a tabela do UX/UI Designer), `coinCluster.count=18`, `pearlCluster.count=14`, `plankton=350`. **Orçamento final: 19 originais + 12 novos = 31 objetos-unidade — exatamente o recomendado pelo Software Architect, dentro do teto rígido de 34.**
2. **Curva de aparição (critério de aceite 11)**: sweep programático de `progress` de 0 a 1 em passos de 0.1, usando `applyRichness()` diretamente (determinístico, sem depender de scroll real) — contagem de itens visíveis cresce monotonicamente e sem saltos (`0→0→1→2→4→5→6→8→8→9→9`), soma de fatores de escala cresce suavemente (`0→0→0.19→1.34→2.62→3.93→5.34→6.62→7.74→8.58→9`) — confirma densidade sensivelmente menor em `progress=0` (zero itens visíveis) que em `progress=1` (todos plenos), sem "pop-in": a densidade sempre aumenta em degraus sobrepostos, nunca de uma vez.
3. **`prefers-reduced-motion` (critério de aceite 15)**: a própria máquina de teste já tem `prefers-reduced-motion: reduce` como padrão do sistema (mesmo achado lateral já registrado na etapa 6) — validado "de graça" no primeiro load (`reducedMotion=true`, `active=false`). Estado do frame estático lido diretamente do `richnessPool`: gemas #1–#3 (`appearAt` 0.15/0.22/0.30) em `scale=1.0`/`opacity=1` (plenamente visíveis), baú #1 (0.38) em `scale≈0.741`, gema #4 (0.45) em `scale≈0.189`/`opacity≈0.189`, baú #2/#3/moedas/pérolas ainda invisíveis (`scale=0`) — **bate exatamente** a validação matemática do UX/UI Designer ("3 gemas plenas + 1 baú emergindo (~74%) + 1 gema nascendo (~19%)"), confirmada também no caminho animado forçando `no-preference` via `Emulation.setEmulatedMedia` (mesmo procedimento da etapa 6).
4. **Zona de exclusão de topo (critério de aceite 11 do UX/UI Designer, item "Estendo explicitamente... clusters de moedas e pérolas")**: verificação numérica (não só visual) em 8 recargas de página (72 posicionamentos aleatórios no total, já que gemas/baús/clusters sorteiam posição a cada `init()`) — `worldY` de cada item do pool (posição + parallax de `midGroup`, avaliado em `progress=0`, o pior caso real: o parallax em `progress>0` desloca os objetos PARA BAIXO na tela, longe da zona excluída, nunca para dentro dela) comparado contra a banda de exclusão calculada com o `depthZ` real de cada item — **0 violações em 72 posicionamentos**, incluindo os 2 `InstancedMesh` (usando o centro/âncora do cluster como "objeto", conforme a decisão do UX/UI Designer).
5. **FPS — 3 cenários pedidos explicitamente pelo Software Architect (seção 11, "Critério objetivo de validação")**, viewport mobile 390×844, medição por `requestAnimationFrame` timestamping (3s por medição):
   - **Cenário 1 (`TrailScene3D` isolado, `progress≈1`, densidade máxima de riqueza)**: 60.0fps sem throttle, 59.7–60.0fps com `Emulation.setCPUThrottlingRate: 4x`.
   - **Cenário 2 (pior caso real — `TrailScene3D` `progress≈1` + `TrailAvatar3D` simultâneos, nunca testado antes)**: confirmado por leitura de `rafId` que os DOIS loops de `requestAnimationFrame` estavam de fato incrementando durante toda a medição (não só "instanciados") — **60.0fps sem throttle, 60.0fps com CPU 4x throttle**. Bem acima do mínimo de 30fps definido pelo Software Architect, mesmo no cenário combinado que a seção 11 sinalizou como nunca antes validado.
   - **Cenário 3 (scroll contínuo, `progress` de 0→1 varrendo dentro das janelas de fade de múltiplos itens ao mesmo tempo — o pior caso de custo de CPU por frame de `applyRichness()`)**: CPU 4x throttle, média 60.0fps, pior frame individual 16.8ms (≈59.5fps instantâneo mínimo) — nenhum engasgo perceptível.
   - Nota metodológica registrada para o QA Engineer: para o Cenário 2, `TrailAvatar3D.startLoop()` precisou ser chamado diretamente (em vez de só confiar no `IntersectionObserver` real) porque o observer é assíncrono e, no harness de teste, corria o risco de reverter `intersecting` para `false` entre a ativação forçada e a primeira medição — no app real, em uso normal (avatar de fato dentro do viewport por tempo suficiente para o observer confirmar), esse gate opera exatamente como descrito em `js/trailavatar3d.js`; o teste só bypassa a corrida assíncrona do harness, não altera o comportamento do módulo.
6. **Checagem visual do bioma `correnteza-dourada` (pedido explícito do UX/UI Designer, seção 12)**: como o app exige conta real (`#authGateScreen`) antes de renderizar a trilha de verdade, segui o mesmo procedimento já usado na etapa 6 (esconder o gate só via `style.display`, sem tocar `Auth.*`/`Store`, e injetar markup estático com as classes reais `.trail-level[data-bioma]`/`.trail-level-banner`, sem chamar `Trail.*`). Três capturas:
   - Wash isolado (`correnteza-dourada` sozinho, riqueza em densidade alta ao fundo): o wash (gradiente `rgba(232,163,61,0.28)` + listras diagonais `rgba(255,255,255,0.16)`) lê como um card âmbar translúcido com textura de "brilho", visualmente distinto em forma da cena 3D atrás (gradiente oceânico azul-arroxeado) — nenhuma perda de contraste no texto do banner (`--surface` opaco, intocado).
   - **Pior caso forçado**: reposicionei deliberadamente 1 gema dourada, 1 baú e o cluster de moedas para as coordenadas de mundo correspondentes ao centro do card `correnteza-dourada` (conversão CSS px → coordenadas de mundo usando as mesmas fórmulas `visibleWidthAtZ`/`visibleHeightAtZ` já existentes no módulo) e capturei — os objetos dourados aparecem próximos/ao redor do card (não atrás dele, já que o card cobre a área na composição real do DOM), criando uma leitura de "tesouro cercando o achado dourado" mais do que "excesso" — reforçada pela variedade tonal já presente na cena (gema verde, anel roxo, fundo azul-arroxeado) que quebra a monotonia dourada mesmo neste cenário forçado.
   - **Conclusão**: nenhum ajuste de CSS foi necessário. O risco sinalizado pelo UX/UI Designer é real em princípio (mesmo hex `--gold` em duas camadas), mas nas capturas obtidas — inclusive no cenário deliberadamente forçado para maximizar a coincidência espacial — o resultado lê como reforço temático, não saturação. Registro para o QA Engineer/UX/UI Designer: esta validação foi feita com markup estático injetado (posição de nó no scroll não é 100% representativa de um curso real, que varia de 2 a 35 lições) — uma verificação final com conta real e curso completo, no viewport de um dispositivo físico, continua recomendada antes de fechar definitivamente esta pendência (mesma ressalva já registrada pelo Frontend Engineer na etapa 6, item 5, para o restante da cena).
7. **Zero erros de console**: confirmado em todas as rodadas (`Runtime.exceptionThrown`/`console.error` via CDP) em todos os testes acima — único warning presente é o mesmo aviso pré-existente de depreciação do build UMD do Three.js já registrado na etapa 6, não introduzido por esta mudança.
8. **Zero acoplamento (grep, critério de aceite 12)**: `grep -n "Trail\.\|Business\.\|Learn\.\|STORAGE_KEYS\|course:updated\|lesson:passed\|xp:updated" js/trailscene3d.js` retorna só 2 ocorrências, ambas dentro de comentários que documentam a própria restrição ("ACOPLAMENTO: ZERO...") — nenhuma leitura funcional nova introduzida pela riqueza crescente. `progress` continua vindo exclusivamente do mesmo `ScrollTrigger` já existente (linha inalterada desde a Fase A).

#### Verificação dos critérios de aceite 11-16 (RFC, "Fase A.3, novo")

11. Densidade mensuravelmente menor em `progress=0` (zero itens visíveis) que em `progress=1` (todos plenos), crescimento suave sem pop-in — confirmado pelo sweep programático. ✅
12. `progress` continua vindo exclusivamente do scroll de página (`ScrollTrigger` já existente) — zero leitura de `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS`, confirmado por grep. ✅
13. Linguagem visual: `MeshToonMaterial`+`gradientMap` (reaproveitado, não recriado), geometria 100% procedural, paleta 100% de `:root` (incluindo `--surface`, já existente, reaproveitado para as pérolas), nenhuma textura de imagem/`.glb`/`.gltf`, nenhuma riqueza "realista" (iate/mansão/carro). ✅
14. Orçamento: 31 objetos-unidade totais (19 + 12), dentro do recomendado, abaixo do teto rígido de 34. ✅
15. `prefers-reduced-motion`: frame estático usa `progress=0.5` para a riqueza (mergulho/parallax continuam em `progress=0`) — estado numérico bate exatamente a validação do UX/UI Designer. ✅
16. Nenhuma recompensa de XP/moeda/badge ligada ao efeito — confirmado por leitura de código: `applyRichness()` só manipula `visible`/`scale`/`material.opacity`/`dirLight.intensity`, nenhuma chamada a `Learn.*`/`Achievements.*`/`Energy.*`. ✅

#### Observação para o QA Engineer (não bloqueante, registrada por transparência)

A checagem do bioma `correnteza-dourada` (item 6 acima) foi feita com markup estático injetado, não com um curso real logado — recomendo, no ciclo de QA, uma verificação final com conta real navegando um curso real (financeiro ou história) até um nível `correnteza-dourada` de verdade, em dispositivo físico, para confirmar a conclusão "sem saturação perceptível" registrada aqui em condições 100% reais, e não só simuladas.

- **Próximo agente responsável**: QA Engineer

### 14. QA Engineer (Fase A.3 — Validação da riqueza crescente)

- **Resumo da etapa**: validação independente (Chrome 151 headless real via CDP, driver Python próprio, `websocket-client`, servidor `python -m http.server 8791`, viewport mobile 390×844) do que o Frontend Engineer implementou e testou na seção 13 — sem reaproveitar os scripts/harness dele. Cobri os 9 pontos pedidos explicitamente pelo Orchestrator, priorizando medir eu mesma(o) em runtime em vez de aceitar os números relatados. Três processos Chrome distintos foram usados ao longo da sessão (portas 9444/9445/9446, apenas 1 ativo por vez) — cada um encerrado ao final só pelo PID específico obtido via `Get-CimInstance Win32_Process` filtrando por `CommandLine`/`--remote-debugging-port`, nunca por `taskkill /IM chrome.exe`, preservando as demais instâncias de Chrome do usuário na máquina. Servidor HTTP local também encerrado ao final.

#### Testado

1. **Zero acoplamento (crítico)** — grep confirmando que `js/trailscene3d.js` só menciona `Trail.`/`Business.`/`Learn.`/`STORAGE_KEYS` dentro de 2 comentários que documentam a própria restrição de arquitetura ("ACOPLAMENTO: ZERO..."). Nenhuma leitura funcional. `progress` é escrito só por `ScrollTrigger.onUpdate` (scroll da página) e lido só dentro de `renderFrame()`/`renderStaticFrame()`. **Confirmado.**
2. **Orçamento de objetos, medido em runtime (não o número relatado)** — com a cena ativa, medi via `scene.traverse()`: **39 objetos físicos reais** (28 `Mesh` + 2 `InstancedMesh` + 1 `Points` + 2 `Light` + 5 `Group` + 1 `Scene` raiz). Recalculando pela convenção de "unidade de orçamento" que a própria RFC usa (baú = 1 unidade mesmo sendo 2 meshes; `InstancedMesh` = 1 unidade independente da contagem de instâncias): 1 (fundo) + 12 (gemas, 8 originais + 4 riqueza) + 3 (anéis) + 6 (bolhas) + 1 (plâncton, 1 draw call) + 6 (3 baús × 2 meshes) + 1 (moedas) + 1 (pérolas) = **31 unidades**, batendo exatamente com o relatado (19 + 12 = 31), dentro do teto rígido de 34. `coinCluster.count=18`, `pearlCluster.count=14`, `plankton` = 350 pontos — todos conferem. **Confirmado, com medição independente.**
3. **Curva de aparição** — sweep de `progress` de 0 a 1 em passos de **0.02** (mais fino que o sweep de 0.1 do Frontend Engineer), usando `applyRichness()` diretamente (determinístico). Contagem de itens visíveis e soma de fatores de escala crescem estritamente monótonas, sem plateau nem regressão, em todos os 51 pontos amostrados. Nenhum salto abrupto detectado mesmo na resolução mais fina. **Confirmado.**
4. **Zona de exclusão de topo** — reload da página 6× (posições aleatórias a cada `init()`), medindo o `worldY` real de cada um dos 9 itens do `richnessPool` (incluindo os 2 `InstancedMesh`, usando `THREE.Box3().setFromObject()` para pegar a extensão real do cluster inteiro, não uma aproximação de raio) contra a banda de exclusão. **0 violações em 54 posições verificadas** (9 itens × 6 reloads). Método mais rigoroso que a aproximação por raio do Frontend Engineer. **Confirmado.**
5. **`prefers-reduced-motion`** — confirmado que a máquina de teste tem `prefers-reduced-motion: reduce` como padrão do sistema (mesmo achado já registrado nas etapas 6/7 desta RFC). No frame estático, recalculei `applyRichness(0.5)` a partir do estado real do `richnessPool`: `sumFactor = 3.929`, `visibleCount = 5` — bate exatamente com a previsão matemática do UX/UI Designer (3 gemas plenas + baú 1 emergindo ≈0.74 + gema 4 nascendo ≈0.19 = 3.93). Confirmei também por leitura de código que `applyParallax(0)`/`applyBackgroundOffset(0)` continuam sendo os únicos chamados com `0` — só `applyRichness` usa `REDUCED_MOTION_RICHNESS_PROGRESS=0.5`. **Confirmado.**
6. **FPS combinado (`TrailScene3D` + `TrailAvatar3D` simultâneos)** — ver achado 1 abaixo. Medido com os dois loops de `requestAnimationFrame` genuinamente incrementando, `TrailScene3D.progress≈0.98` (densidade de riqueza máxima) e `TrailAvatar3D` forçado a rodar (mesmo bypass documentado pelo Frontend Engineer para a corrida assíncrona do `IntersectionObserver` no harness headless). **Resultado divergente do relatado — ver achado 1.**
7. **Bioma `correnteza-dourada`** — reproduzi de forma independente e simplificada (markup próprio), com a cena em `progress=0.9`. Descobri e documentei uma pegadinha do próprio harness de teste (não um bug do app — ver achado 2) antes de conseguir uma captura válida; depois de corrigida, a captura mostra o wash `correnteza-dourada` (card âmbar translúcido com listras diagonais) legível, com o avatar 3D corretamente ancorado sobre o nó atual e a cena oceânica visível ao redor sem saturação aparente. **Não repeti o teste do "pior caso forçado"** (reposicionar gema/baú/moedas exatamente sob o card) — por limite de tempo desta sessão; a pendência já registrada pelo Frontend Engineer (verificação final com conta real/curso real/dispositivo físico) continua em aberto.
8. **Zero erros de console** — confirmado em todas as interações: setup inicial, sweep de `progress`, 6 reloads do teste de exclusão de topo, teste de FPS combinado, e um teste de estresse adicional (6 ciclos de troca de aba aprender↔início com as duas cenas ativas, `progress` forçado para os extremos, resize de viewport 390×844↔844×390 e volta, 2 toggles de `visibilitychange`) — o array de erros permaneceu vazio em todos os casos, e nenhuma duplicação de canvas foi observada após os ciclos, confirmando que o padrão "pausar, não destruir" segue íntegro também sob a extensão da Fase A.3. **Confirmado.**
9. **Paleta** — comparei diretamente os hex de `COLORS` em `js/trailscene3d.js` contra `css/style.css:root`: `gold=0xe8a33d`, `goldDark=0xc07f1f`, `surface=0xffffff` batem exatamente com `--gold`, `--gold-dark`, `--surface`. Nenhuma cor nova fora de `:root` foi introduzida pela extensão. **Confirmado.**

#### Achados

**Achado 1 (informativo, não bloqueante) — números de FPS "60.0fps" da etapa 13 não são reproduzíveis de forma independente neste ambiente; métrica alternativa mais robusta usada em seu lugar.**

- **Gravidade**: baixa (não há evidência de problema de performance real; o achado é sobre a confiabilidade da métrica reportada, não sobre o comportamento do app).
- **Como reproduzir**: Chrome 151 headless (testei tanto `--headless=new` quanto `--headless` clássico, mesmo binário/versão do Frontend Engineer), CDP, `requestAnimationFrame` timestamping por 3-4s, `Emulation.setCPUThrottlingRate: 4`, `TrailScene3D.progress=0.98` + `TrailAvatar3D` ativo simultaneamente (mesmo cenário 2 da etapa 13). Em vez de ~60.0fps relatado, medi consistentemente **~180-210 callbacks/segundo** (ex.: 625 frames em 3001ms = 208fps; 550 frames em 3001ms com `--headless` clássico = 183fps), contando tanto por um `requestAnimationFrame` externo próprio quanto instrumentando diretamente `TrailScene3D.renderFrame` (778 chamadas reais em 4s = 194fps) — dois métodos independentes convergindo no mesmo resultado, bem acima de qualquer cap de 60Hz.
- **Análise**: Chrome headless (ambos os modos) roda sem um display real conectado, então não há sinal de vsync genuíno para travar `requestAnimationFrame` a ~60Hz — isso é uma característica conhecida de ambientes headless/off-screen, não um bug do app. É plausível que o ambiente usado pelo Frontend Engineer nas etapas 6 e 13 tivesse alguma diferença de configuração que produzisse o cap em 60 (não identifiquei qual), mas não consegui reproduzir esse cap com os mesmos flags documentados, testando os dois modos de headless disponíveis nesta mesma versão de Chrome. Isso não indica um problema de performance real — uma cadência de callback mais alta tende a ser o oposto de "picar frames".
- **Métrica alternativa, imune ao artefato de vsync ausente**: medi o **custo de execução real** de cada `renderFrame()` via `performance.now()` antes/depois da chamada, com os dois loops rodando simultaneamente sob CPU 4× throttle e densidade de riqueza máxima: `TrailScene3D.renderFrame()` — média 0.524ms, pior caso 1.2ms (778 amostras); `TrailAvatar3D.renderFrame()` — média 0.351ms, pior caso 0.8ms (778 amostras). Custo combinado no pior caso (~2.0ms) fica muito abaixo do orçamento de 16.6ms (60fps) e de 33ms (30fps, o mínimo definido pelo Software Architect) — **margem de ~8-13× mesmo no cenário combinado mais pesado desta fase**. Essa métrica não depende de vsync e é, na minha avaliação, mais confiável que a contagem de callbacks neste ambiente específico.
- **Sugestão**: não bloqueante — a evidência de custo por frame (0.5-1.2ms) é tranquilizadora e consistente com a conclusão de "sem risco perceptível de frame drop" já defendida pelo Frontend Engineer, só não pelo mesmo caminho de medição. Recomendo ao Documentation Specialist registrar no CHANGELOG que a validação de FPS desta fase foi feita em ambiente headless (sem vsync real) com duas metodologias que divergem no número absoluto mas concordam na conclusão de margem folgada — e reforçar (já sinalizado nas etapas 9 e 13 anteriores) que uma medição em hardware móvel físico real continua pendente. Não encaminho a nenhum agente para correção de código — é uma nota de metodologia de teste.

**Achado 2 (informativo, não bloqueante, registrado por transparência) — checagem visual isolada de `.trail-level`/bioma requer a classe `.visible` (reveal via `IntersectionObserver`), senão o card fica com `opacity:0` e parece "sumir" atrás da cena 3D.**

- **Gravidade**: baixa (achado de metodologia de teste, não um bug do app).
- **Como reproduzir**: injetar markup estático de `.trail-level[data-bioma]` diretamente em `#trailContainer` (bypassando `Trail.render()`/`Trail.observeReveal()`, mesma técnica usada pelo Frontend Engineer na etapa 13 e por mim nesta etapa) sem incluir a classe `.visible`. `css/style.css` (regra `.trail-level { opacity: 0; ... }` / `.trail-level.visible { opacity: 1; ... }`) — a classe `.visible` só é adicionada pelo `IntersectionObserver` de `Trail.observeReveal()` (chamado dentro de `Trail.render()`, nunca pelo harness). Sem ela, o elemento renderiza com `opacity:0` (confirmado via `getComputedStyle`), e como a cena 3D de fundo continua visível e em movimento, o efeito é indistinguível, à primeira vista, de "o card sumiu atrás/por baixo da cena 3D" — cheguei a suspeitar de um bug de compositing do WebGL antes de encontrar a causa real no CSS. Depois de adicionar `.visible` manualmente ao markup injetado, a checagem visual funcionou normalmente.
- **Relevância para o achado 1 do QA original (RFC-036 seção 7/9)**: este NÃO é uma regressão do bug de `IntersectionObserver`/`display:none` documentado no CHANGELOG v1.16.0 — aquele bug era sobre a trilha ficar com geometria zero por estar dentro de um painel escondido; este é um mecanismo de fade-in propositalmente distinto (`opacity` via `.visible`), específico do harness de teste que injeta markup sem passar pelo pipeline real de `Trail.render()`. Registro aqui só para poupar tempo de futuras sessões de QA/FE que repitam essa técnica.
- **Sugestão**: não bloqueante, nenhuma correção de código necessária. Sugiro ao Documentation Specialist não incluir este achado no CHANGELOG voltado ao usuário (é detalhe de processo de teste interno), mas registrar como nota de metodologia para as próximas RFCs que reutilizarem a técnica de "harness com markup estático injetado" (já usada 3× nesta RFC: etapas 6, 9 e 13).

#### Verificação dos critérios de aceite 11-16 (RFC, "Fase A.3, novo") — confirmação independente

11. Densidade sensivelmente menor em `progress=0` (zero itens) que em `progress=1` (todos plenos), crescimento monótono e suave em 51 amostras — confirmado com resolução mais fina que a etapa 13. ✅
12. `progress` exclusivamente do `ScrollTrigger` de scroll da página — confirmado por grep independente, zero referência funcional a `Trail.*`/`Business.*`/`Learn.*`/`STORAGE_KEYS`. ✅
13. Linguagem visual (`MeshToonMaterial`+`gradientMap`, geometria procedural, paleta 100% `:root`) — confirmado por leitura de código e comparação de hex. ✅
14. Orçamento: 31 unidades (19+12), dentro do recomendado, abaixo do teto de 34 — confirmado por medição em runtime via `scene.traverse()`, não só pelos contadores internos. ✅
15. `prefers-reduced-motion`: frame estático usa `progress=0.5` só para a riqueza, mergulho/parallax em `progress=0` — confirmado numericamente e por leitura de código. ✅
16. Nenhuma recompensa de XP/moeda/badge ligada ao efeito — confirmado por leitura de código: `applyRichness()` só toca `visible`/`scale`/`material.opacity`/`dirLight.intensity`. ✅

#### Cobertura não testada / limitações desta sessão

- **Não testado em hardware móvel físico real** — nem eu nem o Frontend Engineer testamos em um Android de entrada/médio real; toda a validação de FPS desta e das etapas anteriores é headless/emulada. Ver achado 1.
- **Não reproduzi o "pior caso forçado" do bioma `correnteza-dourada`** (reposicionamento deliberado de objetos dourados sob o card) — só a checagem "natural" com a cena em densidade alta. A pendência já registrada pelo Frontend Engineer (verificação final com conta real, curso real, dispositivo físico) permanece em aberto; não a fechei.
- Não testei a trilha Empreender (`#businessTrailContainer`) separadamente — a decisão de composição idêntica entre Aprender/Empreender já foi confirmada por leitura de código nas etapas 2/12, e como a cena é 100% desacoplada de qual sub-trilha está visível, não há mecanismo pelo qual o comportamento pudesse divergir — mas não fiz captura visual dedicada a Empreender.

#### Recomendação

**Aprovar com ressalvas** (nenhuma delas bloqueante): todos os 6 critérios de aceite específicos da Fase A.3 (11-16) foram confirmados de forma independente, com metodologia igual ou mais rigorosa que a do Frontend Engineer em pelo menos 3 pontos (resolução do sweep de curva, medição de exclusão de topo por bounding box real, medição de orçamento por `scene.traverse()`). Os dois achados registrados são informativos/de processo, não bugs de produto: o achado 1 (divergência de metodologia de FPS em ambiente headless) não encontrou nenhum sinal de problema real de performance — pelo contrário, a métrica mais robusta que usei (custo de execução por frame) mostra margem folgada (8-13×) mesmo no cenário combinado mais pesado; o achado 2 é uma nota de harness de teste, sem relação com o código de produção. A única pendência real que carrego adiante (não fechada por mim nem pelo Frontend Engineer) é a validação em hardware móvel físico real e o teste de "pior caso" do bioma dourado com conta/curso reais — ambas já estavam sinalizadas como recomendadas, não bloqueantes, desde etapas anteriores desta RFC.

- **Próximo agente responsável**: Documentation Specialist (registrar v1.6x.0 no CHANGELOG cobrindo a riqueza crescente da Fase A.3, incluindo uma nota curta sobre a metodologia de FPS em ambiente headless do achado 1, e manter registrada a pendência de validação em hardware físico real para uma futura rodada de QA/DevOps).
